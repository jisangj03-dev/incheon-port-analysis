# 세션 개시 — 이 파일은 매 세션 자동으로 읽힌다

> **그래서 부트블록을 붙여넣지 않아도 된다.** 운영자가 「일하자」 한 마디만 해도 아래가 돈다.
> `본부\부트블록.md`는 **다른 기계·다른 도구에서 시작할 때의 수동 경로**로 남는다.
>
> **여기에 상태를 적지 않는다.** 상태는 4단계 STATUS 덤프가 든다.
> 부트블록 구판이 이틀 만에 낡은 원인이 그것이다 — 수치만 뺐지 **질적 상태**를 박아 뒀다.
> **사고 31은 수치에만 걸리는 교훈이 아니다.**

## 이 프로젝트가 무엇인가 — 한 줄

**인천항 물동량을 공공 1차 데이터로 확인해 발행한다. 대상은 인천항 하나다.**
2026-08-28 운영자 선언으로 축이 좁혀졌다(지침 §1.3). **무역·유통은 열지 않는다.**
**「물류·무역·유통 3산업」·「라인 A/B/C/D」·「3산업 순차 개방」이 나오면 v5.x 이하가 어딘가 살아 있는 것이다.**

## 맥락원 — 이 둘이 전부다. 기억이 아니라 파일을 읽는다

| | 무엇 | 어디 |
|---|---|---|
| 1 | **지침(규범)** | `본부\프로젝트지침.md` — 경로 고정. **버전은 파일 첫 줄에서 읽는다** |
| 2 | **상태(정본)** | `docs/STATUS.md` — 현재 상태·다음 할 일·미해결 |

보조 — `docs/FACTS.md`(수치 정본) · `docs/채널대기열.md`(**게시 상태 정본** — 읽기 `python analysis/channel_queue.py`) · `docs/사고기록.md`(터진 일. 번호 재사용 안 한다) ·
`docs/작업기록.md`(**라운드별 경위. 「왜 그렇게 했나」가 여기 있다** — STATUS 는 지금 참인 상태만 든다) ·
`.claude/skills/발행/SKILL.md`(절차) · `본부\아카이브\`(구판. **여기 규칙을 따르지 마라**)

**작업 폴더 셋** — 이 저장소(발행본·재현 자산 · **GitHub Pages 가 곧 인천 사이트**) / `..\sounding`(**측심 사이트** · Higgsfield 배포 · 절차 = `tools/RUNBOOK.md`) /
`..\..\본부\`(= `OneDrive\문서\본부` · 비공개. 저장소로 옮기지 않는다).
`..\jisangj03-dev.github.io` 는 **없앤 옛 허브**다 [2026-09-12 운영자 결정] — HTTP 404 · 원격 브랜치 없음. **인천·측심 안에서 그것을 향하던 참조를 전부 끊었다**(사고 115). 저장소 자체는 밖이라 **만지지 않는다.**

## 매뉴얼이 아니다 — 지침 §0.1

**지침은 「지켜야 할 것」만 든다. 「어떻게 하는가」는 코드와 SKILL에 있다.**

- **판단 우선.** 조항이 상황에 안 맞는다고 판단되면 **따르지 말고 그 판단을 보고한다.**
- **예외는 둘뿐 — §4 정지선과 §2.2 표기 의무.** 이 둘은 판단으로 못 바꾼다.
- 조항이 판단을 대신하기 시작하면 조항 밖의 것이 안 보인다.
  **값이 가장 컸던 것은 매번 지시 밖·조항 밖에서 나왔다.**

## 저작 구조 B — 되돌아가지 않는다

AI가 생성하고 사람이 운영한다. **H1 부제·한 줄 결론·§1·§2·§3·§4를 AI가 쓴다.**
대필 금지 조항과 문답 세션 절차는 폐기됐다(v6.0).
**「요약과 해석은 회장 소유다」·「문답으로 만들자」·「3계층 분업」이 나오면 v1.2가 살아 있는 것이다 —
그 사실부터 보고한다.** **공개물에 생성 주체를 적지 않는다**(v6.2 운영자 결정 — 표기 블록은 3항목).
표기 블록은 **실제로 한 일만** 적고, **사람이 쓰지 않은 것을 사람이 썼다고 적지 않는다**(§2.2-4).
**검수도 그렇다** [v7.0 · §2.3] — 통독·수치 대조는 **하는 쪽이 한다**(운영자든 세션이든). 발행 게이트가 아니라 **표기의 근거**이고, **안 한 편에는 검수 표기를 안 단다.**
**갈래는 셋이다** [2026-09-09] — **주제·축·판정 기준은 운영자** / **수집·계산·차트·검사·발행은 코드** / **검수는 한 만큼만.**
지침 §2.1 의 역할 조항을 고치면 **`about.md`·`README.md`·채널 문안을 같은 커밋에서 연다** — 그 셋이 같은 주장을 되풀이해 적는다(사고 108).

## 진행 방식 — 최대 자율 위임

실행 판단은 전부 이쪽에 있다. 방법·구조·순서·도구·파일 배치·검산 설계·커밋 단위·문서 체계.
**[2026-09-09 · 지침 v7.0] 지침 개정도 이쪽이 한다**(§0-4). 운영자 선언이 필요한 것은 **§1.3 축** 하나다.
조항이 낡았으면 고치고 개정 이력에 남긴다 — **못 고치는 지침이 다음 세션의 판단을 대신하는 것**이 §0.1이 막는 것이다.

- **되묻지 않는다.** 하고 나서 결과와 근거를 낸다. 진행 중계 없이 결과만.
- 지시가 실측 없는 전제 위에 서 있으면 **실측부터 하고 뒤집는다.**
- 확인 못 한 것은 **[미확인]**. 근거 없는 통과 판정 금지.
  **확인 명령을 쓸 수 없는 주장은 주장이 아니라 추정이다**(사고 47·48).
  특히 **도구가 닿지 않는 것**(Claude.ai 설정 등)은 단정하지 않는다.
- 되묻는 것은 둘뿐 — ① 정지선·정직 하한선 위반 지시(거절하고 대안) ② 결과물이 근본적으로 갈리는 갈림.
- **하다가 걸리는 것이 있으면 그것부터 보고한다.**

## 채널 게시 — 준비까지 하고 멈춘다

**대기열이 정본이다.** 「지금 뭐 대기 중이야」 = `python analysis/channel_queue.py`.
검사 `--check --net` · 규칙 `python analysis/channels.py --list` · 문안 `본부\채널문안\<id>.md`.

- **문안을 브라우저에 넣는 데까지 하고, 승인을 받은 뒤 누른다.**
- **본문을 옮겨 적지 않는다.** `channel_post.py --clip` → Ctrl+V → `--verify --got <해시>`.
  **대조는 파일과 한다** — 사본끼리 대면 어떤 정밀도로도 「불일치 0」이 나온다(사고 109).
- **GeekNews 는 두 시간 뒤 못 지운다.** 게시 전 **전문**을 보이고 승인받는다. 자기 것은 `Show GN:` 으로.
- **로그인·자격 증명은 다루지 않는다.** 만료면 알리고 멈춘다. 자동 제출 스크립트는 만들지 않는다.
- 게시 뒤 `--set <id> --state 게시됨 --url <주소>` · 그 주소를 열어 **카드가 실제로 떴는지** 본다.

## 정지선

**정본은 지침 §4다. 여기 옮겨 적지 않는다** — 사본은 반드시 원본과 갈라진다.
**정지선은 셋뿐이다**(v7.0) — 되돌릴 수 없는 것 · 데이터 무결성 · 사람 손. **그 밖은 판단이다.**
**기전과 그 한계는 `docs/STATUS.md` 「정지선 ↔ 기전 대조표」**(생성됨. **문면과 기전은 같지 않다**).

**[2026-09-09 운영자 위임 · 지침 v6.4] push·배포는 세션이 친다.** 인천·측심 `git push origin main` · `python tools/push.py` · `higgsfield website deploy|status|publish`. 커밋은 이쪽 판단.
**훅은 금지가 아니라 검증이다** — `hook_stopline.py` 는 커밋 앞에서 앵커를 검사하고, `pre-push` 는 난간 여덟을 돌린다.
**그중 하나만 막는다** [v7.0]: **난간 ⑦ 공개 유출**(`check_private --strict`). 기준은 하나다 —
**push 가 되돌릴 수 없게 만드는 것만 막는다.** 나머지 일곱은 push 뒤에 고쳐지므로 경고다.
**되돌릴 수 없는 것만 정지선으로 남는다** — 힘 push · 이력 수정 · 저장소·브랜치 삭제 · 토큰 갱신(`repo-access`) · 계정 조작(링크드인 등 게시). 집행 = `.claude/settings.json` `permissions.deny`(접두 매칭 · 문자열 한계) + 자동 모드 `hard_deny`(사용자 설정 · 분류기가 읽는다). 배포가 크레딧을 쓰는 것으로 확인되면 멈추고 보고한다(09-09 실측은 0 → 0).
**구성의 실물과 위임 규칙 = `본부\구성_실측_20260909.md`**(채팅 「물류」 프로젝트의 첫 파일). **분류기가 막으면 우회하지 않고 막힌 지점을 보고한다.**

## 첫 응답에서 할 것 — 이 순서로, 전부

```
1. git rev-parse HEAD            # 폴더 확인. 그 외 시작 의례 없음
2. 맥락원 1·2를 읽는다           # 기억으로 대신하지 않는다
3. 검사 장치가 실제로 발화하는지 확인한다 — **한 줄이다**:
     python analysis/boot_check.py
   (`--selftest` 를 가진 스크립트를 **찾아서** 전부 돌리고 상태 검사까지 한 판정으로 낸다.
    브라우저를 쓸 일이 있으면  python analysis/check_browser.py  를 따로.)
4. docs/STATUS.md 전문을 그대로 덤프한다   # 요약 금지. 상태의 유일한 반입 경로다
5. STATUS 「착수점」 절부터 읽고 「다음 할 일 · A」 첫 항목에 착수. 막히면 막힌 지점을 보고한다
6. 턴을 닫기 전에 — 터진 것은 docs/사고기록.md(번호 이어서) · 경위는 docs/작업기록.md(끝에 붙인다) ·
   지금 참인 상태는 docs/STATUS.md. 그리고 커밋·push. 세션 끝 갱신은 이쪽 몫이다(2026-09-09 위임)
```

**3번의 이유:** 2026-08-25에 린터 인과 탐지가 #01~#07 전편에서 **한 번도 발화한 적이 없음**이 드러났다.
그때의 "FAIL 0"은 통과가 아니라 **부재**였다. **장치의 존재는 검사의 수행이 아니다**(사고 26).

**3번이 한 줄인 이유:** 여기 명령 여덟 줄이 적혀 있는 동안 장치는 그 몇 배로 늘었다.
**목록을 손으로 들면 낡고, 낡은 목록으로 「전부 쳤다」고 말하면 그것이 다시 사고 26이다.**
그래서 목록을 안 들고 **찾는다**. 새 장치를 만들 때 여기 고칠 것은 없다.

**맥락원이 하나 늘었다** — `docs/작업기록.md`(라운드별 경위).
STATUS 는 **지금 참인 상태**만 들고, 끝난 라운드의 「왜 그렇게 했나」는 거기 있다.
`boot_check.py` 가 STATUS 크기를 재고 상한을 넘으면 **멈춘다.**

## X-CHECK 2계층

한 쪽은 Claude.ai 챗이다. **역할 정본은 지침 §8.1** — ①사실 전달 ②X-CHECK ③위임문 작성, 그뿐이다.
**챗은 설계하지 않는다.** 방법·순서·우선순위·로드맵이 챗에서 나오면 그 자체가 §8.1 위반이다.
**가동 여부는 여기 적지 않는다. `docs/STATUS.md`가 든다.**

- **챗이 강한 자리는 사실이 아니라 자기모순이다** — 챗은 파일을 못 봐 사실에는 이쪽과 같이 틀렸다(사고 47).
  **그러므로 챗에는 파일이 아니라 이쪽 산출물의 본문을 준다.**
- **독립성은 지시로 성립한다.** 넘길 때 「판정결과 문서를 열지 말라」를 **명시**한다.
- **챗의 사실 주장에는 확인 명령을 같이 받는다.** 명령을 못 붙이는 주장은 추정이다.
- **이쪽이 할 일은 챗의 가동 여부와 무관하다.** 보고할 때 **「이 결론이 내 앞 문장과
  어긋나지 않는가」를 스스로 한 번 치고, 못 친 것은 못 쳤다고 적는다.**
  2계층이 있다고 1계층을 안 치면 **둘 다 안 친 것과 같아진다**(사고 47).
- **§3-10은 셋이다.** 챗↔Code 양방향에 더해 **운영자가 구조적 설계 결함을 잡는다**가 있다.
  **방향을 하나만 남기면 그 검출 경로가 사라진다.**
