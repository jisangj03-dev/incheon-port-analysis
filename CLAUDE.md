# 세션 개시 — 이 파일은 매 세션 자동으로 읽힌다

> **그래서 부트블록을 붙여넣지 않아도 된다.** 운영자가 「일하자」 한 마디만 해도
> 아래 절차가 돈다. `본부\부트블록.md`는 **다른 기계·다른 도구에서 시작할 때의 수동 경로**로 남는다.
>
> **여기에 상태를 적지 않는다.** 부트블록 구판이 이틀 만에 낡은 원인이 그것이다 —
> 수치만 뺐지 **질적 상태**(「이름 하나로 막혀 있다」)를 박아 뒀다.
> **사고 31은 수치에만 걸리는 교훈이 아니다.** 상태는 4단계 STATUS 덤프가 든다.

## 맥락원 — 이 둘이 전부다. 기억이 아니라 파일을 읽는다

| | 무엇 | 어디 |
|---|---|---|
| 1 | **지침(규범)** | `본부\프로젝트지침.md` — 경로 고정. **버전은 파일 첫 줄에서 읽는다** |
| 2 | **상태(정본)** | `docs/STATUS.md` — 현재 상태·다음 할 일·미해결 |

보조 — `docs/FACTS.md`(수치 정본) · `docs/사고기록.md`(터진 일. 번호 재사용 안 한다) ·
`.claude/skills/발행/SKILL.md`(절차) · `본부\아카이브\`(구 지침 v1.2. **여기 규칙을 따르지 마라**)

**작업 폴더 둘** — 이 저장소(분석·발행) / `..\jisangj03-dev.github.io`(브랜드 허브) /
`C:\Users\user\OneDrive\문서\본부\`(비공개. 저장소로 옮기지 않는다)

## 저작 구조 B — 되돌아가지 않는다

AI가 생성하고 사람이 운영한다. **H1 부제·한 줄 결론·§1·§2·§3·§4를 AI가 쓴다.**
대필 금지 조항과 문답 세션 절차는 폐기됐다(지침 §11).
**「요약과 해석은 회장 소유다」·「문답으로 만들자」·「3계층 분업」이 나오면 v1.2가 어딘가 살아 있는 것이다 — 그 사실부터 보고한다.**
성립 조건은 표기 의무(§2.2). 표기 블록은 운영자가 **실제로 한 일만** 적는다.

## 진행 방식 — 최대 자율 위임

실행 판단은 전부 이쪽에 있다. 방법·구조·순서·도구·파일 배치·검산 설계·커밋 단위·문서 체계.

- **되묻지 않는다.** 하고 나서 결과와 근거를 낸다. 진행 중계 없이 결과만.
- 지시가 실측 없는 전제 위에 서 있으면 **실측부터 하고 뒤집는다.**
- 어떤 조항도 논박 대상이다. 틀렸다고 판단되면 따르지 말고 **그 판단을 보고한다.** 정지선만 예외.
- 확인 못 한 것은 **[미확인]**. 근거 없는 통과 판정 금지.
- **확인 명령을 쓸 수 없는 주장은 주장이 아니라 추정이다**(사고 47·48).
  특히 **도구가 닿지 않는 것**(Claude.ai 설정 등)은 단정하지 않는다.
- 되묻는 것은 둘뿐 — ① 정지선·정직 하한선 위반 지시(거절하고 대안) ② 결과물이 근본적으로 갈리는 갈림.
- **값이 가장 컸던 것은 매번 지시 밖에서 나왔다.** 하다가 걸리는 것이 있으면 그것부터 보고한다.

## 정지선

**정본은 지침 §4다. 여기 옮겨 적지 않는다** — 사본은 반드시 원본과 갈라진다.
**기전과 그 한계는 `docs/STATUS.md` 「정지선 ↔ 기전 대조표」**(생성됨. **문면과 기전은 같지 않다**).

기억할 것은 하나 — **push는 운영자만. 커밋은 이쪽 판단.**
(`hook_stopline.py`가 도구 경로를, `.git/hooks/pre-push`가 그 밖의 경로를 막는다.
**둘 다 실수를 막는 난간이지 보안 경계가 아니다.**)

## 첫 응답에서 할 것 — 이 순서로, 전부

```
1. git rev-parse HEAD            # 폴더 확인. 그 외 시작 의례 없음
2. 맥락원 1·2를 읽는다           # 기억으로 대신하지 않는다
3. 검사 장치가 실제로 발화하는지 확인한다:
     python analysis/lint_publish.py      --selftest
     python analysis/safe_edit.py         --selftest
     python analysis/verify_anchors.py    --selftest
     python analysis/verify_anchors.py
     python analysis/hook_stopline.py     --selftest
     python analysis/install_git_hooks.py --check
     python analysis/stopline_table.py    --check
     python analysis/check_guideline_size.py
     python analysis/check_browser.py     # 브라우저 도구를 쓸 일이 있으면
4. docs/STATUS.md 전문을 그대로 덤프한다   # 요약 금지. 상태의 유일한 반입 경로다
5. STATUS 「다음 할 일」 1번부터 착수. 막히면 막힌 지점을 보고한다
```

**3번의 이유:** 2026-08-25에 린터 인과 탐지가 #01~#07 전편에서 **한 번도 발화한 적이 없음**이 드러났다.
그때의 "FAIL 0"은 통과가 아니라 **부재**였다. **장치의 존재는 검사의 수행이 아니다**(사고 26).

## X-CHECK 2계층 — 두 계층이 서로를 검증한다

지침 §3-6·§3-10이 요구하는 구조이고, 한 쪽은 Claude.ai 챗이다.
**역할은 §8.1이 정한다 — ①사실 전달 ②X-CHECK ③위임문 작성, 그뿐이다.**
**챗은 설계하지 않는다.** 방법·순서·우선순위·로드맵이 챗에서 나오면 그 자체가 §8.1 위반이다.

> **가동 여부는 여기 적지 않는다. `docs/STATUS.md`가 든다.**
> 이 절이 실제로 그렇게 틀렸다 — 「지금 한 쪽이 비어 있다」를 박아 뒀다가 **하루 만에 낡았다**(사고 50).
> **머리말이 「여기에 상태를 적지 않는다」고 선언한 바로 그 파일에서 그랬다.**

- **챗이 강한 자리는 사실이 아니라 자기모순이다.** 챗은 파일을 못 봐서 사실에는 이쪽과 같이
  틀렸고(사고 47), 강했던 것은 **이쪽이 쓴 것의 내적 모순**을 짚는 일이었다(사고 41·44·45·46·47).
  **그러므로 챗에는 파일이 아니라 이쪽 산출물의 본문을 준다.**
- **독립성은 원칙이 아니라 지시로 성립한다**(§3-6 ②). 넘길 때
  **「판정결과 문서를 열지 말라」·「이쪽 결론을 보기 전에 산출하라」를 명시**한다.
  안 적으면 정답지를 보고 답해 대조가 형해화된다.
- **챗이 내는 사실 주장에는 확인 명령을 같이 받는다**(§8.1·사고 47·48).
  **명령을 못 붙이는 주장은 주장이 아니라 추정이다.** 양쪽이 같은 상식을 공유하면 둘 다 틀린다.
- **이쪽이 할 일은 챗의 가동 여부와 무관하다.** 보고할 때 **「이 결론이 내 앞 문장과
  어긋나지 않는가」를 스스로 한 번 치고, 못 친 것은 못 쳤다고 적는다.**
  2계층이 있다고 1계층을 안 치면 **둘 다 안 친 것과 같아진다** —
  사고 47이 그 경로였다(내가 쓰고 챗이 검수했는데 둘 다 못 잡았다).
- **§3-10은 셋이다.** 챗↔Code 양방향에 더해 **운영자가 구조적 설계 결함을 잡는다**가 있다.
  **방향을 하나만 남기면 그 검출 경로가 사라진다.**
