# 인천항만공사 물동량 API 탐색 결과 (2026-07-08)

## 핵심 구조
- 공통 경로: `https://apis.data.go.kr/B551504/ipaGtqyProcessAcmslt/{오퍼레이션}`
- 우리 인증키로 **이 서비스 폴더(ipaGtqyProcessAcmslt) 하나만** 호출 가능.
  다른 폴더명은 전부 HTTP 500(미승인/경로없음).
- 게이트웨이 응답 신호:
  - HTTP 500 `Unexpected errors` → 폴더(서비스) 자체가 우리 키에 없음
  - HTTP 404 `API not found` → 오퍼레이션 이름이 없음
  - HTTP 200 `resultCode=00` → 정상
  - HTTP 200 `resultCode=99 Invalid parameter` → 오퍼레이션은 있으나 요청 파라미터가 틀림

## 존재하는 오퍼레이션 3개 (실측)
| 오퍼레이션 | 상태 | 파라미터 | 반환 내용 | 수출입/환적 구분 |
|---|---|---|---|---|
| `getGtqyProcessAcmsltMnby` | ✅ 정상 | `yyyy` | 월별 **연안(coastal)** 물동량 12행 (톤 추정) | ❌ 연안만 나옴 |
| `getAllFrghtGtqyMnthng` | ✅ 정상 | `yyyy` (+`mm`?) | **전체 화물 물동량** 총계, 최신 1개월만 반환 | ❌ 총량만, 방향 없음 |
| `getCntanrLandngLoading` | ⚠️ 파라미터 미상 | 미상(rc=99) | 컨테이너 **양/적하** 추정 | 미확인(가능성 있음) |

### getGtqyProcessAcmsltMnby 필드
`ym, inoutgbnm(=연안), selYearSelMonth(당월), selYear(누적), preYearSelMonth(전년동월), preYear(전년누적)` 등.
구분 파라미터(inoutGbn/ioGbn/gbn 등) 다수 시도 → 전부 무시되고 연안만 반환.

### getAllFrghtGtqyMnthng 필드
`yyyy, mm, sumToday(당월), sumPre(전월), sumVsslTot, sumPreTot, rate...`
yyyy=2023 단독 호출 시 mm=11 한 행만 옴(최신월 스냅샷).

### getCntanrLandngLoading
날짜/기간/터미널/구분 계열 파라미터 30여 종 시도 → 전부 rc=99.
→ data.go.kr 활용가이드(참고문서)에서 정확한 요청변수명 확인 필요.

## 결론
현재 승인된 3개 오퍼레이션 중 **월별 수출입/환적 컨테이너 물동량**을 그대로 주는 것은 없음.
- `getCntanrLandngLoading`가 유일한 컨테이너 레벨 → 요청변수만 확보하면 가장 유망.
- 확실한 수출입/환적 TEU가 필요하면 컨테이너 전용 데이터셋 추가 활용신청 검토:
  - data.go.kr 15003936 (인천항 컨테이너 정보)
  - data.go.kr 15130235 (인천항 공컨테이너 화물 통계) — 월별 수출입 구분·TEU 포함으로 알려짐

---

## 보고서 #02 추가 탐색 (2026-07-09) — "월별 전체 컨테이너 TEU" 존재 여부
목표: 적컨+공컨 합계인 **전체 컨테이너 TEU를 월별**로 주는 데이터가 승인된 15003817에 있는지 확인.
확인 스크립트: `explore_total.py` (item 태그 원본 덤프 방식).

### 실측 결과
| 오퍼레이션 | 반환 태그 | 판정 |
|---|---|---|
| `getAllFrghtGtqyMnthng` | `mm, yyyy, sumToday, sumPre, sumVsslTot, sumPreTot, rate…` | ❌ **전체 화물 톤수(선박, Vssl)**. 컨테이너·TEU 태그 없음. 게다가 **최신 1개월(11월)만** 반환(totalCount=1). 값 크기(월 ~1,170만)도 TEU가 아닌 톤 단위. |
| `getGtqyProcessAcmsltMnby` | `ym, inoutgbnm(=연안), selYearSelMonth…` | ❌ **연안 화물 톤수**만. 컨테이너/TEU 아님. (12개월 제공되나 대상이 연안) |
| `getCntanrLandngLoading` | (호출 불가) | ⚠️ 컨테이너 레벨 유일 후보지만, 요청변수 6종 조합(searchYear/yyyy/mm/year/stdrYear 등) 모두 `rc=99 Invalid parameter`. 활용가이드 문서로 정확한 변수명 확보 필요. |

### 판정
**승인된 15003817에는 "월별 전체 컨테이너 TEU"가 없다.**
- `getAllFrghtGtqyMnthng`는 이름과 달리 **전체 화물 톤수 + 최신 1개월**이라 비율 분모로 못 씀.
- 전체 컨테이너 TEU를 월별로 얻으려면 **컨테이너 전용 데이터셋 추가 확보**가 필요.

### 검토 대상(다음 후보) — 전체 컨테이너 TEU 확보용
1. **data.go.kr 15003936 (인천항 컨테이너 정보)** — 컨테이너 처리실적(적/공 포함 TEU) 가능성. 활용신청·오퍼레이션 확인 필요.
2. **`getCntanrLandngLoading` 요청변수 확보** — 이미 승인된 폴더 안이라 변수명만 알면 가장 빠름. data.go.kr 활용가이드(참고문서) 필요.
3. (대안) 이미 쓰는 **공컨 API(ipaEmpConCargoInfo)**는 분자(공컨)만 제공 → 분모(전체 컨테이너)는 위 1~2에서 확보해야 비율 계산 가능.

---

## 보고서 #02 분모 후보 조사 (2026-07-09) — 전체 컨테이너 TEU 데이터셋 비교
목적: 적컨+공컨 = 전체 대비 공컨 비율. 분모(전체 또는 적컨 TEU 월별)를 줄 데이터셋 탐색.

### 후보 비교표
| 데이터셋 | 인천 필터 | 월별 | TEU 단위 | 적/공·전체 구분 | 형태 | 판정 |
|---|---|---|---|---|---|---|
| **15059131 해수부 수출입컨테이너처리실적** | ⚠️ 지역기준 조회 언급, 항만필드 명세 미확인 | ✅ `sym`~`eym` 연월범위 | ✅ (`eContnTeuTotal`, `tContnTeuTotal` 등) | ✅ 수입(e)/수출(t) × 내국/외국선박 × 적/공 | 월별 **집계 통계** | 🥇 **1순위(단, 인천 필터 확인 필요)** |
| 15003936 인천항 컨테이너 정보 | ✅ 인천 전용 | ❌ | ❌(직접 합계 없음) | 건별로는 가능 | **건별 raw**(선적단위 연 ~1.1만건) | ❌ 집계부담+커버리지 불명 |
| 15057250 해수부 국가별컨테이너처리실적 | ❌ 국가 기준 | ✅ | ✅ | 국가별 | 집계 | ❌ 항만 아님 |
| index.go.kr 1267 / SP-IDC 항만별 실적 | ✅ 인천 명시(2022년 319만 TEU) | ✅ | ✅ 수출입/환적 | 전체(수출입/환적) | 지표·파일(비 API) | ✅ **공표치 교차검증용 최적** |

### 15059131 응답 필드(실측 명세)
`useYm`(통계연월), 수입계열 `eContnTeuTotal / eIntrvsslContnTeuTotal(내국선박) / eFnshplContnTeuTotal(외국선박)`,
수출계열 `tContnTeuTotal / tIntrvsslContnTeuTotal / tFnshplContnTeuTotal`. 각 계열이 적/공 구분 포함.
요청변수: `serviceKey, sym(시작연월), eym(종료연월), pageNo, numOfRows`. 갱신 실시간.

### 추천
- **1순위: 15059131 (해수부 수출입컨테이너처리실적)** — 월별·TEU·적/공·수출입 구분을 한 데이터셋에서 제공.
  분모(전체=적+공)와 분자(공)를 이걸로 직접 산출 가능하고, 기존 공컨 API 수치 교차검증까지 됨.
- **신청 전 반드시 확인할 2가지:**
  1. **인천항 필터**: 응답에 항만(지역) 필드가 있어 인천만 뽑을 수 있는가? (없으면 전국 총계라 분모로 못 씀)
  2. **적컨/전체 범위**: "수출입 처리실적"이 적컨(full) 위주인지, 공컨(empty)까지 포함하는지 → 중복합산 방지.
- **교차검증**: index.go.kr(항만별 컨테이너 화물처리 실적) 또는 SP-IDC 인천항 월별 TEU로 산출값 검증.
- 15003936(인천 컨테이너 정보)은 건별 raw라 월별 집계 부담·커버리지 불명 → 분모용으로 부적합.

---

## 보고서 #02 실측 결론 (2026-07-09) — 15059131은 인천 분모로 못 씀 (확정)
검증 스크립트: `verify_denominator.py`. 주소 `https://apis.data.go.kr/1192000/SsopCargContnImxprt2/Ym`, 2025년(sym=202501~eym=202512) 호출.

### 실측 결과
- resultCode=00 정상, totalCount=**144** (= 12개월 × 12개 구분).
- 구분 필드는 `areaCd/areaNm` 하나뿐인데, 값이 **수출입 상대 "해외지역"** 이다:
  `일본 지역, 극동아시아, 동남아시아, 서남아시아, 중동, 유럽, 북미주, 중미, 남미, 대양주, 아프리카, 기타` (12개).
- **국내 항만(인천 등) 구분 필드가 아예 없음.** 즉 이 데이터는 "전국(전 항만) 실적을 상대 해외지역별로 쪼갠 것".
- 필드 의미: `e*`=수입 / `t*`=수출 계열, `Intrvssl`=내국선박·`Fnshp`=외국선박, `Fcontn`=적컨(Full)·`Econtn`=공컨(Empty).
  적/공 구분은 되지만 **지역이 해외지역이라 인천만 분리 불가**.

### 결론
**15059131로는 인천항 분모(전체 컨테이너 TEU)를 뽑을 수 없다** — 어제 뺀 이유(전국을 상대국/지역별로만 제공) 그대로 유효.
→ **방향 전환: 15003936(인천항 컨테이너 정보)** 로 인천 분모 확보를 시도한다.

---

## 보고서 #02 재확인 (2026-07-09) — 15003817에 '월별 컨테이너 TEU' 오퍼레이션은 없음
데이터셋 설명에 "컨테이너 처리량"이 있어, 어제 본 3개 외 숨은 오퍼레이션이 있는지 실측 탐지.
스크립트: `discover_ops.py` (후보 오퍼레이션명 25종 배치 호출 + 태그 덤프).

### 실측 결과
- 폴더(ipaGtqyProcessAcmslt)에서 **응답하는 오퍼레이션은 여전히 3개뿐**. 나머지 후보 22종은 전부 HTTP404(오퍼레이션 없음).
  - 시도한 이름: `getAllFrghtGtqyYear`, `getGtqyProcessAcmsltYear`, `getShipInout*`, `getCntanrCarryInout*`, `getCntanrGtqy(Mnthng)`, `getCntanrProcessAcmslt*`, `getCntanrTeu*`, `getContainerGtqy` 등.
- 작동하는 3개의 태그에 **컨테이너/TEU 필드 없음**:
  - `getAllFrghtGtqyMnthng` → 전체 화물 톤수(1개월).
  - `getGtqyProcessAcmsltMnby` → 전수 확인해도 구분명이 **'연안'뿐**. 구분 요청변수(inoutgbnd/inoutGbn/gbn 01~10) 전부 무시 → 연안 화물 톤수 월별.
  - `getCntanrLandngLoading` → 유일한 컨테이너 레벨이나 여전히 rc=99(요청변수 미상)로 호출 불가.

### 결론
**15003817에는 "월별 컨테이너 물동량(TEU)"을 주는 오퍼레이션이 없다(실측).**
- 설명의 "컨테이너 처리량"은 사실상 `getCntanrLandngLoading`(양/적하) 하나를 가리키는 것으로 보이며, 이건 요청변수 미상으로 호출 불가 + TEU 집계 여부도 불명.
- 따라서 분모(인천 전체 컨테이너 TEU)는 15003817로 확보 불가 → **계획대로 15003936(인천항 컨테이너 정보) 활용신청** 진행이 맞음.
- (참고) `getCntanrLandngLoading` 요청변수를 확보하면 승인 폴더 내 해결 여지가 남지만, 활용가이드 문서 확보 경로는 이번엔 배제하기로 함.

---

## 보고서 #02 발상전환 (2026-07-09) — 공컨의 '짝 데이터' 탐색 → 항만운영 통계(15091225) 발견
공컨(15130235)이 있으니 같은 계열의 "적컨(Full)" 또는 "전체 컨테이너" 월별 TEU 데이터가 있는지 조사.

### 인천항만공사(ICPA) 컨테이너 계열 데이터셋 실측 목록
| 데이터셋(번호) | 내용 | 분모 적합성 |
|---|---|---|
| 15130235 공컨테이너 화물 통계 | **공컨(Empty)** 월별 TEU (우리가 이미 보유=분자) | 분자 |
| 15003936 인천항 컨테이너 정보 | 건별 운영데이터(본선작업·선석·반출입예정), 터미널·선석 조건 | ❌ 월 TEU 집계 불가 |
| 15003817 물동량 처리 실적 | 화물 톤수·연안·선박, 컨테이너는 양적하(호출불가) | ❌ |
| **15091225 항만운영 통계정보** | **"항만별 컨테이너 처리량(TEU)" + 월별 컨테이너 통계** | 🥇 **전체 TEU 분모 후보** |
| (15157746) 항만운영 통계정보 조회서비스 | 위 15091225의 조회서비스 쌍둥이 | 동일 |

→ **"적컨/Full 전용 짝 데이터셋"은 ICPA에 없음.** 대신 **전체 컨테이너 처리량(TEU)**을 주는 **항만운영 통계정보(15091225)**가 존재.

### 후보 비교표 (분모용)
| 후보 | (a)인천항 | (b)월별 | (c)TEU | (d)적컨/전체 | 상태 | 판정 |
|---|---|---|---|---|---|---|
| **15091225 항만운영 통계정보** | ✅ ICPA 전용 | ✅(월별 컨테이너 통계 명시) | ✅ 처리량(TEU) | **전체** 컨테이너 | 미승인(신청필요) | 🥇 **1순위 추천** |
| index.go.kr 1267 항만별 컨테이너 처리실적 | ✅ 인천 명시 | ✅ 연/월 | ✅ | 전체(수출입/환적) | 공표통계(비API) | ✅ **교차검증/대안** |
| 15003936 인천항 컨테이너 정보 | ✅ | ❌ 건별 | ❌ | - | 승인가능 | ❌ |
| 15059131 해수부 수출입컨처리실적 | ❌ 해외지역축 | ✅ | ✅ | 적/공 | 승인됨 | ❌ 인천분리 불가 |

### 추천 & 다음 단계
1. **1순위: 15091225(항만운영 통계정보) 활용신청** — 전체 컨테이너 처리량(TEU)을 인천·월별로 직접 제공할 것으로 확인. 승인 후 오퍼레이션/응답필드 실측으로 "월별 전체 TEU" 확정.
   - 신청 후 확인할 것: 컨테이너 처리량 오퍼레이션명, 월별 여부, TEU 단위, (가능하면) 수출입/환적 구분.
   - 이걸 확보하면 **전체(분모)=15091225, 공컨(분자)=15130235** 로 비율 산출 + 교차검증 가능.
2. **대안/교차검증: index.go.kr(항만별 컨테이너 화물처리 실적, idx_cd=1267)** — 인천 전체 컨테이너 TEU 공표치. API 승인 지연 시 수동 확보 가능.

---

## 보고서 #02 분모 확보 방법 (2026-07-09, 최종) = index.go.kr 공표 통계
**방법론:** "인천항 월별 전체 컨테이너 TEU"를 주는 공공데이터 API **6개를 실측 검증**했으나
모두 부적합 → **공식 확정 통계(index.go.kr / SP-IDC)로 분모를 보완**한다.

### 검증한 API 6개와 탈락 사유 (실측 근거)
| # | 데이터셋 | 탈락 사유 |
|---|---|---|
| 1 | 15003817 getAllFrghtGtqyMnthng | 전체 화물 **톤수** + 최신 1개월만 (컨테이너·TEU 아님) |
| 2 | 15003817 getGtqyProcessAcmsltMnby | **연안** 화물 톤수만 (구분변수 무효) |
| 3 | 15003817 getCntanrLandngLoading | 컨테이너 레벨이나 요청변수 미상(rc=99), 호출 불가 |
| 4 | 15059131 해수부 수출입컨처리실적 | 축이 **해외지역**(일본/유럽…) — 인천 분리 불가 |
| 5 | 15003936 인천항 컨테이너 정보 | 건별 운영데이터(터미널·선석), 월 TEU 집계 불가 |
| 6 | 15091225 항만운영 통계정보 | 전체 TEU 가능성 있으나 미승인 + 문서 확정 불가(후순위 보류) |

### 채택: index.go.kr "항만별 컨테이너 화물처리 실적" (idx_cd=1267)
- **출처(원자료):** 해양수산부 **SP-IDC(해운항만물류정보시스템)**, 갱신 매 익년 4월. index.go.kr(e-나라지표) 게시.
- **범위 확인(중복합산 방지 핵심):** 이 통계의 '전체 컨테이너'는 **적컨·공컨을 모두 포함**(index.go.kr 명시 "적·공 모두 포함").
  → 즉 **전체 = 적컨 + 공컨**. 우리 공컨(15130235)은 이 전체 안에 이미 포함됨.
  → 비율 = 공컨 / 전체. **전체에 공컨을 다시 더하면 안 됨(중복).**
- **주기:** 이 지표 페이지는 **연도별**. (월별은 이 페이지에 없음 → SP-IDC 월별 조회 또는 ICPA 월별 보도자료로 확보)

### 확보한 실제 공표 수치 (추정 아님, 공표치)
| 연도 | 인천항 전체 컨테이너(TEU) | 비고 |
|---|---|---|
| 2025 | 약 **3,440,000** (≈344만, 전년比 **-3.2%**) | 수출입 340만 + 환적 4만. index.go.kr 게재 |
| 2024 | **3,558,455** | 역대 최대. 수입 1,772,061 / 수출 1,737,129 / 환적 492,265* |
| (참고) 2024.12 월간 | **321,531** | ICPA 보도자료 월 최대치 (월별 확보 가능함을 보여주는 예시) |

\* 2024 환적 수치는 보도자료마다 49,265/492,265 혼재 → 인용 시 원문 재확인 필요.

### 다음 단계 (분모 월별화)
- **2025년 월별 전체 TEU**는 index.go.kr 1267(연도별)엔 없음.
  → **SP-IDC(해운항만물류정보시스템) 월별 조회** 또는 **ICPA 월별 보도자료**에서 2025년 1~12월 전체 TEU를 공표치로 확보.
- 확보 후: 분모=월별 전체 TEU(SP-IDC/ICPA), 분자=월별 공컨 TEU(15130235) → 공컨 비율 산출.
- **원칙: 추정치 금지, 공표된 실제 값만 사용. 산출값은 index.go.kr 연간치와 합계 대조로 검증.**
