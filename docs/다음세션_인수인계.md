# 다음 세션 인수인계

**최종 갱신: 2026-08-25 (세션: 전면 진단 + 지침 v3.0 개편안 + 브랜드 확장 설계 + 발행 린터 구축)**
**※ 지침 v3.0 확정 시 이 문서는 `docs/STATUS.md`로 대체된다. 확정 전까지 이 문서가 정본이다.**
**적용 지침: 프로젝트 지침 v1.1 (2026-08-04, 비공개 보관). 구 마스터 지침 v6.x는 아카이브(참조 전용).**

이 문서와 지침이 어긋나면 **지침이 우선**한다.
작업 폴더: C:\Users\user\OneDrive\문서\역량부서\인천항물동량

---

## 0. 사용법
- 컨텍스트가 리셋돼도 「지침 + 이 문서」 두 개로 맥락이 복원되어야 한다.
- 세션 시작 시 경로 게이트: `git rev-parse HEAD`로 폴더 오인을 차단한다.
- **원격 동기화 상태(ahead/behind)는 이 문서에 적지 않는다.** `git status -sb`로 확인한다.

## 1. 현재 상태
- 공개 저장소: github.com/jisangj03-dev/incheon-port-analysis (GitHub Pages 연동)
- 보고서 **#01~#06 발행 완료**
- **#07 판정 완료. 본문 §2·§4 작성 완료. 빈칸 3개 남아 미발행.**
- 진실의 원천 = `docs/07_판정결과.md`
- 선커밋 기준 = `docs/07_주제검증.md` §4 (blob c95bbfc859c67fd7cdddb497eb822eadce4b3544)

## 2. #07 선커밋 완주 (2026-08-04)

대상 범위 S = 2026-01~06 (표본외 연장 검증 + 전년 동기 비교).

| 기준 | 결과 | 값 |
|---|---|---|
| V1 방향 | PASS | 수출 74,065.75 > 수입 8,570.25 |
| V2 배율 | PASS | 8.642 (기준 3.9 이상) |
| V3 월별 격차 | PASS | 6/6 성립 |
| V4 격차 크기 | PASS | +34.4%p (기준 +5.0%p 이상) |

- 기준 무수정 · 사후조정 0 · 이탈 0. 실행 게이트 1~5 전부 통과.
- 격차 부호 연속 성립 기록: 2022-10부터 **45개월**로 연장.
- 선커밋 이행 기록 원본은 비공개 본부에 보관(공개 저장소 미포함).

### 산출물 sha256
- `analysis/container_2026_direction.csv` (36행)
  DF63A87327A0E34A97B08CB29F07466B67FE532059D07110A3E40072F6445B45
- `analysis/probe/recheck_2025_direction_20260804.csv` (64행)
  CCD319B157274905975A79C02561FCBE081FB392F8AA55734FF5FED8CCEF774B
- `analysis/container_2025_direction.csv` (정본·수정금지)
  EA17A4CA45FD59B308E558D1A01C9F9E7D66B885C1DEF13A1112B72BC8EDD954

## 3. 2026-08-12 발행 세션 결과

### A. 서두 정리 — 완료 (push까지)
- `.gitignore`에 `_probe_dump.txt` 등재. 정지선을 저장소가 강제하게 함.
- 인수인계 개정. 링크드인 4주 관찰 조기 종결에 따른 거짓 제약 제거.
- `.gitattributes` 신설. **md는 LF 고정, csv는 CRLF 고정.**
  csv를 LF로 바꾸면 기록된 sha256 앵커 3건이 전부 깨진다. 앵커 보존이 우선이다.
  `.py`는 앵커가 없어 정책 미적용(core.autocrlf=true에 위임).
- push 완료: `3b786d2..e1a1e7d`.

### B. 차트 2종 — 완료 (커밋 2cb39d4)
- `reports/images/size_40ft_monthly_2026.png` (85,072 bytes) — V3 월별 격차
- `reports/images/direction_yoy_2025_2026.png` (100,508 bytes) — 전년 동기 대비
- `analysis/chart_07_2026.py` — 렌더 코드
- X-CHECK 재실시: 챗이 값을 먼저 공개한 뒤 Claude Code가 판정결과 문서를 열지 않고
  원시 CSV에서 독립 산출. **불일치 0건.** 2025 계층(72,558.75 / 399,476.5 / 5.506)도
  이번에 원시 재산출로 대조됐다.
- 커밋 전 로컬 렌더에서 y축 눈금 표기 오류를 발견해 1회 재렌더. 판정값·로직 무변경.

### C. 보고서 본문 — §2·§4 완료, 빈칸 3개 대기 (커밋 4841a50)
- `reports/report_07_공컨테이너_표본외검증.md` (8,487 bytes)
- 기존 6편 관례 적용 완료: 프론트매터 없음 / 작성일·분석대상 불릿 블록 /
  이미지 경로 `images/xxx.png` / 캡션 `<sub>...</sub>` / 각주는 문서 끝 번호 목록 /
  부록은 H2. 각주 3건 이관 시 문구는 무변경.
- **비어 있는 곳 3개 — 전부 회장 작성분이다:**
  1. H1의 【부제】
  2. §1 핵심 요약
  3. §3 해석
- H1 직후 「이전 편과의 계보」 인용 블록도 6편 관례이나, 서사이므로 만들지 않았다.
- 「한 줄 결론」 불릿도 §1과 같은 성격이라 넣지 않았다.

### D. 미실시 → 일부 해소 (2026-08-25)
- **Pages 확인 — 완료 (2026-08-25).** 허브 · #06 본문 · 차트 이미지 · 표 전부 정상 렌더.
  `.gitattributes` 신설 부작용 없음. **이 항목 종결.**
- README 허브 등재 (6편 → 7편). 본문 완성 후. **미실시 유지.**

## 4. 다음 세션에 할 일 (순서 고정)

> **2026-08-25 개정.** 아래 1~5는 그대로 유효하나, 그 앞에 **회장 결정 2건**이 놓인다.
> **0-a. 지침 v2.0 개편안 확정 여부** — `본부\프로젝트지침_v2.0_개편안_20260825.md`.
>   회장 선언 전까지 유효 지침은 v1.1이다.
> **0-b. 브랜드 허브 신설 여부** — `jisangj03-dev.github.io` 사용자 사이트(현재 비어 있음).
>   **저장소 rename은 금지 확정** — Pages URL은 리디렉트 대상이 아니다(GitHub 공식 문서 확인).
> 판단 근거는 `본부\평가_20260825_현황진단.md`.

1. **H1 부제 + §1 + §3 작성 [문답 세션]** — Claude가 질문하고 회장이 답해 문장을 만든다.
   Claude 단독 대필 금지. 셋 다 성격이 같으므로 한 자리에서 처리한다. 재료는 §4.1.
2. **README 허브 등재** — 6편 → 7편. 기존 형식:
   `- **[보고서 #NN] <제목>** — <한 줄 요지>` 다음 줄 들여쓰기 `[보고서 전문 보기 →](reports/report_NN_....md)`
   하단 계보 문단도 갱신.
3. **Pages 확인** — Ctrl+Shift+R로 캐시 무효화 후.
4. **push**
5. **유통** — 링크드인 · 인스타그램 · 스레드 3채널. 1편 파일럿으로 실소요 시간을 먼저 측정하고,
   측정 후에 페이스를 수치로 선커밋한다. 측정 전 페이스 확정 금지.
   카드뉴스 상한 1건 1시간, 템플릿 1회 제작 후 재사용.

### 4.1 빈칸 3개 작성 재료 (Claude 제공분 — 문장 아님)

**H1 부제 참고**
#05·#06의 부제 실물을 먼저 확인하고 그 톤에 맞춘다. 두 편 모두 결론 선언형이다.
방법 서술형("사전 선언 기준으로 판정하다")으로 가면 6편과 성격이 달라진다.

**§1에 들어갈 사실 조각**
- 무엇을: 2026 상반기 6개월로 기존 결론을 표본외 검증
- 순서: 기준 선언 → 커밋 → 그다음 데이터 수집
- 결과: V1~V4 전부 PASS, 기준 무수정
- 숫자 한둘: 배율 8.642 / 45개월 연속 / 격차 +34.4%p 중 택
- 단서 한 줄: 2026은 잠정치

**§3에서 답할 질문**
- 45개월 연속 성립은 무엇을 말해주고, 무엇은 말해주지 않는가?
- 수입 -21.4%와 수출 +10.7%가 동시에 나온 상태를 어떻게 볼 것인가?
- 배율 5.506 → 7.754는 이 시리즈에서 어떤 자리에 놓이는가?
- FAIL이었다면 어떻게 했을 것이며, 그 답이 PASS의 의미를 어떻게 바꾸는가?
- 이 방식이 다음 편에 무엇을 남기는가?

**문안 가드레일**
- 인과·목적 서술 금지. "~때문에", "~를 위해" 불가. 값과 값의 관계까지만.
- 2026 수치에 "확정"·"확인됐다"를 붙이지 않는다. "산출됐다"·"관측됐다"로.
- 비중은 박스 수 기준. TEU 비중 금지(순환 논증).
- PASS는 기준 통과이지 법칙이 아니다. 과대 해석 금지.

## 5. 불변 정지선 (저장소 레벨)
- `_probe_dump.txt` — 커밋 · 삭제 · **열람** 전부 금지. untracked 유지. `.gitignore` 등재됨.
- `probe_2026_raw.csv` — 삭제 · 열람 금지.
  **[2026-08-12 관측] 이 파일은 `analysis/probe/probe_2026_raw.csv`로 저장소에 추적 중이다.**
  정지선은 「삭제·열람 금지」이지 「커밋 금지」가 아니므로 위반이 아니다.
  데이터는 공공 API 원시 응답이라 비밀정보가 아니며, §0 노출 이력을 제3자가
  검증할 수 있게 하는 감사추적 자산으로 현상 유지한다. 열람 금지는 유지한다
  (챗이 다시 읽으면 같은 컬럼 오인이 재발할 수 있다).
- `analysis/container_2025_direction.csv` — 수정 · 덮어쓰기 금지 (sha256은 §2).
- `analysis/container_2026_direction.csv` — 행 순서에 의미 없음(API 비고정). 정렬·재저장 금지.
- `config.py` — 인증키 보관. `.gitignore` 등재 유지. 공개 저장소 절대 미포함.
- **2026-01~05 방향 · 배율은 검증이 아니다**(강등 구간). 참고 통계로만 쓰고
  `docs/07_주제검증.md` §0 노출 이력 ②를 별표로 인용한다.
- **2026 전 구간은 잠정치.** 적재일 병기 + 수집일 명기 + 사후 수정 가능성 고지.
- 인과 · 목적 서술 금지. `_99` = "기타 규격" [미확인] 유지.
- 2026 방향별 CSV 경로 = `analysis/` 직하 [확정]. `analysis/data/` 신설 금지.
- 공개 수치는 검증 가능한 1차 출처만.
- **push 전: 회장 통독 + 챗 전문 전수 검산.** 전문은 실물 바이트 덤프로.
- 차트는 커밋 전 로컬 렌더.

## 6. 기술 위생 — 승계된 사실
- 원시 파일 파싱은 **태그명 기준**. 위치 인덱스 금지.
- **API 응답 행 순서는 호출마다 비고정** [확정]. 2025 재수집분과 정본이 3,171B로 동일하나
  위치 기준 462셀 불일치, 자연키(`yyyy, mm, GInOut, ocCt`) 매칭 시 전 셀 완전 일치.
  → 대조는 **자연키 매칭으로만** 한다.
- 회귀 앵커 검증은 기존 CSV 재합산이 아니라 **동일 코드 경로로 재수집**해 맞춘다.
  재수집분은 별도 파일로 보존하고 정본을 덮어쓰지 않는다.
- 강한 앵커는 **git blob 해시 또는 SHA-256**. PowerShell `Measure-Object`의
  `-Line`(빈 줄 미포함) · `-Character`(한글을 1자로 계수)는 바이트가 아니다.
- **[2026-08-12 신규] 줄끝 정책은 앵커를 깰 수 있다.** `.gitattributes`에 `* text=auto eol=lf`를
  걸면 CRLF로 측정된 sha256 앵커가 checkout 시점에 전부 무효가 된다.
  정책을 세울 때는 **먼저 `git ls-files --eol`로 인덱스/워킹트리 실측**하고,
  쓰기 후 해시 스냅샷 대조로 부작용 0을 증명한 뒤에만 커밋한다.
- **[2026-08-12 신규] 경로식 검사 주의.** `git ls-files -- "파일명"`은 루트만 매칭한다.
  하위 폴더 파일은 `"*파일명"`으로 확인해야 한다. 잘못된 경로식은
  "미추적"이라는 **근거 없는 통과 판정**을 만든다.
- **[2026-08-12 신규] 문서 개정은 반드시 전문 납품.** 챗이 대화 중 준비한 개정안을
  "직전 본문 + 델타"로 지시하면 Claude Code에게는 원본이 없어 적용 지점이 사라진다.
  이번 세션에서 실제 발생했고 §5 가드(일치 개수 != 1이면 중단)가 잡았다.
  지침 §0 「이전 버전 참조 납품 금지」의 실증이다.
- API 응답 성공이 데이터 완전성을 보장하지 않는다. totalCount · 월별 존재 확인 필수.
  제로 패딩 주의("01" != 1). 음수 주장(존재 없음)은 양수 주장보다 엄격히 검증한다.
- X-CHECK는 양측이 서로의 값을 보기 전에 산출해야 실효가 있다. 챗 선공개 → Code 독립 산출.
  Code에게는 **판정결과 문서를 열지 말라**고 명시해야 독립성이 성립한다.
- 텍스트 정정은 `ReadAllBytes` → 치환 → `WriteAllBytes` 바이트 왕복으로만.
  `Set-Content` · `Out-File`은 BOM과 줄끝을 조용히 바꾼다.
  **대상에 따라 규격이 다르다 — 본부(OneDrive) md는 BOM+LF, 이 저장소 md는 BOM 없음·LF.** 섞지 마라.
- 편집 지시 전에 **대상 문자열의 현재 존재 여부부터 대조**한다. 일치 개수가 1이 아니면
  쓰기 전에 중단하는 가드를 붙인다. 가드 없는 Replace는 아무 일도 안 하고 "완료"만 보고한다.
- 검증 지표는 선언 전에 "그 수단이 대상을 실제로 집계하는가"부터 확인한다.
  실증: GitHub Insights > Traffic은 Pages 방문을 집계하지 않는다.
- 롤링 윈도우 지표는 당일 캡처. 지연 = 영구 소실.
- 발행 진실은 렌더된 화면 · 디스크 바이트 · git blob · tarball. 편집기 미리보기 신뢰 금지.
- 프로브는 값 비노출 설계(존재 여부 · 행 수 · 적재일만 출력).
- 이 IDE는 긴 명령에서 `EPERM: uv_spawn`으로 프로세스가 뜨지 않는다.
  긴 스크립트는 **내용 수정 없이** 덩어리로 분할해 순서대로 실행한다.

## 7. 관측 (해석 미착수)
- 2026-01~06 연안항(`ocCt=2`) 12행 전부 0 [관측]. 후속 시리즈 소재 후보. **해석 없음.**

## 8. 종결 · 정정
- **링크드인 4주 관찰: 2026-07-31(D+17) 조기 종료. 판정 없음.**
  사유는 성과 부진이 아니라 전략 재편 — 새 지침이 채널 상시 발행을 전제하므로
  관찰의 전제(4주 침묵)가 소멸했다.
  **D+28 캡처 의무 소멸. 링크드인 신규·수정 콘텐츠 금지 조항도 해제됨.**
  D+14 확보분은 비공개 본부에 보존.
- 확정 채널 3 = 링크드인 · 인스타그램 · 스레드. 원본 허브 = GitHub Pages.
  블로그 계열은 쓰지 않는다(회장 확정). 모든 채널 게시물은 Pages 원문 링크를 포함한다.

## 9. 미해결
- **저장소 · 브랜드 구조 미정** — 다중 시리즈를 담을 그릇을 정해야 한다.
  사이트 승격(게이트 1) 판정도 여기 포함해 함께 결정한다. 자동 승격 아님.
  **[2026-08-25 확정] 저장소 rename 금지** — GitHub은 rename 시 issues·stars·clone은
  리디렉트하나 **프로젝트 사이트(Pages) URL은 리디렉트하지 않는다**(공식 문서 원문:
  "with the exception of project site URLs"). 링크드인 게시물이 가리키는 것이 그 URL이다.
  → 현 저장소·URL은 영구 동결. 대안은 사용자 사이트 `jisangj03-dev.github.io`(실측 404, 비어 있음)
  또는 커스텀 도메인. `docs/벤치마킹.md`의 Quarto 재도입 조건 1·2는 이미 충족 상태다.
- #05 = 3개년 조건부 채택 유지. **2025 전체 방향별 승격은 보류** —
  IPA 직접 문의 또는 공식 공개 시에만 해제.
- 구 지침 v6.1 본체 파일 위치 미확인. 발견 시 아카이브로.
- 이력서 학점 · 어학 미기재. 실물 확인 후 보강, PDF 재출력은 Claude에게.

## 10. 2026-08-25 진단 세션 기록

**산출물 (비공개 · 본부 보관 · 공개 저장소 미포함)**
- `본부\평가_20260825_현황진단.md` — 저장소 전수 · 보고서 7편 · Pages · 링크드인 평가
- `본부\프로젝트지침_v2.0_개편안_20260825.md` — 지침 전면 개편'안' (확정은 회장 선언으로만)

**저장소 변경** — 용어 정정(#07) + 본 문서 갱신. **push 없음.**

**확인된 사실**
- Pages 렌더 정상. `.gitattributes` 신설 부작용 0건.
- #07 수치 전 항목 재검산 — 판정결과와 불일치 0건.
- 내부 링크·이미지 파손 0건, 미사용 차트 이미지 0건.
- 저장소 rename 시 Pages URL 리디렉트 없음 [확정]. `jisangj03-dev.github.io` 비어 있음(404).
- 링크드인: 게시물 1건 · 팔로워 1 · 1촌 1 · 헤드라인이 "보고서 4편"(실제 발행 6편) — 갱신 필요.
- 절차:내용 비율 실측 = 30,849 B(인수인계+스킬 2종) : 76,986 B(보고서 본문) = 1 : 2.5.

**처리 대기 — 회장 판단 필요**
1. **용어 통일 범위.** 공식 라벨은 `ocCt` 1=수출입항 · 2=연안항 [확정, `GInOut_코드규명.md` §3].
   - 정정함: `report_07`(미발행), 본 문서 §7.
   - **손대지 않음: 발행본 `report_05`(4곳) · `report_06`(3곳)의 "외항".**
     발행물 본문 수정은 회장 통독 정지선 대상이며, "외항"은 오류가 아니라 축 약칭이다.
   - **정정하지 않음: `07_주제검증.md` · `07_판정결과.md`의 "외항".** 선커밋 당시 문안이므로
     사후수정 금지 원칙에 따라 보존한다.
2. **#07 템플릿 이탈.** 계보 인용블록 · 한 줄 결론 · 방법론 절이 #01~#06에 있고 #07에 없다.
   부제 · §1 · §3 작성 시 형식 통일 여부를 함께 확정한다.
3. **`07_판정결과.md` §4 ⓐ의 "확정 집계" 표기.** 2026 잠정치 고지와 문구가 어긋나나
   선커밋 문안(`07_주제검증.md` §0)을 승계한 것이므로 정정하지 않았다. 판단 필요.


## 11. 2026-08-25 확장 위임 세션 기록 (2차)

**저장소에 실제로 만든 것 — 정지선을 사람이 나르지 않게 내렸다**

| 파일 | 역할 |
|---|---|
| `docs/FACTS.md` | 수치 정본 대장. 값 · **창** · **지위**의 단일 출처. 26건 등재 |
| `analysis/lint_publish.py` | 발행 린터. 결론 자리 수치의 등재·지위를 기계로 검사. `--selftest` 내장 |
| `docs/사고기록.md` | 사고 로그 24항목(지침에서 분리·공개). 새 사고는 뒤에 붙인다 |
| `docs/STATUS.md` | 상태 1장(지침 v3.0 확정 시 이 인수인계를 대체) |

- **린터 인수시험 통과** — 2026-08-25 사고 2건(배율 창 오판 / 강등 구간 값의 결론 자리 인용)을
  픽스처로 재현해 검출 확인. `python analysis/lint_publish.py --selftest`
- **발행본 전수 린트: FAIL 0 / WARN 103.** WARN은 대장 미등재 수치 백로그.

**표기 정본 확정 및 소급 정정**
- 정본 = `수출입항`(ocCt=1) / `연안항`(ocCt=2). 근거 = `GInOut_코드규명.md` §3 [확정]
- 소급: **#06 3곳 · #05 4곳** 정정 + 각 편에 `### 수정 이력` 추가. 발행본 옛 표기 잔존 0건
- **미정정(의도적):** `07_주제검증.md`·`07_판정결과.md`의 "외항" — 선커밋 당시 문안이므로 보존

**본부 산출물 (비공개)**
- `본부\프로젝트지침_v3.0_개편안_20260825.md` — v2.0 안은 폐기 배너 처리
- `본부\브랜드확장_설계_20260825.md` — 저장소 구조 · 사이트 IA · 라인 · 채널 우선순위 [확정]
- `본부\평가_20260825_현황진단.md` — 2차 조사분 추가

**신규 산업 소스 실측**
- 관세청 `항구 공항별 수출입실적(GW)` = 1순위(인천항 시리즈와 같은 대상·다른 소스 → 「단일 소스」 한계 해소)
  - 실호출 `SERVICE_KEY_IS_NOT_REGISTERED_ERROR`(30). 대조군 공컨 API는 정상.
    → **회장 액션: 공공데이터포털 활용신청**
  - 함정: `'26.7.1. 인천 지방행정체계 개편으로 수출입신고 시도코드 변경`
- KOSIS: HTTP 종료·분당 호출제한·출처 「국가데이터처」 **3건 전부 확인**. **별도 인증키 → 회장 액션**
- **두 소스 모두 실호출 미완이므로 무역·유통 라인은 주제 선정 단계에 진입하지 않았다.**

**채널**
- **GitHub 프로필 README 없음** → 1순위(비용 최저·적합성 최고). 링크드인은 헤드라인 최신화만
- 차단으로 확인 못 한 도메인: 디스콰이엇 · 메일리 · KOSIS 웹 → **[미확인] 유지, 추정하지 않음**

**push 없음.** 공개 발행은 회장 서명.
