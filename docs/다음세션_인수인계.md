# 다음 세션 인수인계 문서

> 이 프로젝트를 다음 세션에서 이어받을 때 가장 먼저 읽는 문서다. 현재 상태·주의사항·다음 할 일을 순서대로 정리했다.

---

## 1. 프로젝트 개요

- **이름**: 「인천항 물동량 분석」 (역량부서 프로젝트)
- **목적**: **취업 포트폴리오** — 선광(인천항 컨테이너 터미널 운영사) 및 대기업 물류 직군 지원용
- **성격**: 공공데이터를 기반으로 인천항 컨테이너 물동량을 분석하는 **정기 리서치 보고서 사이트**
- **핵심 컨셉**: 데이터를 직접 탐색·검증하고, 추정치 없이 **공표된 실측값만** 사용해, 해석까지 담은 보고서를 번호를 붙여(#01, #02 …) 쌓아 나간다.

## 2. 완성된 보고서

| 번호 | 제목 | 핵심 결론 |
|------|------|-----------|
| **#01** | 인천항 공컨테이너 물동량 분석 (2025) | 연간 **99만 TEU**(991,170), 외국적 선사 **88.2%**, 2월 설 연휴 계절성(-31.2%) |
| **#02** | 인천항 공컨테이너 비율 분석 (2025) | 전체 컨테이너의 약 **28.8%가 빈 컨테이너** → #01의 "수출입 불균형·재배치 부담" 가설을 정량 검증 |

- #01 → #02는 하나의 흐름이다: #01이 "공컨 비중이 높다"는 관찰을 제시했고, #02가 이를 **전체 물동량 대비 비율**로 검증했다.
- #02의 핵심 난제는 **분모(인천항 월별 전체 컨테이너 TEU) 확보**였다. API 6종을 검증했으나 모두 부적합 → 인천지방해양수산청 공표 통계 원문(hwpx)에서 직접 추출해 해결했다. (상세: `docs/API_탐색결과.md`)

## 3. 공개 상태

- **깃허브 저장소**: https://github.com/jisangj03-dev/incheon-port-analysis
- **사이트(GitHub Pages)**: https://jisangj03-dev.github.io/incheon-port-analysis/
- **저장소 구조**:

```
.
├── README.md          # 보고서 허브 (프로젝트 소개 + 보고서 목록 링크)
├── reports/           # 분석 보고서
│   ├── report_01_공컨테이너_물동량.md
│   ├── report_02_공컨테이너_비율.md
│   └── images/        # 보고서 차트 PNG (스크립트가 여기에 생성)
├── analysis/          # 재현용 코드·데이터 (모든 .py, .csv, config)
└── docs/              # 조사·기록 문서 (이 파일 포함)
```

- README는 **허브(보고서 목록)** 이고, 실제 보고서 본문은 `reports/` 안에 있다. 코드는 모두 `analysis/` 폴더 안에서 실행한다.

## 4. 환경 주의사항 ⚠️

- **작업 폴더**: `문서\역량부서\인천항물동량`
  - ⚠️ **`수출서류랩`(다른 프로젝트)과 절대 섞지 말 것.** 완전히 별개 프로젝트다.
- **클로드 코드를 켤 때 반드시 경로부터 확인**한다. 엉뚱한 폴더에서 작업하면 파일이 뒤섞인다.
- **`config.py`(공공데이터 인증키)** 는 `.gitignore`로 제외되어 저장소에 올라가지 않는다. 새로 받으면 `analysis/config_example.py`를 복사해 `analysis/config.py`로 만들고 키를 넣는다. **절대 커밋하지 말 것.**
- **push가 2분 타임아웃**나면, 커밋까지만 하고 멈춘 뒤 **별도 PowerShell 창에서 `git push origin main`** 을 직접 실행한다.

## 5. ✅ 해결된 이슈 (기록용)

**증상(과거)**: GitHub Pages 사이트에 옛날 화면(#01 보고서 전체 + `fetch_test.py` 같은 옛 파일 구성 표)이 떴었다.

**원인**: 저장소 파일은 정상이었고(README는 "보고서 허브"로 정상), **Pages 서빙(빌드/캐시)** 지연 문제였다.

**해결**: 이후 커밋들이 재배포되면서 **자연 반영**되어 최신 허브 화면으로 정상화됐다. 별도 조치 없이 해소됨.

## 6. 오늘 작업 기록

**2026-07-11**
- 공컨 API `GInOut`·`ocCt` **코드 [확정]** → `docs/GInOut_코드규명.md`. 공식 항목정의서(구 페이지 15130235의 2026-03-13 웨이백 스냅샷)로 확인: `GInOut`=수출입구분(**1=수입·2=수출·3=수입환적·4=수출환적**), `ocCt`=외내항구분(**1=수출입항·2=연안항**). 실측 잠정 판독(방향·환적)이 적중, 3·4는 환적의 수입/수출 세분으로 확인.
- **#01 표기 정정**: 원시 "한 달 64행" 서술 오류를 **2025년 전체 64행(월 4~6행)**, 축은 **수출입구분×외내항구분**으로 사실 정정(캡션·본문·`analyze_container.py` 주석). 수치 변경 없음. #01에 수정 이력 1줄 추가.
- **구 데이터 ID 교체**: `15130235 → 15157693` (`calc_ratio.py` 등). `03_주제검증.md`에 코드규명 크로스링크·[확정] 갱신.
- #03 주제 **A′안(공컨 방향 분해)으로 확정**, 코드 [확정] 완료로 **착수 가능**.

**2026-07-10**
- README 보고서 목록의 두 링크 텍스트를 파일 경로 대신 **"보고서 전문 보기 →"** 로 변경 (커밋 `c5297bb`). 괄호 안 파일 경로는 그대로 유지.
- 보고서 #01·#02의 **작성일 연도 오기 수정**: `2025-07-08 → 2026-07-08`, `2025-07-09 → 2026-07-09`. (분석 대상의 "2025년" 등 다른 날짜는 그대로 둠.)

## 7. 다음 작업

> #03 주제는 **A′안(공컨 수출입 방향 분해)으로 확정**됐고, 코드 **[확정] 완료로 착수 가능**. 데이터 가용성 검증(`docs/03_주제검증.md`)과 코드 규명(`docs/GInOut_코드규명.md`)을 먼저 읽을 것.

**#03 = A′안 (공컨테이너 수출입 방향 분해) — 확정 · 착수 가능**
- 내용: 공컨을 `GInOut`(수입·수출·수입환적·수출환적) × `ocCt`(수출입항·연안항) 축으로 분해해 **수출입 방향별 흐름**을 분석.
- **착수 조건 충족**: 코드가 **[확정]**됨(`docs/GInOut_코드규명.md`, 공식 항목정의서 근거). 방향 라벨을 보고서에 사용해도 됨.
- **참고(선택)**: 인천항만공사 032-890-8254 전화 확인은 이제 필수 아님 — 원하면 교차확인용으로만.
- 유의: `ocCt=2`(연안항)는 공컨 실적 0이므로 방향 분해는 사실상 `GInOut` 4분류 중심.

**참고(#03 후보 판정, `docs/03_주제검증.md` 5장):**
   - ✅ 공컨 **수출입 방향 분해**(GInOut·ocCt) ← 채택(A′안)
   - ✅ 공컨 **2024 vs 2025 연도 비교** (병행 가능)
   - ✅ hwpx **터미널별(신항/남항/국제여객부두)** 또는 **국가별** 컨테이너 분석
   - ❌ hwpx로 수출입·반입반출·환적 나눈 전체 컨테이너 분석은 **데이터 없어 불가**
- 보고서가 **3개 이상 쌓이면 Quarto 도입** 검토 (목록·내비게이션이 필요해지는 시점. 지금 도입하면 도구가 먼저 앞서는 함정 — `docs/벤치마킹.md` 참고).
- 사이트가 어느 정도 갖춰지면 **링크드인 등에 홍보**.

## 8. 작업 원칙

- **데이터 검증 먼저, 계산은 나중.** 후보 데이터가 목적(인천항·월별·TEU·적/공 구분)에 맞는지 직접 호출·확인한 뒤에 계산한다.
- **API가 없으면 공식 통계로 보완.** 추정치를 만들지 않고 공표된 실측값만 쓴다. (#02 분모가 그 사례)
- **AI 활용은 정직하게 명시하되, 판단은 직접.** 데이터 선정·검증·해석은 사람이 한다고 보고서·README에 명기한다.
- **한 세션에 몰아넣지 말 것.** 분석 작업과 사이트(구조·배포) 작업을 분리해서 진행한다. 비용·집중도 관리에 유리하다.

## 9. 참고 문서

- `docs/GInOut_코드규명.md` — 공컨 API `GInOut`·`ocCt` 코드 실측 규명 (크로스탭·판독 논리·[잠정] 상태)
- `docs/API_탐색결과.md` — API 후보 탐색·검증 로그 (분모 확보 방법론 포함, 면접 설명용)
- `docs/벤치마킹.md` — 보고서 퍼블리싱 벤치마크 조사 (KMI 넘버링, OWID 구조, Quarto 도입 시점)
- `docs/TODO.md` — 후속 과제 메모
