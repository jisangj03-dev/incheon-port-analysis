# 다음 세션 인수인계 문서

> 이 프로젝트를 다음 세션에서 이어받을 때 가장 먼저 읽는 문서다. 현재 상태·주의사항·다음 할 일을 순서대로 정리했다.

---

## 1. 프로젝트 개요

- **이름**: 「인천항 물동량 분석」 (역량부서 프로젝트)
- **목적**: **취업 포트폴리오** — 선광(인천항 컨테이너 터미널 운영사) 및 대기업 물류 직군 지원용
- **성격**: 공공데이터를 기반으로 인천항 컨테이너 물동량을 분석하는 **정기 리서치 보고서 사이트**
- **핵심 컨셉**: 데이터를 직접 탐색·검증하고, 추정치 없이 **공표된 실측값만** 사용해, 해석까지 담은 보고서를 번호를 붙여(#01, #02 …) 쌓아 나간다.

## 2. 완성된 보고서

| 번호 | 제목 | 핵심 결론 |
|------|------|-----------|
| **#01** | 인천항 공컨테이너 물동량 분석 (2025) | 연간 **99만 TEU**(991,170), 외국적 선사 **88.2%**, 2월 설 연휴 계절성(-31.2%) |
| **#02** | 인천항 공컨테이너 비율 분석 (2025) | 전체 컨테이너의 약 **28.8%가 빈 컨테이너** → #01의 "수출입 불균형·재배치 부담" 가설을 정량 검증 |
| **#03** | 인천항 공컨테이너 수출입 방향 분석 (2025) | 공컨의 **수출 방향 85.1%**(수입의 **6.1배**), 월별 배율 **4.1~8.3배** → 불균형이 수출 방향으로 쏠려 있음을 규명 |

- #01 → #02는 하나의 흐름이다: #01이 "공컨 비중이 높다"는 관찰을 제시했고, #02가 이를 **전체 물동량 대비 비율**로 검증했다.
- #02의 핵심 난제는 **분모(인천항 월별 전체 컨테이너 TEU) 확보**였다. API 6종을 검증했으나 모두 부적합 → 인천지방해양수산청 공표 통계 원문(hwpx)에서 직접 추출해 해결했다. (상세: `docs/API_탐색결과.md`)

## 3. 공개 상태

- **깃허브 저장소**: https://github.com/jisangj03-dev/incheon-port-analysis
- **사이트(GitHub Pages)**: https://jisangj03-dev.github.io/incheon-port-analysis/
- **저장소 구조**:

```
.
├── README.md          # 보고서 허브 (프로젝트 소개 + 보고서 목록 링크)
├── reports/           # 분석 보고서
│   ├── report_01_공컨테이너_물동량.md
│   ├── report_02_공컨테이너_비율.md
│   ├── report_03_공컨테이너_수출입방향.md
│   └── images/        # 보고서 차트 PNG (스크립트가 여기에 생성)
├── analysis/          # 재현용 코드·데이터 (모든 .py, .csv, config)
│   └── probe/         # 주제검증용 프로브 스크립트·원시 CSV (#04)
├── docs/              # 조사·기록 문서 (이 파일 포함)
└── .claude/skills/    # 보고서분석·보고서발행 SKILL.md (절차 표준화)
```

- README는 **허브(보고서 목록)** 이고, 실제 보고서 본문은 `reports/` 안에 있다. 코드는 모두 `analysis/` 폴더 안에서 실행한다.

## 4. 환경 주의사항 ⚠️

- **작업 폴더**: `문서\역량부서\인천항물동량`
  - ⚠️ **`수출서류랩`(다른 프로젝트)과 절대 섞지 말 것.** 완전히 별개 프로젝트다.
- **클로드 코드를 켤 때 반드시 경로부터 확인**한다. 엉뚱한 폴더에서 작업하면 파일이 뒤섞인다.
- **`config.py`(공공데이터 인증키)** 는 `.gitignore`로 제외되어 저장소에 올라가지 않는다. 새로 받으면 `analysis/config_example.py`를 복사해 `analysis/config.py`로 만들고 키를 넣는다. **절대 커밋하지 말 것.**
- **push가 2분 타임아웃**나면, 커밋까지만 하고 멈춘 뒤 **별도 PowerShell 창에서 `git push origin main`** 을 직접 실행한다.

## 5. ✅ 해결된 이슈 (기록용)

**증상(과거)**: GitHub Pages 사이트에 옛날 화면(#01 보고서 전체 + `fetch_test.py` 같은 옛 파일 구성 표)이 떴었다.

**원인**: 저장소 파일은 정상이었고(README는 "보고서 허브"로 정상), **Pages 서빙(빌드/캐시)** 지연 문제였다.

**해결**: 이후 커밋들이 재배포되면서 **자연 반영**되어 최신 허브 화면으로 정상화됐다. 별도 조치 없이 해소됨.

## 6. 오늘 작업 기록

**2026-07-14 (#04 분석·발행)**
- **#04 분석**: 공컨 API(15157693) 2022~2025 4개년 원시 재수집 → 검증 게이트 4종 통과(모집단 ocCt=1·소수 앵커·_10 기록 전환) → 집계·차트 2매 산출. 로컬 커밋 2건(`c84bc21`, `7934633`).
- **#04 발행(2026-07-14 발행)**: README 허브에 #04 등재("보고서 전문 보기 →" 링크)·저장소 구조 트리·체인 인용블록 4편으로 교체(관찰→검증→규명→지속성). #03 §5·#01 §3-2에 후속(#04) 링크 추가. TODO §3 연도 비교 항목 ✅. 본문 수치 무변경.
- **핵심 결과**: 48개월 전부 수출 방향 우위, 월별 배율 바닥 3.9배(2023년 12월). 2월 전월 대비 감소는 4개년 모두 관측(−10.8%~−38.4%), 단 2024년 연중 최저는 11월. 전체 컨테이너 연간값 2024년 3,558천TEU·2025년 3,444천TEU(hwpx 원문 인쇄값, #02 분모와 동일).

**2026-07-13 (#04 주제 검증·확정)**
- **후보 3종 프로브 검증 → #04 주제 = 후보 ② 연도 비교(2022–2025 공컨 방향 추세) 확정.** 상세 `docs/04_주제검증.md` §4, 프로브 `analysis/probe/`(A/B/B2/C/C2).
- **후보 ① 터미널별 → #05 1순위 이월**(hwpx 2025 12/12·2024 3부두 12/12 가용). **후보 ③ 규격별 → 기각**(_99 공식 정의 미확인; TEU 환산식·방향별 구성은 `GInOut_코드규명.md` §8 박제).
- **⚠️ 0패딩 프로브 버그 정정**: 초기 프로브가 `searchStartM=1`(0패딩 누락)로 2024를 "10~12월뿐"으로 오탐 → 후보 ②를 한때 탈락 분류. `"01"` 정정 후 **2022~2025 전 연도 가용** 확인. 인수인계 "2024 조회 정상" 기록은 옳았고, 틀린 것은 프로브였다.
- 커밋: `5511313`(프로브 B2·C2+정정+규격 박제), `b2ed73c`(주제 확정) 등.

**2026-07-13 (#03 발행)**
- **#03 발행**: README 허브에 #03 등재("보고서 전문 보기 →" 링크), 저장소 구조 트리·3편 체인 인용블록 갱신.
- **#01 용어 통일·후속 링크**: '반입·반출' → 공식 축 용어 '수출입'(머리부·부록 2곳), §4에 #03 후속 링크 추가, 수정 이력 1줄(수치 무관).
- **#02 후속 주석**: §5 후속 과제의 적컨 방향 분해 항목에 #03 규명 결과·적컨 직접 분해 불가 판정 주석 추가.
- **TODO 정리**: §5 ⬜(#03 발행 일괄)·§3 반입/반출 분리 차트 항목 ✅ 완료 처리.
- **커밋 1건 + push 완료.** (수치·표·차트 무변경 확인)

**2026-07-13 (절차 표준화 + Quarto 판단)**
- **절차 표준화**: `.claude/skills/보고서분석/SKILL.md`, `.claude/skills/보고서발행/SKILL.md` 2종 추출(#01~#03 3회 반복 근거, 커밋 `2cfb3cc`). 기계적 반복만 담고 판단 지점은 ⛔ 정지선 표기. 검증 게이트는 일반형으로 정리 — 구조는 재사용, 기대값은 보고서별 확정(`1d31938`).
- **Quarto 도입 판단**: 기각·조건부 보류(`docs/벤치마킹.md`에 재도입 조건 3종 기록).

**2026-07-11 (오후 — #03 분석·초안)**
- **#03 분석 완료**: `analysis/analyze_direction.py` 신규(원시 재수집→검증 게이트 5종→방향 집계→차트 2매). 검증 게이트 **5종 전부 PASS**(G1 행수·코드집합 / G2-① 연안항 0 / G2-② 월합 12개 CSV 일치 / G2-③ 연간 991,170 / G3 1~3월 회귀). 결과 확정: **연간 수출 85.1%(843,838 TEU) vs 수입 14.1%(139,418 TEU), 배율 6.1배**, 환적 0.8%. 월별 배율 4.1배(3월)~8.3배(6월).
- **#03 초안 작성**: `reports/report_03_공컨테이너_수출입방향.md` (#02 골격). 원시 데이터 `analysis/container_2025_direction.csv`(규격별 8필드 포함)·차트 2매 생성.
- **로컬 커밋 1건 완료, push 대기.** README·#01·#02·타 docs는 발행 세션에서 일괄.

**2026-07-11 (오전 — 코드 규명)**
- 공컨 API `GInOut`·`ocCt` **코드 [확정]** → `docs/GInOut_코드규명.md`. 공식 항목정의서(구 페이지 15130235의 2026-03-13 웨이백 스냅샷)로 확인: `GInOut`=수출입구분(**1=수입·2=수출·3=수입환적·4=수출환적**), `ocCt`=외내항구분(**1=수출입항·2=연안항**). 실측 잠정 판독(방향·환적)이 적중, 3·4는 환적의 수입/수출 세분으로 확인.
- **#01 표기 정정**: 원시 "한 달 64행" 서술 오류를 **2025년 전체 64행(월 4~6행)**, 축은 **수출입구분×외내항구분**으로 사실 정정(캡션·본문·`analyze_container.py` 주석). 수치 변경 없음. #01에 수정 이력 1줄 추가.
- **구 데이터 ID 교체**: `15130235 → 15157693` (`calc_ratio.py` 등). `03_주제검증.md`에 코드규명 크로스링크·[확정] 갱신.
- #03 주제 **A′안(공컨 방향 분해)으로 확정**, 코드 [확정] 완료로 **착수 가능**.

**2026-07-10**
- README 보고서 목록의 두 링크 텍스트를 파일 경로 대신 **"보고서 전문 보기 →"** 로 변경 (커밋 `c5297bb`). 괄호 안 파일 경로는 그대로 유지.
- 보고서 #01·#02의 **작성일 연도 오기 수정**: `2025-07-08 → 2026-07-08`, `2025-07-09 → 2026-07-09`. (분석 대상의 "2025년" 등 다른 날짜는 그대로 둠.)

## 7. 다음 작업

(a) 홍보 확정·1단계(README) 완료. 다음: 2단계 취업 서류(코워크 첫 실사용, 회장님 입력 필요) → 3단계 링크드인 → 4주 반응 관찰. 그 후 또는 병행으로 #05 주제 검증(부두별 hwpx). 홍보 전략 전문은 `docs/홍보전략.md`.

**2단계 재정의 (취업 서류)**
- 기존 취업 서류 없음 확인 → "단락 추가"가 아니라 **이력서 신규 제작**. 코어 문안([A] 이력서 블록·[B] 자소서 문단)은 챗(2026-07-14 새벽)에서 작성 완료, **회장님 확정 대기**.
- **코워크 첫 세션 순서**: ① 새 폴더 `문서\취업서류` 생성(공개 저장소 밖) ② `코어문안.md`에 [A]·[B] 저장 ③ 이력서 docx+PDF 조립 ④ 포트폴리오 1장 PDF.
- 확장 사다리(사이트·오디언스·전자책)는 `docs/홍보전략.md` 게이트 절 참조.

**#05 후보 뱅크**
- **① 터미널별(hwpx, 2024–2025)** — 신항/남항/국제여객부두. 2025 12/12·2024 3부두 12/12 가용. "신항=SNCT+HJIT"(특정 기업 아님), '그외'=인천⇔제주 연안·내항·북항(원문 각주). **1순위.**
- **② 규격별(10/20/40ft)** — 공컨 API 원시에 이미 수록되나 _99 공식 정의 미확인(`GInOut_코드규명.md` §8). 정의 확보가 선결.

## 8. 작업 원칙

- **데이터 검증 먼저, 계산은 나중.** 후보 데이터가 목적(인천항·월별·TEU·적/공 구분)에 맞는지 직접 호출·확인한 뒤에 계산한다.
- **API가 없으면 공식 통계로 보완.** 추정치를 만들지 않고 공표된 실측값만 쓴다. (#02 분모가 그 사례)
- **AI 활용은 정직하게 명시하되, 판단은 직접.** 데이터 선정·검증·해석은 사람이 한다고 보고서·README에 명기한다.
- **한 세션에 몰아넣지 말 것.** 분석 작업과 사이트(구조·배포) 작업을 분리해서 진행한다. 비용·집중도 관리에 유리하다.

## 9. 참고 문서

- `docs/GInOut_코드규명.md` — 공컨 API `GInOut`·`ocCt` 코드 실측 규명 (크로스탭·판독 논리·[확정] 완료(웨이백 공식 항목정의서). 규격 필드 TEU 환산식 포함)
- `docs/API_탐색결과.md` — API 후보 탐색·검증 로그 (분모 확보 방법론 포함, 면접 설명용)
- `docs/벤치마킹.md` — 보고서 퍼블리싱 벤치마크 조사 (KMI 넘버링, OWID 구조, Quarto 도입 시점)
- `docs/TODO.md` — 후속 과제 메모
