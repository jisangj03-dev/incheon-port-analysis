# 다음 세션 인수인계

**최종 갱신: 2026-08-12 (세션: #07 발행 — 서두 정리)**
**적용 지침: 프로젝트 지침 v1.1 (2026-08-04, 비공개 보관). 구 마스터 지침 v6.x는 아카이브(참조 전용).**

이 문서와 지침이 어긋나면 **지침이 우선**한다.
작업 폴더: C:\Users\user\OneDrive\문서\역량부서\인천항물동량

---

## 0. 사용법
- 컨텍스트가 리셋돼도 「지침 + 이 문서」 두 개로 맥락이 복원되어야 한다.
- 세션 시작 시 경로 게이트: `git rev-parse HEAD`로 폴더 오인을 차단한다.
- **원격 동기화 상태(ahead/behind)는 이 문서에 적지 않는다.** `git status -sb`로 확인한다.
  상태값을 문서에 박으면 반드시 stale 해진다 (2026-08-12 개선).

## 1. 현재 상태
- 공개 저장소: github.com/jisangj03-dev/incheon-port-analysis (GitHub Pages 연동)
- 보고서 **#01~#06 발행 완료**
- **#07 판정 완료 · 미발행.** 판정 커밋 = 5430224
- 진실의 원천 = `docs/07_판정결과.md`
- 선커밋 기준 = `docs/07_주제검증.md` §4 (blob c95bbfc859c67fd7cdddb497eb822eadce4b3544)

## 2. #07 선커밋 완주 (2026-08-04)

대상 범위 S = 2026-01~06 (표본외 연장 검증 + 전년 동기 비교).

| 기준 | 결과 | 값 |
|---|---|---|
| V1 방향 | PASS | 수출 74,065.75 > 수입 8,570.25 |
| V2 배율 | PASS | 8.642 (기준 3.9 이상) |
| V3 월별 격차 | PASS | 6/6 성립 |
| V4 전년 동기 대비 | PASS | +34.4%p (기준 +5.0%p 이상) |

- 기준 무수정 · 사후조정 0 · 이탈 0. 실행 게이트 1~5 전부 통과.
- X-CHECK(챗 선공개 → Claude Code 독립 산출) 불일치 0건.
- 격차 부호 연속 성립 기록: 2022-10부터 **45개월**로 연장.
- 선커밋 이행 기록 원본은 비공개 본부에 보관(공개 저장소 미포함).
- **2026-08-12 재검산**: 발행 세션 서두에서 챗이 `container_2026_direction.csv` 원시 36행으로
  판정결과 전 수치(V1~V4, 배율 5건, 반기 합산, 전년 대비 14건, Teu 항등식, 정수성 288셀)를
  독립 재계산 — **불일치 0건**. 게이트 1 닫힘 검산 재확인.

### 신규 산출물 (커밋 5430224)
- `analysis/collect_07_2026_direction.py` — 수집
- `analysis/judge_07_2026.py` — 판정
- `analysis/container_2026_direction.csv` — 2026-01~06, 36행(헤더 제외)
  sha256 DF63A87327A0E34A97B08CB29F07466B67FE532059D07110A3E40072F6445B45
- `analysis/probe/recheck_2025_direction_20260804.csv` — 게이트 1 증거 보존, 64행
  sha256 CCD319B157274905975A79C02561FCBE081FB392F8AA55734FF5FED8CCEF774B
- `docs/07_판정결과.md`

## 3. 다음 작업 (발행 세션, 순서 고정)
1. ~~인수인계 갱신 · `.gitignore` 보강 · push~~ — 2026-08-12 처리.
2. **차트 2종 렌더** — ⓐ V3 월별 격차, ⓑ 전년 동기 대비. 커밋 전 로컬 렌더로 눈으로 확인.
3. **보고서 #07 본문 작성.** §1 요약 · §3 해석은 **회장 직접 작성** — 문답 방식, 대필 금지.
   Claude는 핵심 포인트·가드레일 제공과 회장 문장 교열까지만.
   문안 주의: 2026 수치를 "확정"으로 쓰지 않는다. "산출 완료(잠정치)"로 표기한다.
4. Pages 반영 확인은 Ctrl+Shift+R 캐시 무효화 후.
5. 유통 — 링크드인 · 인스타그램 · 스레드 3채널. 1편 파일럿으로 실소요 시간을 먼저 측정하고,
   측정 후에 유통 페이스를 수치로 선커밋한다. 측정 전 페이스 확정 금지.
   카드뉴스 상한 1건 1시간, 템플릿 1회 제작 후 재사용.

## 4. 불변 정지선 (저장소 레벨)
- `_probe_dump.txt` — 커밋 · 삭제 · **열람** 전부 금지. untracked 유지.
  2026-08-12 `.gitignore` 등재로 이중 방어.
- `probe_2026_raw.csv` — 삭제 · 열람 금지 (노출 사고 재발 방지).
- `analysis/container_2025_direction.csv` — 수정 · 덮어쓰기 금지.
  sha256 EA17A4CA45FD59B308E558D1A01C9F9E7D66B885C1DEF13A1112B72BC8EDD954
- `analysis/container_2026_direction.csv` — 행 순서에 의미 없음(API 비고정). 정렬·재저장 금지.
- `config.py` — 인증키 보관. `.gitignore` 등재 상태 유지. 공개 저장소 절대 미포함.
- **2026-01~05 방향 · 배율은 검증이 아니다**(강등 구간). 참고 통계로만 쓰고
  `docs/07_주제검증.md` §0 노출 이력 ②를 별표로 인용한다.
- **2026 전 구간은 잠정치.** 적재일 병기 + 수집일 명기 + 사후 수정 가능성 고지.
- 인과 · 목적 서술 금지. `_99` = "기타 규격" [미확인] 유지.
- 규격 비중은 박스 수 · 소수 1자리. TEU 금지(순환 논증).
- 2026 방향별 CSV 경로 = `analysis/` 직하 [확정]. `analysis/data/` 신설 금지.
- 공개 수치는 검증 가능한 1차 출처만.
- **push 전: 회장 통독 + 챗 전문 전수 검산.** 전문은 실물 바이트 덤프로 — 편집기 미리보기 경유 금지.
- 차트는 커밋 전 로컬 렌더.

## 5. 기술 위생 — 승계된 사실
- 원시 파일 파싱은 **태그명 기준**. 위치 인덱스 금지.
- **API 응답 행 순서는 호출마다 비고정** [확정]. 2025 재수집분과 정본이 3,171B로 바이트 동일이나
  위치 기준 462셀 불일치, 자연키(`yyyy, mm, GInOut, ocCt`) 매칭 시 64행 x 14열 전 셀 완전 일치.
  → 대조는 **자연키 매칭으로만** 한다.
- 회귀 앵커 검증은 기존 CSV 재합산이 아니라 **동일 코드 경로로 재수집**해 맞춘다.
  기존 파일 재합산은 그 파일이 옳다는 전제로 그 파일을 검증하는 순환이다.
  재수집분은 별도 파일로 보존하고 정본을 덮어쓰지 않는다.
- 강한 앵커는 **git blob 해시 또는 SHA-256**. PowerShell `Measure-Object`의
  `-Line`(빈 줄 미포함) · `-Character`(한글을 1자로 계수)는 바이트가 아니다.
- API 응답 성공이 데이터 완전성을 보장하지 않는다. totalCount · 월별 존재 확인 필수.
  제로 패딩 주의("01" != 1). 음수 주장(존재 없음)은 양수 주장보다 엄격히 검증한다.
- X-CHECK는 양측이 서로의 값을 보기 전에 산출해야 실효가 있다. 챗 선공개 → Code 독립 산출 순서 유지.
  Claude가 외부 API에 접근 불가한 수집 계층은 사전 프로브 관측치 대조로 대체하고 그 사실을 명시한다.
- 텍스트 1줄 정정도 `ReadAllBytes` → 치환 → `WriteAllBytes` 바이트 왕복으로만 한다.
  PowerShell `Set-Content` · `Out-File`은 BOM과 줄끝을 조용히 바꾼다. **대상에 따라 규격이 다르다 - 본부(OneDrive) md는 BOM+LF, 이 저장소 md는 BOM 없음·LF.** 섞지 마라.
- 편집 지시 전에 **대상 문자열의 현재 존재 여부부터 대조**한다. 일치 개수가 1이 아니면
  쓰기 전에 중단하는 가드를 붙인다. 가드 없는 Replace는 아무 일도 안 하고 "완료"만 보고한다.
- 검증 지표는 선언 전에 "그 수단이 대상을 실제로 집계하는가"부터 확인한다.
  실증: GitHub Insights > Traffic은 Pages 방문을 집계하지 않는다.
- 롤링 윈도우 지표는 당일 캡처. 지연 = 영구 소실.
- 발행 진실은 렌더된 화면 · 디스크 바이트 · git blob · tarball. 편집기 미리보기 신뢰 금지.
- 프로브는 값 비노출 설계(존재 여부 · 행 수 · 적재일만 출력).
- 이 IDE는 긴 명령에서 `EPERM: uv_spawn`으로 프로세스가 뜨지 않는 사례가 있다.
  긴 스크립트는 내용 수정 없이 덩어리로 분할해 순서대로 실행한다.

## 6. 관측 (해석 미착수)
- 2026-01~06 내항(`ocCt=2`) 12행 전부 0 [관측]. 후속 시리즈 소재 후보. **해석 없음.**

## 7. 종결 · 정정
- **링크드인 4주 관찰: 2026-07-31(D+17) 조기 종료. 판정 없음.**
  사유는 성과 부진이 아니라 전략 재편 — 새 지침이 채널 상시 발행을 전제하므로
  관찰의 전제(4주 침묵)가 소멸했다.
  **D+28(2026-08-11) 캡처 의무 소멸. "관찰 종료 전 링크드인 신규 · 수정 콘텐츠 금지" 조항도 함께 해제.**
  D+14 확보분(게이트 1·2·3 전부 0, 지표 F 계측 결함 문서화)은 비공개 본부에 보존.
- 위 종결에 따라 구 "다음 작업 후보" 목록의 D+14 관찰 · D+28 판정 항목은 폐기.
- 확정 채널 3 = 링크드인 · 인스타그램 · 스레드. 원본 허브 = GitHub Pages.
  블로그 계열은 쓰지 않는다(회장 확정). 모든 채널 게시물은 Pages 원문 링크를 포함한다.

## 8. 미해결
- **저장소 · 브랜드 구조 미정** — 다중 시리즈를 담을 그릇을 정해야 한다.
  사이트 승격(게이트 1) 판정도 여기에 포함해 함께 결정한다. 자동 승격 아님.
- #05 = 3개년 조건부 채택 유지. **2025 전체 방향별 승격은 보류** —
  IPA 직접 문의 또는 공식 공개 시에만 해제.
- 구 지침 v6.1 본체 파일 위치 미확인. 발견 시 아카이브로. 교훈 핵심은 지침 §8에 승계됨.
- 이력서 학점 · 어학 미기재. 실물 확인 후 보강, PDF 재출력은 Claude에게.
