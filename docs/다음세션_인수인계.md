# 다음 세션 인수인계 문서

> 다음 세션에서 가장 먼저 읽는 문서. 현재 상태·주의사항·다음 할 일.

## 1. 프로젝트 개요
- **이름**: 「인천항 물동량 분석」 (역량부서 프로젝트, 취업 역량 증명)
- **핵심 컨셉**: 직접 탐색·검증, 추정치 없이 **공표 실측값만**, 해석까지 담아 번호(#01…)로 누적.

## 2. 완성된 보고서 (5편)

| 번호 | 제목 | 핵심 결론 |
|---|---|---|
| #01 | 공컨테이너 물동량 (2025) | 연간 99만 TEU, 외국적 88.2%, 2월 −31.2% |
| #02 | 공컨테이너 비율 (2025) | 전체의 28.8%가 빈 컨테이너 |
| #03 | 공컨테이너 수출입 방향 (2025) | 공컨 수출 85.1%(수입의 6.1배) |
| #04 | 불균형 지속성 (2022–2025) | 48개월 전부 수출 우위, 월 최소 3.9배 |
| #05 | 컨테이너 수지 (2022–2024) | 전체 방향 균형(1.0배)이나 적컨은 수입 2배 안팎(2.20/1.94/2.02배), 공컨 수출 쏠림의 거울상. 3개년 조건부 채택. |

## 3. 공개 상태
- 저장소: https://github.com/jisangj03-dev/incheon-port-analysis
- 사이트: https://jisangj03-dev.github.io/incheon-port-analysis/
- **origin/main 최신 = `9df3d60` (ahead 0).** #05 커밋 체인: `53a337a`(발행) → `d8488d5`(차트1 범례 겹침 교정) → `9df3d60`(README #05 반영).
- 구조: README / reports(#01~#05 + images) / analysis(코드·CSV·`chart_05_balance.py`·`calc_jeok.py`·`total_container_direction.csv`·probe) / docs(03·04·05_주제검증, GInOut_코드규명, API_탐색결과, 벤치마킹, TODO, 이 파일) / .claude/skills 2종.
- ⚠ `_probe_dump.txt`는 **untracked 유지**(커밋 안 함). 도구가 반복 삭제를 제안해도 무시.

## 4. 환경 주의 ⚠
- 작업 폴더: `C:\Users\user\OneDrive\문서\역량부서\인천항물동량`. 켤 때 경로 확인.
- `config.py`(인증키) `.gitignore` 제외 — **절대 커밋 금지.**
- push 2분 타임아웃 시 별도 PowerShell에서 `git push`.
- 실행: 안티그래비티 IDE 터미널의 `claude`. 내장 Agent(Gemini)는 클로드 코드 아님.

## 5. 최근 작업 기록

**2026-07-15 (#05 발행)**
- 주제검증(조건부 채택) 위에서 발행. **§1 요약·§3 해석 = 회장 직접 서술(불변 선)**, 표·차트·§4·§5·부록·조건 3·각주 2 = 챗 조립.
- 적컨 = 전체 방향별(공표) − 공컨 방향별(API) 복원. `chart_05_balance.py`(게이트: 배율·항등식 대조) → 차트 2매. `calc_jeok.py` 교차검증.
- #02 §5("적컨 방향 분해 불가") 오판 정정, #04 §3-⑤("어느 부두가") 이월문장 정정 → 둘 다 #05로 연결.
- README 목록·소개문·저장소 트리에 #05 반영.
- **3층 검증 전부 통과**: 챗 원문 대조(tarball) + 클로드 코드 게이트 + 회장 브라우저 렌더.
- 발행 중 포착·수정: 표2 각주 `#03·#04` 훼손(원문 대조로 확인) / 차트1 범례↔주석 겹침(커밋 후 렌더로 발견 → 범례 하단 분리로 교정).

(이전: #04 발행 2026-07-14 / #03 발행·주제검증 2026-07-13 / 코드규명 2026-07-11 — 상세는 각 주제검증 문서·git 로그.)

## 6. 다음 작업
1. **D+14 관찰 (2026-07-28)** — GitHub Traffic 캡처 필수(14일치). 링크드인 4주 관찰(본부 3호 선커밋).
2. **D+28 판정 (2026-08-11)** — 선커밋 기준으로만.
3. (선택·병렬) **2025 방향별 승격** — IPA PRESS ROOM(boardKey=217, menuKey=4035) 수동 / SP-IDC. 확보 시 #05를 4개년 완전 채택으로 갱신(`total_container_direction.csv`에 2025행 추가 → `calc_jeok.py` YEARS 편입 → 표·§4 판정 갱신).
4. **이력서 학점·어학 보강** — 실물 확인 후. PDF 재출력은 Claude(docx 제공).
5. (미래) **#06 주제검증** — 부두별(신항/남항/국제여객부두)은 공컨×부두 교차표 부재로 **유보**(05_주제검증 §0). hwpx 4개년 축적 시 재검토. 다른 후보 발굴.
6. 크몽: 확인 안 감(종료·휴면).

## 7. 작업 원칙 (불변)
- 검증 먼저, 계산 나중. 공식 출처만, 언론·AI 숫자·만TEU 합산 금지.
- **트랙 A 분석·보고서 본문의 에이전트 위임 금지 — 회장 직접(면접 방어).** 계산·차트 코드는 클로드 코드 가능.
- AI 활용 명시. push 전 회장 통독. 발행물의 진실은 렌더(교훈 24) — 챗 원문(tarball) + 회장 브라우저.
- 세션 분리(분석/발행/전략/주제선정). 폴더 분리 엄수.
- 상쇄율은 항등식 → 초과분 분해로만. "정산/메운다/쌓인다/구조 단정" 금지.

## 8. 참고 문서
- `docs/05_주제검증.md` — #05 판정·조건·항등식 규명·소스 규명 (진실의 원천)
- `docs/GInOut_코드규명.md` — 공컨 API 코드 [확정]·TEU 환산식
- `docs/API_탐색결과.md` — API 탐색 로그 / `docs/벤치마킹.md` — 퍼블리싱 벤치마크·Quarto 재도입 조건 / `docs/TODO.md` — 후속 메모(구버전, 본 문서 §6 우선)
