# 검수 기록

> 저작 구조 B의 검수 표기를 뒷받침하는 기록이다.
> `python analysis/review_pick.py <보고서> --log` 가 한 줄씩 붙인다.
> **기록이 없는 편은 검수 표기를 달지 않는다**(지침 §2.2-4).
> **[지침 v7.0] 대조는 하는 쪽이 한다** — 운영자든 세션이든. 발행 게이트가 아니라 표기의 근거다(§2.3).

**[2026-08-26 추첨 방식 변경]** 이 날부터 추첨은 **결정론**이다 — 시드 = `보고서 파일명 + 날짜`.
같은 보고서를 같은 날 몇 번 돌려도 같은 값이 나오므로 **재실행으로 쉬운 값을 고를 수 없다.**
아래 첫 행(48/124)만 구 방식(매 실행 무작위)으로 기록됐고, 그래서 시드 날짜 칸이 비어 있다.
그 행이 기록하는 대조 자체는 실제로 수행됐다.

| 일시 | 문서 | 추첨 · 시드날짜 | 대조한 값 | 위치 |
|---|---|---|---|---|
| 2026-08-26 03:21 | report_07_공컨테이너_표본외검증.md | 48/124 | 50,336 | :63 |
