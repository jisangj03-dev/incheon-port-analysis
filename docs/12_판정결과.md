# #12 판정 결과 — 220개월 중 39개월

작성일: 2026-09-12
분석 대상: 인천항 컨테이너 **터미널별** 월별 처리량 · 2023-03 ~ 2026-07 · 창 41개월 · 관측 39개월
기준 문서: `docs/12_주제검증.md` — **선커밋 blob `894439103c58ec4da3d8794a1f43f9b2e0aee2ab`**
판정 코드: 아직 없다
상태: **판정 미착수.** 이 문서는 지금 **제목 정정과 노출 이력**만 든다.

**선커밋 기준 무수정. 사후조정 없음.**

---

## 0. 제목 정정 — **선커밋의 제목이 넓었다** [2026-09-12 운영자 지적]

**선커밋 문서의 제목은 「창을 되찾는다: 터미널 층을 41개월로」다. 그 제목이 넓다.**

| | 수 | 비율 |
|---|---|---|
| 게시판이 든 「항만운영통계」 글 | **220개월** (2006-09 ~ 2026-07) | — |
| 그중 **우리가 읽은** 달 | **39개월** | **17.7%** |
| 이 편의 창(빈 두 달 포함) | 41개월 | 18.6% |

**되찾은 것이 아니라 읽을 수 있는 데까지 간 것이다.** 나머지 181개월은 **원문에 있고 우리가 못 읽는다** —
첨부가 `.hwp`·`.xls` 이고 우리 파서는 HWPX 만 연다. **「원문에 없다」가 아니다**(선커밋 §2·§4 가 이미 그렇게 적었다).

- **선커밋 문서는 안 고친다.** blob `89443910` 이 증거이고 **한 글자도 못 고친다**(§3-7 · 선커밋 §5).
  그 문서의 제목은 **그 시점의 기록으로 남는다.**
- **이 편의 제목은 여기서 고쳐진다.** 발행본 H1 과 리드는 **위 비율을 든다.**
  선커밋 §5 가 「고쳐야 할 일이 생기면 고치지 말고 노출 이력 절을 판정결과에 만들어 그 아래 적는다」고 정해 뒀고, 이 절이 그것이다.
- **기준 셋(S1·S2·S3)과 창(2023-03~2026-07)은 안 바뀐다.** 바뀌는 것은 **제목의 범위**뿐이다.

**같은 얼굴이 이틀에 두 번 났다**(사고 116). #11 의 제목도 본문보다 넓었다 —
「다섯 곳을 **대표하지 않는다**」인데 잰 것은 부호 동행 하나였다. **두 번 다 본문은 정직했고 제목만 넓었다.**
처분은 발행 SKILL §3 과 `lint_publish.py` 의 `[범위]` WARN 이 든다.

## 1. `.hwp`·`.xls` 를 읽는 일 — **이 편에 넣지 않는다** [2026-09-12 운영자 결정]

**별도 과제로 세운다.** 지금 넣으면 선커밋이 흔들린다 — 창이 바뀌고, 창이 바뀌면 S1 의 역월별 해 수와
S3 의 정합 창이 같이 바뀐다. **기준이 창을 따라가는 것**이고 §3-7 이 막는 것이 그것이다.

- **무엇을 읽어야 하나.** 2006-09 ~ 2023-02 의 첨부 181건. `.hwp`(대부분)와 `.xls`(일부 달의 품목별 실적).
  `analysis/probe/terminal_axis_history.csv` 가 달마다 어느 상태인지 든다.
- **왜 어려운가.** HWP 5.0 은 OLE 복합 문서이고 본문이 압축 스트림 안에 있다. HWPX 처럼
  표준 라이브러리(`zipfile`+XML)만으로 안 열린다. **의존성을 들이는 결정이 먼저다.**
- **읽어도 축이 있다는 뜻은 아니다.** 터미널 단위 표가 그 서식에도 있었는지는 **[미확인]**이다.
  열어 보기 전에는 모른다 — **「글이 있다」와 「그 축이 있다」는 다르다**(사고 26).
- **순서.** ① 표본 몇 달을 열어 축의 실재를 확인(값 비노출) → ② 되면 그 구간으로 **새 편·새 선커밋**.
  **#12 의 기준을 그 창에 옮겨 붙이지 않는다.**

## 8. 노출 이력

**기준 변경 없음.** 선커밋 이후 `docs/12_주제검증.md` 를 한 글자도 안 고쳤다
(확인 = `git hash-object docs/12_주제검증.md` → `894439103c58ec4da3d8794a1f43f9b2e0aee2ab`).

| | 무엇 | 판정에 미친 영향 |
|---|---|---|
| ① | **제목 정정**(§0). 「되찾는다」 → 「220개월 중 39개월」 | **없다.** 창·기준·모집단이 그대로다 |
| ② | **`.hwp`·`.xls` 판독을 별도 과제로 분리**(§1) | **없다.** 이 편의 창에 안 들어온다 |

**값은 아직 안 봤다** — 수집은 끝났고(`analysis/terminal_monthly_long.csv` · 39개월) 판정 코드는 없다.
