# #12 판정 결과 — 220개월 중 39개월

작성일: 2026-09-13 (선커밋 2026-09-12)
분석 대상: 인천항 컨테이너 **터미널별** 월별 처리량 · 2023-03 ~ 2026-07 · **창 41개월 · 관측 39개월 · 판정 불가 두 달**
기준 문서: `docs/12_주제검증.md` — **선커밋 blob `894439103c58ec4da3d8794a1f43f9b2e0aee2ab`**
판정 코드: `analysis/judge_12_terminal_long.py` (`--selftest` 있음)
도해 코드: `analysis/chart_12_share_long.py` (`--selftest` 있음)
상태: 판정 완료

**선커밋 기준 무수정. 사후조정 없음.**

---

## 0. 제목 정정 — **선커밋의 제목이 넓었다** [2026-09-12 운영자 지적]

**선커밋 문서의 제목은 「창을 되찾는다: 터미널 층을 41개월로」다. 그 제목이 넓다.**

| | 수 | 무엇의 비율인가 |
|---|---|---|
| 게시판 구간 폭 (2006-09 ~ 2026-07) | **239개월** | — |
| └ 그중 **게시판에 글이 있는 달** | **220개월** | 구간 폭의 92.1% |
| └ **게시판에 글이 없는 달** | **19개월** | 239 − 220 |
| 이 편의 창 (2023-03 ~ 2026-07) | 41개월 | — |
| └ 그중 **우리가 읽은** 달 | **39개월** | **글이 있는 220개월의 17.7%** |
| └ **우리가 못 읽는** 달 | **181개월** + 창 안의 두 달 | 220 − 39 = 181 |

**세 수를 갈라 적는다** — **239**(폭) · **220**(글이 있는 달) · **19**(글이 없는 달).
**제목의 분모는 220 이다** — 없는 글을 못 읽은 것으로 세지 않는다.
**「글이 없다」와 「우리가 못 읽는다」는 다르다** — 앞은 원문이 없고, 뒤는 원문이 있는데 파서가 못 연다.
재측정·재현 = `python analysis/probe/probe_15_board_depth.py --full` (재측정 2026-09-13).

*[2026-09-13 정정] 이 표는 처음에 **폭 239 와 빠진 달 19 를 안 들었다.** 그리고 발행본 리드가
「게시판에는 220개월이 **다 있고**」로 나갔다 — **거짓이다.** 두 끝을 뺀 독자가 239 를 얻고
220 과 맞출 수 없던 자리다. 발행본 각주 3 과 §9 가 같이 든다. **판정값에 영향 없다**(19개월은 창 밖).*

**되찾은 것이 아니라 읽을 수 있는 데까지 간 것이다.** 못 읽은 것은 **두 종류이고 섞으면 안 된다** —
**181개월은 원문이 있는데 우리 파서가 못 연다**(첨부가 `.hwp`·`.xls` · 우리 파서는 HWPX 만 연다) ·
**19개월은 게시판에 글 자체가 없다.** **앞쪽에만 「원문에 없다」가 아니다」가 붙는다.**

- **선커밋 문서는 안 고친다.** blob `89443910` 이 증거이고 **한 글자도 못 고친다**(§3-7 · 선커밋 §5).
- **발행본의 제목이 그 비율을 든다** — `reports/report_12_터미널별_장기창.md`.
- **기준 셋과 창은 안 바뀐다.** 바뀐 것은 **제목의 범위**뿐이다.

**같은 얼굴이 이틀에 두 번 났다**(사고 116) — #11 「다섯 곳을 **대표하지 않는다**」·#12 「**되찾는다**」.
**두 번 다 본문은 정직했고 제목만 넓었다.** 처분은 발행 SKILL §3 과 `lint_publish.py` 의 `[범위]` WARN 이 든다.

## 1. `.hwp`·`.xls` 를 읽는 일 — **이 편에 넣지 않는다** [2026-09-12 운영자 결정]

**별도 과제로 세운다.** 지금 넣으면 선커밋이 흔들린다 — 창이 바뀌고, 창이 바뀌면 **S1 의
역월별 해 수**와 **S3 의 정합 창**이 같이 바뀐다. **기준이 창을 따라가는 것**이고 §3-7 이 막는 것이 그것이다.

- **무엇을 읽어야 하나.** 2006-09 ~ 2023-02 의 첨부 181건. `analysis/probe/terminal_axis_history.csv` 가 달마다 상태를 든다.
- **왜 어려운가.** HWP 5.0 은 OLE 복합 문서이고 본문이 압축 스트림 안에 있다.
  HWPX 처럼 표준 라이브러리(`zipfile`+XML)만으로 안 열린다. **의존성을 들이는 결정이 먼저다.**
- **읽어도 축이 있다는 뜻은 아니다** — 그 서식에 터미널 표가 있었는지는 **[미확인]**이다(사고 26).
- **순서.** ① 표본 몇 달을 열어 축의 실재를 확인(값 비노출) → ② 되면 **새 편·새 선커밋**.

---

## 2. 한 줄

**S1 FAIL · S2 FAIL · S3 FAIL. PASS 0 · FAIL 3.**
**셋 다 선커밋이 미리 적은 귀무기대와 가까운 값이 나왔다** — S1 관측 2개월(기대 2.875) ·
S2 중앙값 0.468(귀무 0.50) · S3 단조 아님(귀무확률 0.0833).

## 3. 판정표

| 기준 | 내용 | 모집단 | 결과 | 기준값 | **사전 난이도**(선커밋이 같이 적었다) | 판정 |
|---|---|---|---|---|---|---|
| **S1** 계절성 | 신항 몫 편차 부호가 관측된 모든 해에서 같은 역월 수 | 역월 12 × 해 2~4 | **성립 2 / 검사 12** | ≥ 7 | 귀무기대 **2.875개월**(어림) | **FAIL** |
| **S2** 쌍 동행 | 터미널 쌍의 전년대비 부호 일치율 **중앙값** | 쌍 **10** | **0.468** | > 0.50 | 귀무 중앙값 **정확히 0.50 — 동전** | **FAIL** |
| **S3** 몫의 단조성 | 3~7월 정합 창의 연평균 신항 몫이 단조인가 | 해 **4** × 다섯 달 | **단조 아님** | 단조 | 귀무확률 **0.0833** (= 2/4!) | **FAIL** |

**PASS 0 · FAIL 3 · 판정 불가 0.**
**사전 난이도는 판정에 안 썼다** — 판정은 관측과 기준만으로 났고 인수시험이 그것을 고정한다.
**귀무모형은 「부호·순서가 무관하고 등확률」 하나로 고정했고 그 가정은 검증하지 않았다.**

## 4. 게이트

| | 결과 |
|---|---|
| 1 소스 실재 | **통과** — 출처 대장 39달 · `SHA256_앞16` 전부 있음 · 달 집합 일치 |
| 2 완전성 | **통과** — 관측 39개월 각각 합계 1 · 부두군 2 · 터미널 5 |
| 3 결합·구조 | **기록** — 아래 정정 |
| 4 판정 불가 | **기록** — **창 41 / 관측 39 / 판정 불가 2**(2023-12 · 2024-01) |
| 5 항등식 | **기록** — 다섯 곳 합 − 공표 합계가 **−1 ~ +1 천TEU** · ±2 초과 달 **없다** |
| 6 지위 | 2026 구간은 **잠정치**다(§4). S3 은 2023~2025 와 2026 을 한 표에 놓아 각주를 단다 |

**⟨판정 중 정정⟩ 선커밋 §0-1 의 전제 하나가 틀렸다.**
그 절이 「부두군 소계 행 수가 3↔4 로 갈린다」를 **이미 본 사실**로 적었는데,
**그 수는 프로브가 계층 `그 외` 를 같이 센 것**이었다(`parse_groups()` 반환 길이).
**CSV 에서 계층 `부두군` 은 39개월 내내 신항·남항 둘로 같다.** 갈리는 것은 **계층 `그 외`** 이고
**17개월에만** 있다: 2023-03 2023-04 2025-04 2025-05 2025-06 2025-07 2025-08 2025-09 2025-10 2025-12
2026-01 2026-02 2026-03 2026-04 2026-05 2026-06 2026-07.

- **게이트 3 이 요구한 일은 그대로 했다** — 무엇이 갈리는지 전수로 기록했다.
  바뀐 것은 **「무엇이 갈리는가」의 이름**뿐이다.
- **국제여객부두는 소계 행이 없다** — 터미널 IPT 가 그 자체로 한 칸이다(FACTS 68 · #11 과 같다).
- **`그 외` 는 판정에 안 넣는다**(선커밋 §2). 몫의 분모는 **공표 합계**이므로 `그 외` 가 있는 달에는
  세 몫의 합이 100%에 조금 못 미친다 — 반올림과 같은 성질이다.
- **기준은 고치지 않았다.**

## 5. S1 — 같은 역월의 편차 부호 · **FAIL**

편차 = **그 달의 신항 몫 − 그 해 관측월 평균 신항 몫.** **0 은 「같다」에 안 넣는다**(이 창에는 0이 없다).

해 평균 — 2023 **66.68%** · 2024 **66.21%** · 2025 **64.97%** · 2026 **67.02%**
(2023 은 3~11월 9달 · 2024 는 2~12월 11달 · 2025 는 12달 · 2026 은 1~7월 7달).

| 역월 | 해 수 | 부호(연도 순) | 모두 같은가 |
|---|---|---|---|
| 1월 | 2 | + − | 아니다 |
| 2월 | 3 | + + − | 아니다 |
| 3월 | 4 | − + − − | 아니다 |
| 4월 | 4 | − − + + | 아니다 |
| 5월 | 4 | + − + + | 아니다 |
| 6월 | 4 | − + − + | 아니다 |
| 7월 | 4 | + + + − | 아니다 |
| 8월 | 3 | + + − | 아니다 |
| 9월 | 3 | + + − | 아니다 |
| 10월 | 3 | + − − | 아니다 |
| **11월** | 3 | − − − | **같다** |
| **12월** | 2 | − − | **같다** |

**성립 2 / 검사 12 · 판정 불가 역월 0. 기준은 7 이상이었다. FAIL.**

**이 표가 든 사실 둘.**
- **되풀이가 있는 역월은 둘이고 둘 다 음(−)이다** — 11월·12월의 신항 몫이 그 해 평균보다 낮았다.
- **12월은 해가 둘뿐이다**(2023-12 를 못 읽었다). **해가 둘인 역월은 우연히 같기가 쉽다** —
  귀무확률이 1/2 이다. 성립 둘 중 하나가 그 역월이다.

**인과를 쓰지 않는다.** 왜 그런지는 이 데이터가 답하지 않는다.

## 6. S2 — 쌍 열의 부호 일치율 · **FAIL**

쌍마다 분모가 다르다 — **어느 한쪽이라도 부호 0 인 달은 그 쌍에서 뺀다**(선커밋 S2).

| 쌍 | 같은 달 / 센 달 | 일치율 |
|---|---|---|
| E1CT ↔ HJIT | 28 / 38 | **0.737** |
| HJIT ↔ ICT | 26 / 38 | 0.684 |
| E1CT ↔ ICT | 24 / 39 | 0.615 |
| ICT ↔ SNCT | 23 / 39 | 0.590 |
| HJIT ↔ SNCT | 18 / 38 | 0.474 |
| E1CT ↔ IPT | 18 / 39 | 0.462 |
| IPT ↔ SNCT | 17 / 39 | 0.436 |
| ICT ↔ IPT | 17 / 39 | 0.436 |
| HJIT ↔ IPT | 16 / 38 | 0.421 |
| E1CT ↔ SNCT | 16 / 39 | **0.410** |

**중앙값 0.468**(5번째 0.462 와 6번째 0.474 의 평균). **기준은 0.50 초과였다. FAIL.**

**이 기준의 성질을 다시 적는다.** 귀무 하에서 각 쌍의 기대 일치율이 정확히 0.50 이므로
**중앙값이 0.50 을 넘을 사전 확률은 대략 절반**이다. **세 기준 중 유일하게 양쪽이 열려 있었고,
열린 채로 떨어졌다.** 관측 0.468 과 기준 0.50 의 거리는 좁다 —
**이 한 값으로 「다섯이 따로 움직인다」고 말할 수 없다.**

**중앙값이 가리는 것.** 위쪽 넷(0.590~0.737)과 아래쪽 여섯(0.410~0.474)이 갈린다.
**그 갈림의 성질은 이 기준이 재지 않았다** — 재려면 폭 자체를 판정 대상으로 세워야 한다(§9).

## 7. S3 — 3~7월 정합 창의 연평균 신항 몫 · **FAIL**

**부분 연도를 섞지 않으려고 3~7월로 좁혔다**(네 해가 전부 갖는 다섯 달).

| 해 | 3~7월 평균 신항 몫 | 앞 해 대비 |
|---|---|---|
| 2023 | **66.60%** | — |
| 2024 | 66.19% | 낮아졌다 |
| 2025 | **65.15%** | 낮아졌다 |
| 2026 | **67.22%** | 높아졌다 |

**세 해가 한 방향이고 마지막 해가 반대다. 단조가 아니다. FAIL.**

**순서만 본 기준이다.** 네 값의 크기 차이는 판정에 들어가지 않았다 —
들어간 것은 「계속 오르거나 계속 내리는가」뿐이다.
**「전환」·「회복」이라고 부르지 않는다**(선커밋 표기 규칙).
**2026 은 1~7월만 있는 해이고 잠정치 구간이다** — 정합 창으로 좁혀도 그 사실은 남는다.

## 8. 참고 통계 (검증 아님 — 결론 자리 금지)

- **부두군 몫의 폭** — 신항 **62.8~68.8%**(6.1%p) · 남항 **19.6~24.1%**(4.6%p) ·
  국제여객부두 **8.8~14.6%**(5.8%p).
- **쌍 일치율의 폭** — 최고 0.737 · 최저 0.410 · 폭 0.327.
- **터미널별 월 최대·최소**(천TEU) — E1CT 29(2025-12)/18(2025-02) · HJIT 122(2026-05)/71(2025-02) ·
  ICT 47(2024-04)/33(2026-02) · IPT 45(2024-12)/23(2024-02) · SNCT 106(2023-03)/73(2026-02).
- **다섯 곳 합 − 공표 합계** — −1 ~ +1 천TEU(게이트 5).

## 9. 이 편이 못 한 것

- **글이 있는 220개월 중 181개월을 못 읽는다.** 첨부가 `.hwp`·`.xls` 다. **「원문에 없다」가 아니다.**
  **그 달들에 터미널 표가 있었는지도 [미확인]**이다.
- **그것과 별개로, 게시판에 글 자체가 없는 달이 19개월 있다**(구간 폭 239 − 글 220).
  **그 19개월은 분모에 안 넣었다** — 없는 것을 못 읽은 것으로 세지 않는다.
- **창 안의 두 달도 못 읽었다**(2023-12 · 2024-01). 그 두 글만 `.hwp` 이고 이웃은 `.hwpx` 다.
- **방향(수출/수입)·공적 구분이 이 소스에 없다.** #11 과 같다.
- **역월별 해 수가 둘~넷이다.** **계절성을 확정할 표본이 아니다** — 낸 것은 부호가 되풀이된 횟수뿐이다.
- **쌍의 갈림을 판정 대상으로 세우지 않았다.** 중앙값 하나로 판정했고 폭은 참고 통계로만 냈다.
- **#11 의 T1~T4 를 이 창에서 다시 판정하지 않았다.** 같은 기준을 넓은 창에 옮겨 붙이면
  **기준이 창을 따라간 것**이 된다. 그 명제가 39개월에서도 서는지는 **새 선커밋의 일**이다.

## 10. 노출 이력

**기준 변경 없음.** 선커밋 이후 `docs/12_주제검증.md` 를 한 글자도 안 고쳤다
(확인 = `git hash-object docs/12_주제검증.md` → `894439103c58ec4da3d8794a1f43f9b2e0aee2ab`).

| | 무엇 | 판정에 미친 영향 |
|---|---|---|
| ① | **제목 정정**(§0). 「되찾는다」 → 「220개월 중 39개월」 | **없다.** 창·기준·모집단이 그대로다 |
| ② | **`.hwp`·`.xls` 판독을 별도 과제로 분리**(§1) | **없다.** 이 편의 창에 안 들어온다 |
| ③ | **선커밋 §0-1 의 「부두군 소계 행 수 3↔4」가 틀렸다**(§4 정정) | **없다.** 게이트 3 은 기록이고 정지 사유가 아니다. 갈리는 것의 **이름**만 바뀌었다 |
| ④ | **정정 [2026-09-13]** — 발행본 리드의 「게시판에는 220개월이 **다 있고**」가 거짓이었다. **폭 239 · 글 220 · 없는 달 19** 를 §0 표와 발행본 §2.1 표가 같이 들게 했다(각주 3) | **없다.** 19개월은 이 편의 창(2023-03~2026-07) 밖이다 |

**값은 선커밋 커밋 뒤에 봤다.** 수집(`terminal_monthly_long.csv` · 39개월)은 blob 이 굳은 뒤에 돌았고,
**#11 의 `terminal_monthly.csv` 는 한 바이트도 안 건드렸다**(출처 대장 경로가 `--out` 을 따라간다).
