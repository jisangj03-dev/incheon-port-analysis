# #08 판정 결과 — 무역 라인 첫 판정 (관세청 항구·공항별 수출입실적)

**판정일 2026-08-26 · 본문 미작성. 이 문서는 판정 기록이다.**

## 선커밋

기준은 `docs/무역라인_개시게이트.md`가 든다. **데이터를 받기 전에 커밋했다.**

| | |
|---|---|
| 선커밋 blob | `df28c251d12570595b8944d7ee28e5618af0e3a4` |
| 커밋 | `a16c241` 「선커밋: 무역 라인 게이트 ①~⑤ — 값을 보기 전에 쓴다」 |
| 대조 | `git show df28c251` 로 판정 시점 기준 전문을 그대로 읽을 수 있다 |

**기준 무수정.** 판정 후 게이트 문서의 질문·창·기준을 고치지 않았다.

**값을 안 본 상태에서 썼다는 근거.** 선커밋 이전에 돌린 것은 `probe_porttrade_schema.py`뿐이고,
그 프로브는 축(태그명·항만코드·행 수)만 내고 값을 마스킹한다. 마스킹이 값의 **자릿수**를
노출하는 것은 한계로 게이트 문서에 적어 뒀다.

## 재현

```
python analysis/probe_customs.py            # 게이트 ① — 종료코드 0
python analysis/collect_porttrade.py        # 수집 → analysis/porttrade_202412_202607.csv
python analysis/judge_porttrade.py          # 판정 — 종료코드 0
```

## 창

| | |
|---|---|
| 기간 | **2024-12 ~ 2026-07 (20개월)** — 선커밋 창 그대로 |
| 모집단 | `portCd = KRINC`(인천항) 행 전부. 월별로 `cstmSgn`(신고 세관)을 가로질러 합산 |
| 단위 | 신고 **건수** · **USD** |
| 원시 | `analysis/porttrade_202412_202607.csv` (10,329행 · 20개월 전량) |

**`ICN`(인천국제공항)·`KRGIN`(경인항)은 포함하지 않았다.** 별개 코드다.

## 검산

| | 결과 |
|---|---|
| **기준 B — 명세 합 = 총계** | **일치.** 20개월 × 값 5종 = 100건 전부 |
| 내적 — `balPayments = expDlr − impDlr` (인천항) | **20개월 전 구간 성립** |

> **기준 B는 첫 실행에서 105건 전건 불일치로 떨어졌다.**
>
> **고친 것은 파서다. 기준은 안 고쳤다.** 총계행의 `year` 값이 「총계」로 와서 월 축이 뭉친 것이고,
> `reqYymm`(요청한 달)을 모든 행에 박아 수집기를 고쳤다. **사고 37.**
> 기준 B의 문장·임계·대상은 그대로다 — `git show df28c251` 로 판정 시점 원문과 대조된다.
>
> **최종 지위: 통과.** 다만 이력을 지우지 않는다 — 「1차 FAIL → 파서 수정 → 재실행 PASS」다.
>
> **순환 위험을 적어 둔다.** 검산을 통과시키려고 코드를 고치는 것은 원칙적으로 위험하다.
> 이 건이 그 위험을 피한 근거는 둘이다. ① 수정 근거가 검산 실패가 아니라 **응답 구조의 직접 관측**이었다
> (총계행에 `year='총계'`가 실제로 들어 있다. 원시 CSV에 그대로 남아 있어 재확인된다).
> ② 수정 후 100건이 **전건 정확히 일치**했다 — 합을 맞추려 손댔다면 부분 일치가 나온다.
> 그래도 **자기가 만든 검산을 자기가 통과시켰다는 구조는 남는다.** 외부 대조(한계 2)가 그 자리를 메운다.

## 판정 — 질문 3개

| | 질문 | 결과 |
|---|---|---|
| **Q1** | 인천항은 수입 우위인가 수출 우위인가 | **금액·건수 둘 다 수입 우위** |
| **Q2** | 그 방향이 몇 개월 연속 성립하는가 | **20개월 / 20개월 창 — 전 구간** |
| **Q3** | 건수 기준과 금액 기준이 어긋나는 달이 있는가 | **0건** |

**게이트 ③ A 통과 · B 통과.** C(FACTS 등재·린터)는 본문 작성 단계의 판정 대상이다.

### 값 · 창 · 지위

**「수입 우위」는 방향이지 값이 아니다.** 값을 창·지위와 함께 따로 낸다.
방향·연속성은 선커밋 질문이라 **`검증`**, 배율은 판정 대상이 아닌 파생값이라 **`관측`**이다.

| 값 | 창 | 지위 |
|---|---|---|
| **20개월** | 2024-12 ~ 2026-07 · 창 길이이자 **수입 우위 성립 월수(전 구간)** | **검증** |
| **0건** | 동일 창 · 건수 방향과 금액 방향이 어긋난 달 | **검증** |
| 1.976배 | 동일 창 합산 · 인천항 수입÷수출 · **USD** | 관측 |
| 6.637배 | 동일 창 합산 · 인천항 수입÷수출 · **신고 건수** | 관측 |
| 2.064배 | **2026-07 단월** · 인천항 수입÷수출 · USD | 관측 |
| 3,830,215,205 USD / 7,906,486,316 USD | 2026-07 단월 · 인천항 수출 / 수입 신고액 | 관측 |

**월별 배율 범위** — 금액 1.593(2025-02) ~ 2.455(2025-01) · 건수 5.570(2025-04) ~ 8.020(2024-12). 전부 `관측`.

**전 구간 잠정치다.** 2026 구간이 섞여 있어 "확정"·"확인됐다"를 쓰지 않는다.

## 무엇과 맞대는가 — 결합 게이트는 선커밋대로 지켰다

`report_05`가 인천항만공사 소스로 **「적컨은 수입, 공컨은 수출」**을 냈다.
이 판정은 그중 **적컨 = 수입 우위** 쪽을 **관세청 신고 체계**에서 다시 본 것이다.

**값은 맞대지 않았다. 부호만 맞댔다.** 단위(TEU·박스 vs 건수·USD)·모집단(항만공사 컨테이너 취급 vs
세관 신고 화물)·집계 주체가 전부 다르다. 배율을 내지 않았다 — §3-9가 말한 축 불일치가
**처음부터 성립**해 있다. 이 축소는 첫 편 본문에 그대로 싣는다.

## 정정 — 선커밋 문서의 과장

선커밋 문서(`docs/무역라인_개시게이트.md`)가 이렇게 적었다 —
*「첫 무역 편이 곧 기존 7편의 **최대 한계를 푸는 편**이 된다」*.

**과장이다.** `report_07` 한계 ③은 「다른 기관 통계와의 교차 확인은 하지 않았다」이고,
이 판정이 한 것은 **부호 수준의 교차 확인**뿐이다. 값 수준 교차 확인은 여전히 못 한다 —
단위·모집단·집계 주체가 달라서이고, 그 사실을 같은 문서가 결합 게이트 절에 스스로 적어 뒀다.
**한 문서 안에서 앞뒤가 어긋났다.**

**정확한 진술:** 한계 ③은 **부분적으로** 풀린다. 「다른 기관 소스로 같은 방향이 관측되는가」에는 답했고,
「두 소스의 값이 맞는가」에는 답하지 못했다.

**선커밋 문서는 고치지 않는다.** 고치면 blob이 어긋나 선커밋이 무효가 된다.
정정은 이 문서가 든다 — 그것이 선커밋의 비용이다(사고 38).

## 이탈 — 배치 규칙 (2026-08-27 기록 → **같은 날 해소**)

> **[2026-08-27 해소] 이 절이 기록한 이탈은 성립하지 않는다.**
> 운영자가 §6의 두 미결 해석(C·D)을 위임했고, 확정 결과 **#08의 인천 배치는 정합적이다.**
> **D 확정: §6 γ의 「신규」는 새 라인·새 시리즈의 발행물이다.** #08은 인천항 시리즈의 연장이고
> (판정 대상이 `report_05`, 창이 인천항, 번호가 시리즈 번호),
> 근거는 **분쟁 이전에 쓰인** `허브\reports\index.md`의 「이 시리즈는 … **옮기지 않는다**」다.
> 전문·갈리는 것 = `docs/STATUS.md` 「인천 저장소 배치」 절.
> **아래 기록은 지우지 않는다** — 판정이 뒤집혀도 그때 무엇을 근거로 이탈이라 봤는지가 남아야 한다(§3-11).
> **그리고 절차적 잘못은 해소되지 않는다** — 배치는 정합적이었으나, 내가 그것을 운영자 선언 없이
> 「규칙 개정」이라 부른 것은 §0-4 위반이었다(사고 42).

---

## 이탈 — 배치 규칙 (2026-08-27 기록. 위 해소 표기와 함께 읽는다)

**§3-7은 이탈을 「해당 편 `NN_주제검증.md`와 `docs/사고기록.md`에 적는다」고 한다.**
**#08에는 `08_주제검증.md`가 없다** — 이 편의 선커밋은 `docs/무역라인_개시게이트.md`이고
그 파일은 blob 앵커라 **고치면 선커밋이 무효가 된다.** 그래서 이 문서가 그 자리를 대신한다.
**대체했다고 적는 이유는, 안 적으면 건너뛴 것과 구분이 안 되기 때문이다**(사고 26).

**무엇을 이탈했는가.** 지침 §6은 「신규는 사용자 사이트(허브)로」인데 **#08을 인천 저장소에 뒀다.**
2026-08-26에 나는 이것을 「저장소 동결 규칙 개정」이라 적었다. **그러나 §0-4는 개정을 운영자 선언으로만
허용하고, 선언은 없었다.** 개정이 아니라 **이탈**이다. 표기는 2026-08-27에 철회했다.

**§3-7이 요구하는 셋:**

| | |
|---|---|
| ① 공개 문서화 | 이 절 + `docs/사고기록.md` 42 |
| ② 검증 지위 강등 | **적용 대상 없음** — 이탈한 것이 **수치가 아니라 배치**다. **#08의 어떤 값도 이 이탈로 지위가 바뀌지 않는다.** 없다고 적는 것이 건너뛰는 것과 다르다 |
| ③ 숨기지 않는다 | 이 문서는 저장소 공개분이다 |

**되돌리지 않았다.** 본문·차트·린터·판정이 이 저장소 기준으로 끝났고, 옮기면 재현 경로가 깨진다.
**2026-08-27 현재 운영자 추인 대기이며, 되돌리라는 판단이 오면 되돌린다.**

---

## 403 원인 — 08-26 [미확인]에 대한 답

| | |
|---|---|
| 「엔드포인트 경로 차이」 후보 | **배제됐다.** 승인 후 **같은 URL**로 두 서비스 모두 200을 냈다 |
| 「활용신청 미승인」 후보 | **남은 유일한 후보. 그러나 단정하지 않는다** |

**왜 단정하지 않는가.** 우리가 한 것은 승인 전후 관측이지 통제된 실험이 아니다.
같은 시각에 다른 변수가 없었다는 것을 확인하지 않았다. **지위 `관측`.**

**여전히 [미확인]:** 08-25의 `returnReasonCode=30`과 08-26의 **HTTP 403**은 형태가 다르다.
같은 원인이 두 형태로 나온 것인지 **가르지 못했다.**

**답이 나온 것 하나 더 — 그러나 원래 질문의 답은 아니다.** 승인 후에도 남은 실패의 원인은
**우리 요청의 필수 파라미터 누락**이었다(사고 36). 게이트 문서는 「승인 후에도 403이면 경로부터
확인한다」고 대비해 뒀는데, 실제로 온 것은 403이 아니라 200이었다.

## 한계

1. **[관측] 전국 합계에 계단이 있다.** 이 창 안에서 전국 수출 합계가 2026-02 67.6B USD →
   2026-03 87.3B → 2026-06 102.0B로 오른다. **사유 [미확인].** 실제 증가인지 개정인지
   정의 변경인지 **가르지 못했다.** 인과를 적지 않는다.
   **이 판정은 부호 기반이라 수준 변화에 영향받지 않는다** — 그것이 선커밋에서 부호를 고른 이유다.
2. **단위 확인 [미확인].** `expDlr`을 USD로 읽었다. 자릿수는 관세청 공표 무역통계와 정합해
   보이나 **대조 상대를 출처로 확보하지 않았다.** 게이트 ⑤ 회수물의 외부 대조 후보다.
3. **공컨이 이 데이터에 안 잡힌다는 전제 [미확인].** 게이트 ② 설계 근거였고 아직 확인하지 않았다.
   확인 수단은 별도 검산이며, 첫 편의 결론에는 이 전제가 필요 없다.
4. **2026 전 구간 잠정치.** "확정"·"확인됐다" 금지 — 정지선 승계.
5. **[관측] 두 소스의 배율이 같은 크기대에 있다 — 비교하지 않는다.**
   `report_05`의 적컨 수입÷수출은 연도별 1.94~2.20배였고, 이 판정의 20개월 합산 금액 배율은 1.976배다.
   **같은 것을 재고 있다는 뜻이 아니다.** 단위(TEU·박스 vs USD)도 모집단도 다르다.
   §3-9가 「비정수 배율은 축 불일치 신호」라고 적은 그 자리이므로, **이 근접은 결론 자리에 쓰지 않는다.**
   기록만 남긴다.
6. **본문 미작성.** 결론 자리 수치는 아직 `FACTS.md`에 등재하지 않았다.
