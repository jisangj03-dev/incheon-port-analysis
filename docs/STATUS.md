# STATUS

**갱신: 2026-09-06** (16차 · **인천 Pages 자체 레이아웃 · 측심 마감 2차** · 측심은 공개 상태 그대로)

---

## 착수점 — 이 절만 읽어도 일을 시작할 수 있다

**1) 검사를 한 줄로 친다.**

```
python analysis/boot_check.py
```

`--selftest` 를 가진 스크립트를 **찾아서** 전부 돌리고 상태 검사까지 한 판정으로 낸다.
**목록을 손으로 안 들고 찾는다** — 들면 낡는다(그 낡음이 실제로 있었다: `CLAUDE.md` 가
여덟 줄을 들고 있는 동안 장치는 27개가 됐다). **실패가 있으면 거기부터다.**
**시간이 고르지 않다** — 사고 84(미해결 절이 실측을 든다). 시간 초과는 **한 번 다시 친다**.
**두 번 다 넘으면 「모름」이고 통과가 아니다.** 「느린 순」 세 줄이 같이 나온다.

**2) 지금 축은 「공개」다** [2026-08-31 운영자 위임]. 방향 둘 — ①웹사이트를 빠르게 공개
②공개된 것을 링크드인 등 외부 채널에 게시. **그래서 새 편을 안 연다.**
**바로 착수할 것은 「다음 할 일 · B」의 B-1 이고, 그것은 운영자 손이다.**
A 는 이쪽이 지금 할 수 있는 것, C 는 아직 안 본 축이다.

**3) 마지막 세션이 어디서 멈췄나 (2026-09-06 · 「애플처럼 세련되게」 라운드).**
**측심은 공개돼 있다 — `https://sounding.higgsfield.app`**(2026-09-03 · 운영자 결정으로 세션이 push·배포·게시 · §2.4 · 공개 표면 실물 검사 전부 통과 · 경위 = 작업기록).
**이번에 한 것 둘.** ① **인천 Pages 에 자체 레이아웃** — `_layouts/` · `_includes/` · `assets/css/vidimus.css` · OG 카드. 발행본 아홉 편이 GitHub 기본 테마
(헤더 = 저장소 이름)로 나가고 있었다(실물). 발행본 마크다운은 안 건드렸다. **push 뒤 래퍼가 바뀌는 것이 의도다** — 새 지문 = `docs/렌더_기준선.md` 2026-09-06 절.
② 측심 마감 2차 — 바 밑 흰 번짐(무대 하단 페이드 · 지나간 챕터 컨트롤 숨김) · 지표 카운트업 · 252 선 드로잉 · 데이터 절 정렬 · 푸터 서체. **배포는 운영자 몫**(B-9 ④⑤).
**배포본 전수 검사 통과**(로컬 워커 런타임 · 클릭 QA FAIL 0 · 뷰포트 5종 0 · 폼 접수 · axe 0 · 인쇄 · 마감 13). 인천 새 레이아웃도 axe 0(흐르는 표 키보드 접근을 스크립트로 넣음).
**남은 것 — 인천 push(B-1)**: 권한 계층이 막아 운영자 손. 표기 행은 push 전제(`98be47e` · 검수 먼저면 revert). 그 전까지 측심의 외부 링크 여덟이 404 다.

**4) push 전에 실물을 보려면 한 줄이다.**
`python analysis/render_preview.py --serve` → `http://127.0.0.1:8800/` (휴대폰까지 `--lan`).
**블로킹 명령이라 운영자 터미널에서 띄운다** — 세션 안에서 `!` 로 치면 시간 초과에 죽는다.
어디서나 열리는 검수 시트는 아래 「검수 시트」 절이 든다.

**5) 이 프로젝트가 지금 가장 자주 밟는 함정 셋** — 새 작업을 시작하기 전에 한 번 본다.
- **「못 한다」가 「안 봤다」인 것.** 사고 58·63·76 이 전부 그것이었고 76 에서
  **원천의 77%를 안 보고 있었다**는 것이 드러났다. 한계를 적기 전에 **한 번 더 친다.**
- **분모가 0인 통과.** 「N건 발견」 옆에 「M건 검사」가 없으면 0이 통과처럼 보인다(사고 77).
- **조심으로 안 되는 것.** 같은 실수를 두 번 하면 고치지 말고 **경로를 없앤다**(사고 75·81).

---

> 현재 상태 · 다음 할 일 · 미해결만 담는다.
> 규범은 지침(`본부\프로젝트지침.md` — **버전은 그 파일 첫 줄이 든다**)에, 절차는 `.claude/skills/발행/SKILL.md`에,
> 사고는 `docs/사고기록.md`에, 수치는 `docs/FACTS.md`에 있다. **여기에 복제하지 않는다.**
>
> **예외는 아래 「정지선 ↔ 기전」 표 하나다.** 지침 §4는 조항 **문면**을 들고,
> 이 표는 **그 문면을 실제로 집행하는 코드의 명제와 그 한계**를 든다. **둘은 같지 않다**(사고 46).
>
> **여기 남는 것은 지금 참인 상태뿐이다.** 라운드 경위는 `docs/작업기록.md` 로 옮긴다
> (지우지 않는다). `analysis/boot_check.py` 가 이 파일의 크기를 재고 **상한을 넘으면 멈춘다.**
> 이 규율이 왜 생겼는지의 경위도 `docs/작업기록.md` 에 있다.

<!-- 정지선표:시작 (생성됨. 손으로 고치지 마라 — analysis/stopline_table.py) -->

**정지선 ↔ 기전 대조표.** 지침 §4의 조항 문면과 **그것을 실제로 집행하는 코드의 명제**를
나란히 놓는다. **문면과 기전은 같지 않다** — 다르면 다르다고 적는 것이 이 표의 목적이다(사고 46).

> **이 블록은 생성된다. 손으로 고치지 마라.** 정본은 각 가드 파일의 `정지선-집행/명제/한계` 3줄이다.
> `python analysis/stopline_table.py --check` (부트블록 3번) · 갱신은 `--write`.

| 집행 파일 | §4 조항 (문면) | **이 코드가 집행하는 명제** | **닿지 않는 곳** |
|---|---|---|---|
| `git_pre_push.sh` | §4 — push는 운영자만 | `CLAUDECODE`가 있고 우회 변수가 없으면 **경로와 무관하게** push를 거부한다 | `.git/hooks/`는 **커밋되지 않는다**(새 clone엔 없음) · **`CLAUDECODE`는 지울 수 있다** · 세션 안 `!` 접두도 같이 막혀 `VIDIMUS_PUSH_OK=1`이 필요하다. **내 실수를 막는 난간이지 보안 경계가 아니다** |
| `hook_stopline.py` | §4 — push는 운영자만 / 커밋 전 앵커 검사 | Claude Code 도구 경로로 들어온 **명령 문자열** 중 push를 거부하고, commit 앞에서 앵커 검사를 돌린다 | **문자열만 본다** — `subprocess.run(['git','push'])` 같은 간접 호출은 못 막는다(실측). 2층이 그것을 받는다 |
| `lint_publish.py` | §4 — 데이터 지위(강등 구간·잠정치·박스 수 기준·`_99`·PASS) / §3 결론 자리 | 발행본·지면·채널 문안의 결론 자리에서 지위 위반·인과 서술·FACTS 미등재를 잡는다 | **발행본은 골격을 못 찾으면 `[구조]` WARN만 낸다**(사고 26) · **지면은 표·그림 안을 안 본다** — 강등 값을 표에만 넣고 산문에서 안 부르면 지나간다 |
| `safe_edit.py` | §4 — 문서 치환은 safe_edit로만. 일치 개수 ≠ 1이면 안 쓴다 | 이 도구를 거친 치환은 일치 1건일 때만 파일을 바꾸고 전후 SHA-256을 남긴다 | **이 도구를 안 거치는 편집은 못 막는다.** Write·Edit·리다이렉션은 그대로 통한다 |
| `verify_anchors.py` | §4 — 앵커 3건 수정·정렬·재저장 금지 / 봉인 2건 열람 금지 | 앵커 3건의 SHA-256이 기록값과 같고, 이 도구가 봉인 2건의 내용을 안 읽는다 | 다른 도구·사람이 앵커를 고치는 것은 못 막는다. 커밋 시점에 **사후로** 알려 줄 뿐이다 |

**정지선 중 코드가 집행하지 않는 것은 이 표에 없다** — 데이터 지위·공개 경계·문서 규율 일부가 그렇다.
**없다는 것이 곧 「사람이 지킨다」는 뜻이고, 그것도 사실이므로 적어 둔다.**

<!-- 정지선표:끝 -->

## 현재 상태

- **[2026-09-02] 지침이 v6.2 다** — 운영자 결정 둘. ①**실명 금지**(§2.2-4 · `check_private.py`)
  ②**AI 생성 표기 폐지**(구 1·2·3항 → 표기 블록 3항목). 폐지 목록 = 지침 §11.
**프로젝트 방향이 전면 개편됐다.** 무역·유통으로 넓히지 않는다.
근거·조항 전문 = **지침** (§1.3 축 · §5 발행 · §6.1 첫 화면 · §10-9).
이 선언에서 나온 것 넷 — ① 축 축소 ② 매뉴얼 감축 ③ 다수 채널 게시 ④ 인천항 웹사이트.

- **지침 v6.0 확정**[2026-08-28]. 47,794 B → 35,999 B (−24.7%). v5.1 원문은 `본부\아카이브\`.
  **상한도 같이 내렸다** — `check_guideline_size.py` `LIMIT` 48,000 → **38,000**.
  안 내리면 줄인 12 KB가 그대로 다시 채울 여유가 된다.
  현재 값은 `python analysis/check_guideline_size.py`로 읽는다(사고 31).
- **§0.1 신설 — 「지침은 지켜야 할 것만 든다. 어떻게 하는가는 안 든다.」**
  **판단 우선 원칙:** 조항이 상황에 안 맞으면 따르지 말고 그 판단을 보고한다.
  **예외는 §4 정지선과 §2.2 표기 의무뿐이다.**
- **§6.1이 반전됐다.** 「산업이 아니라 축을 내건다」 → **「인천항을 내건다」.**
  그 조항의 전제(「두 번째 산업이 붙는다」)가 선언으로 사라졌다.
- **브랜드 이름은 안 바뀐다.** `Vidimus · 비디무스`는 **방법의 이름**이다. 대상은 태그라인이 진다.

- **[2026-09-02] 인천에 404 지면을 만들었다** (`404.md`). 그전에는 없는 주소가
  GitHub 기본 「Page not found」로 떨어졌다(실측) — 발행본 9편이 전부 거기 있으므로
  옛 링크·오타가 닿는 자리다. **빌드 결과는 push 해야 안다**(사고 20).

### 저장소 둘 — 역할이 갈렸다

| | 무엇 | 상태 |
|---|---|---|
| `incheon-port-analysis` (이 저장소) | 발행본 원문 · 원시 데이터 · 재현 코드 | **영구 동결 = 존치.** rename·폐기 금지. 인천항 시리즈 **연장편도 여기** |
| `jisangj03-dev.github.io` (허브) | **인천항 사이트.** 첫 화면 · `/reports/` · `/data/` · `/verify/` · `/about/` | **한 번도 push된 적 없다.** `origin/main` 없음 |

- 공개 저장소: `github.com/jisangj03-dev/incheon-port-analysis` (Pages 렌더 정상).
  **발행된 사이트는 커밋 `409fa2d` 기준이다** — 그 뒤는 전부 미push.
- 보고서 **#01~#07 발행 완료. #08은 본문·린터까지 완료, 검수·push 대기.**
- 저작 구조 **B — AI가 생성하고 사람이 운영한다.** 표기 의무(§2.2)가 성립 조건.

### 검사·생성 장치 — 33종(모드 3). `--selftest`가 있는 것은 매 세션 발화 확인
> **이 수는 아래 목록의 이름 개수**이고, `boot_check` 의 「`--selftest` N개」와 **다른 수다**
> (그쪽은 세는 쪽 둘을 포함하고 `--check` 만 가진 장치는 뺀다). **같게 만들려 하지 마라.**

> **이 수와 목록은 `check_counts.py` 가 맞댄다**(pre-push). 손으로 세지 마라 —
> 두 번 어긋났고 두 번째에 장치가 됐다. 경위 = `docs/작업기록.md` · 사고 31·75·80·87.

`lint_publish.py`(+`--channel`) · `safe_edit.py` · `verify_anchors.py` · `hook_stopline.py` ·
`install_git_hooks.py --check` · `stopline_table.py --check` · `check_guideline_size.py` ·
`check_status_fresh.py` · `check_review_log.py` · `check_browser.py` · `review_pick.py` ·
`check_links.py` · `render_preview.py` · `collect_terminal_monthly.py` ·
`build_terminals_page.py --check` · `collect_port_facilities.py` ·
`build_berths_page.py --check` · `check_generated.py` · `facts_worklist.py` ·
`check_facts.py` · `build_datasets_page.py --check` ·
`check_a11y.py` · `htmltable.py` · `build_skyline.py --check` ·
**`collect_09_backfill.py`** · **`chart_09_backfill.py`** · **`check_counts.py`** ·
**`basis_10_monthly_floor.py`** · **`judge_10_monthly.py`** · **`build_series_chart.py`** ·
**`check_jekyll.py`** · **`check_private.py`** · **`check_site_facts.py`**

- **셈 검사** `check_counts.py` — **산문에 적힌 수가 실제와 맞는가**(사고 80·81).
  짝 **다섯** — 장치 수 · **장치 목록 ↔ 실물** · 첫 화면 편수 · 허브 목록 행수 ·
  **사고 번호의 연속과 중복.** `pre-push` **다섯 번째 난간**(경고). 경위 = `docs/작업기록.md`.
  **다섯째 짝(2026-08-31)이 순환을 끊는다** — 넷째까지는 **STATUS 의 수와 STATUS 의 목록**을
  맞대 27 → 29 가 된 라운드에 **통과**를 냈다(사고 87). 이제 `analysis/` 직하를 훑어
  **「있는데 안 적힌 것」**을 찾는다. **`analysis/probe/` 는 안 본다** — 이 목록은
  「`--selftest` 를 가진 것」이 아니라 **「상시 장치」**다. `boot_check` 는 프로브도 **돌린다** —
  **안 도는 것이 아니라 이 목록의 대상이 아니다.**
  **닿지 않는 곳:** 등록한 짝 다섯만 본다. 산문의 모든 수를 세면 오탐만 는다 —
  **새로 「N개」를 적을 때 짝을 같이 넣는 것이 규율이다.** 그리고 **수가 맞다고
  문장이 맞는 것은 아니다.** 소스 문자열 가드는 여전히 **문자열을 본다** —
  「이 이름을 코드가 안 쓴다」를 실물로 확인할 길이 그것뿐이다.
- **스카이라인 생성** `build_skyline.py` — 첫 화면 장식. 인수시험이 **장식임을 강제한다**
  (`aria-hidden` · 글자 0 · 색 리터럴 0 · 배를 안 그림).
  **닿지 않는 곳:** 무엇으로 보이는지는 안 본다(사고 73). 띄워 봐야 검사한 것이다.

- **[2026-08-30 신설] 접근성 게이트** `check_a11y.py` — 표 이름 · `th scope` ·
  `svg` role/이름 · `img alt` · 건너뛰기 링크와 그 목적지 · 글자 없는 링크.
  `pre-push` **네 번째 난간**(경고). 경위 = `docs/작업기록.md`.
  **닿지 않는 곳:** **대비를 계산하지 않는다**(전 지면·두 테마 미달 0건은 실측해 토큰에 반영) ·
  **마크다운 표는 안 본다** ·
  **속성이 붙었다는 것은 낭독기가 읽어 준다는 확인이 아니다.** 낭독기로 들어 본 사람은 없다.
- **표 후처리** `htmltable.py` — 생성기 셋의 표 서른 개에 이름과 머리 방향을 채운다.
  **닿지 않는 곳:** 마크다운 표는 못 본다 · **속성만 넣지 낭독을 확인하지 않는다.**

- **[2026-08-29 신설] 데이터 카탈로그 생성** `build_datasets_page.py` — 공개 CSV 를 읽어
  행수 · 열 스키마 · **열별 빈칸 비율** · 창을 계산해 허브 `/datasets/`를 만든다.
  **봉인 파일은 열지 않고** 이름과 크기만 든다. `check_generated.py` 가 신선도를 본다.
  **닿지 않는 곳:** 값이 **맞는지**는 안 본다 — 있고 비어 있음만 센다.
  「쓴 곳」은 파일명 문자열 검색이라 **간접 참조는 못 잡는다.**

- **[2026-08-29 신설] 대장 자체 검사** `check_facts.py` — 중복 · 안읽힘 · 칸수 · 지위 · 창.
  **린터는 대장을 믿고 본문을 판정하는데 대장 자체는 아무도 안 보고 있었다.**
  `pre-push` 세 번째 난간(경고). 경위(사고 66 · 내가 만든 결함 둘) = `docs/작업기록.md`.
  **닿지 않는 곳:** 창이 **맞는지**는 안 본다. 형식과 일관성만 본다.

- **[2026-08-29 신설] 생성물 신선도** `check_generated.py` — 생성되는 산출물 넷이
  **지금 CSV 와 어긋나는지** 본다. 「CSV 가 바뀌었나」가 아니라 **「지금 CSV 로 만들면
  다른 것이 나오나」**를 본다 — 생성기 코드만 고쳐도 갈라지기 때문이다.
  **`pre-push` 에 경고로 붙였다**(막지 않는다 · 차단은 `--strict`).
  **정지선을 건드렸으므로 발화를 직접 쳤다**(경위 = `docs/작업기록.md`).
  **[2026-09-02] 종료코드 1 을 「낡았다」로 읽고 있었다** — 생성기가 터져도 1 이다.
  역추적이 있으면 **「모른다」**로 낸다(사고 89). 그 전에 **거짓 「낡았다」를 한 번 냈다.**
  **닿지 않는 곳:** ~~허브만 push 하면 건너뛴다~~(2026-09-03 닫힘 · `find_guard`) ·
  **역추적 없이 `sys.exit(1)` 로 조용히 죽는 것은 여전히 낡음과 못 가른다.**

- **린터 지면 모드** — 허브 지면을 **표 안은 관측, 머리말과 표 밖 산문은 주장**으로 갈라 검사한다.
  **모드는 파일이 정한다**(front matter `layout:`). 경위 = `docs/작업기록.md` · 사고 69.
  **닿지 않는 곳:** **표·그림 안은 안 본다.** 강등 값을 표에만 넣고 산문에서 안 부르면 지나간다.
- **push 정지선 2층** — 문면·명제·한계는 위 「정지선 ↔ 기전」 표가 든다.
  관할 3곳(인천 · 허브 · **측심**) 설치 일치 확인 — [2026-09-03] 측심을 넣었다. 9/6 에 push 되는
  저장소인데 2층이 비어 있었다. **같은 날 난간 ③④⑤ 가 붙은 날부터 stderr 까지 버려 push 때
  말할 수 없었음이 드러났다**(사고 92) — 고쳤고, ②④⑤ 는 형제 저장소 push 때도 돈다(A-1 닫힘). 세 저장소에서 직접 쳤다.
- **검수 게이트** `check_review_log.py` — push 범위의 신규 `reports/*.md`가
  `docs/검수기록.md`에 있는지 본다. 경고가 기본, 차단은 `--strict`(승격은 운영자 판단).
  **닿지 않는 곳:** 수정(M) 보고서는 안 본다 · **기록의 내용이 맞는지는 안 본다** · 소급하지 않는다.
- **[2026-08-29 신설] 링크 게이트** `check_links.py` — 저장소 둘의 링크를 걷어
  **살아있음 / 날것 / push대기 / 죽음 / 미확인** 다섯으로 판정한다.
  `--selftest` · `--net`. **`push대기`가 이 도구의 실체다** — 로컬에 있고 `origin/main` 에
  없는 것을 근거를 대고 그렇게 부른다. 경위 = `docs/작업기록.md` · §3-5.
  **[2026-09-02] `--live` 신설 — 발행된 실물을 실제로 눌러 본다.** 나머지 모드는 전부
  **로컬 파일**을 읽는데, 발행 파이프라인이 소스를 바꿔서 내보내므로 **소스 판정이
  사이트에 대한 진술이 아니다**(사고 91). 주석 안의 `href` 는 안 센다 — 누를 수 없다.
  **닿지 않는 곳:** 앵커는 안 본다 · **200은 「그 자료가 그대로 있다」가 아니다** ·
  `push대기`는 경로만 보고 내용은 안 본다 · 공공기관 사이트의 봇 차단을 링크 죽음과 못 가른다 ·
  Liquid 해석은 부분집합이고 **못 푼 것은 푼 척하지 않고 따로 센다** ·
  **`--live` 는 링크된 것만 본다** — 아무 데서도 안 걸린 지면은 「없다」가 아니라 「못 본다」다 ·
  **허브는 발행 전이라 실물이 없다**(「아직 없다」로 낸다. 통과가 아니다).

- **[2026-09-01 신설] Jekyll 빌드 게이트** `check_jekyll.py` — **허브는 한 번도 빌드된 적이
  없고 로컬 빌드 수단이 없다.** 첫 push 가 곧 첫 빌드인데 **Pages 빌드는 조용히 죽고**
  허브는 옛 판이 없어 **사이트 전체가 404 로 남는다.** 빌드를 깨는 여섯을 본다 —
  `_config.yml`·front matter YAML · Liquid 짝 · **link·post_url 대상** · include · layout.
  **front matter 가 있는 파일과 `_layouts`·`_includes` 만** 훑는다(나머지엔 Jekyll 이
  Liquid 를 안 돌려 잡으면 전부 오탐이다 · §3-5).
  **닿지 않는 곳: 이것은 빌드가 아니다** — kramdown 출력·플러그인 버전·Pages 고유 제약은
  안 본다. 파서가 **PyYAML 이라 Psych 와 갈릴 수 있고, 없으면 「모름」**이다.
  허용 플러그인 목록은 **손에 들어 낡으므로** 목록 밖은 경고지 실패가 아니다.
  **[2026-09-02] `pre-push` 여섯 번째 난간**(경고). 그냥 붙이면 **하필 허브 push 때
  안 돈다** — 난간 ①~⑤ 는 `analysis/` 를 현재 저장소 안에서만 찾는데 허브엔 없다.
  `find_guard()` 가 형제 폴더에서 찾는다. **발화를 허브 cwd 에서 직접 쳤다.**

- **[2026-09-01 신설] 공개 유출 게이트** `check_private.py` — **[운영자 지시] 사이트에
  운영자 실명을 싣지 않는다.** 그날 공개 대상 11곳에 있었고 전부 지웠다. 지운 것은 그때
  상태일 뿐이라 장치로 막는다 — 표기 블록은 §2.6 에 따라 **베껴지면서 퍼진다.**
  실명 · 개인 이메일 · 박힌 인증키(실패) · 로컬 절대경로(경고)를 **추적 파일만** 훑는다 —
  **인천 · 허브 · 측심**([2026-09-03] 측심과 TS/TSX/SVG/`.env*` 를 넣었다. 그 전엔 **정작 나갈 저장소를 안 봤다**).
  실측 = `check_private.py`(셋 다 유출 0건).
  **닿지 않는 곳: 이미지 안의 글자와 git 이력을 못 본다.** `og.png` 는 사람이 눈으로 봤다.
  이력에 남은 것은 이력을 다시 써야 지워지고 **그건 운영자 판단**이다.
  **[2026-09-02] `pre-push` 일곱 번째 난간**(경고 · ⑥과 같은 방식으로 허브에서도 돈다).
  **이 난간만은 경고가 모자란다고 본다** — 다른 여섯이 경고인 이유는 「걸리는 것이
  push 뒤에도 고쳐진다」인데 **유출은 이력에 남아 안 고쳐진다.** 다만 **차단 승격은
  이 저장소가 운영자 판단으로 정해 둔 자리**라 기본값을 안 바꿨다(B-8).

- **[2026-09-03 신설] 사이트 상수 ↔ 대장** `check_site_facts.py` — 측심이 화면에 박은 수치(지표 4 · 부두 4 · 시계열 극값 ·
  판정 8 · 챕터 문안)가 `FACTS.md`·판정결과 문서와 같은가. 타일이 가리키는 **대장 행 번호**까지 맞댄다. `pre-push` **여덟 번째 난간**(경고 ·
  형제 저장소 push 때도 돈다). **닿지 않는 곳:** 값이 맞는지는 안 본다 — 대장과 같은지만.

### X-CHECK 2계층 — **가동 상태만 여기 든다**(역할과 규율은 `CLAUDE.md` · 지침 §8.1)

- **[2026-08-31 실측 · 확인됨] 챗 사본이 v5.1 이다.** 검수 회차에서 챗이 **v6.0 에 없는 조항**을
  정지선으로 들었고 8건 중 3건이 오판, 그중 둘이 이 낡음에서 나왔다.
  원인은 `본부\프로젝트운영블록.md` 가 자기 안에서 **「충돌하면 v5.1 이 이긴다」고 선언**하는 것.
  대체본 `본부\챗_운영블록.md` 는 **만들어져 있다**(2026-09-02 v6.1 반영).
  **남은 것은 운영자가 프로젝트 지식을 교체하는 일뿐이다** — 도구가 안 닿는 자리다.

## 검수 시트 — 운영자가 볼 수 있는 자리

**[2026-09-02] `https://claude.ai/code/artifact/dc236b19-9490-472e-9c05-b207708915be`** — 비공개 · 운영자 계정 소유.
맨 앞이 「당신이 정해야 하는 것 셋」이다. **낡는다** — 데이터나 결정 상태가 바뀌면 같이 낡고, 같은 파일 경로로
다시 발행하면 같은 URL 에 덮인다. 제작 경위와 갱신 = `docs/작업기록.md`.

## 다음 할 일

> **누가 하는가로 먼저 가른다.** 종전에는 1번이 운영자 전용이라 세션을 열자마자
> 막혔다. 막히면 그때부터 무엇을 할지 매번 다시 고르게 되고, 고르는 데 앞머리를 쓴다.
>
> **A는 이쪽이 지금 바로 할 수 있다. B는 운영자 손이 있어야 한다.**
> A 항목에는 **왜 그것이 다음인지**를 같이 적었다 — 다음 세션이 그 판단을 다시 안 하도록.

### A. 이쪽이 바로 할 수 있는 것

> **[2026-08-31 · 공개 위임] A 에 새 분석은 안 채운다.** 축이 공개로 바뀌었고
> **공개를 넘기는 동작이 전부 운영자 손(B)이다.** 여기에 새 편을 채우면
> 재고만 늘고 공개는 그만큼 멀어진다. **#10 판정은 끝났고, 새 편은 B-1 이
> 풀릴 때까지 안 연다**(§1.4 2층 · §10-1).
>
> **[2026-09-02] 그래도 A 가 빈 것은 아니다.** 공개 경로 자체를 튼튼하게 하는 일은
> A 이고 재고를 안 늘린다 — 이번 라운드의 난간 ⑥⑦ 이 그것이었고, **그 일을 하다가
> 사고 89·90 이 나왔다.** 그 계열의 다음이던 A-1 은 2026-09-03 에 닫혔다(사고 92 가 같이 나왔다).

**A-0. ~~측심 실물 검사~~ [2026-09-03 끝 · 공개 표면에서 전부 통과].** 인천 push 뒤 `check_links.py --live-url` 한 번 더(외부 링크 여덟). 명령은 런북 §Code(`check_links --live-url` · `--net` · `probe.mjs` · `form.mjs --expect-id`).

**A-2. `GInOut`×`ocCt` 조합이 언제 줄었는지 — 아직 안 본 축.**
2005~2009 는 8조합이 매월 오고 2021 은 6조합이다(연도별 행수 96 → 72).
**어느 조합이 언제 사라졌는지는 아직 안 봤다.** 연안(`ocCt=2`) 0 물음과 맞물릴 수 있다.
- 이것은 **월 단위 관측**이고 우리가 본 것은 연 단위 집계뿐이라 **선커밋 여지가 있다.**

**A-3. 마크다운 표 8개의 이름·`scope`.** `data/`·`reports/`·`verify/`·`corrections/`.
로컬 렌더러로는 **봤고 없었다.** kramdown 이 같은 것을 내는지 [미확인]이라 손대지 않았다.
**push 뒤 실물에서 한 번 보면 닫힌다** — 그러므로 B-1 뒤에 온다.

### B. 운영자 손이 있어야 하는 것

**B-1. #08·#09 검수 → 인천 push. 이것이 지금 유일한 병목이다.** [2026-09-03] 옛 허브 push 는 **안 한다** — 측심(B-9)이 대체했다.
**측심의 죽은 링크 7개가 이 push 하나로 산다.**
**막는 것은 검수 하나뿐이다** — `report_08`·`report_09` 의 표기 블록이 「기계가 무작위로
뽑은 수치 1건의 출처 대조 / 기록은 `docs/검수기록.md`」를 **명시**하는데 그 기록이 없다.
지금 push 하면 **표기가 일어나지 않은 검수를 증언한다**(§2.2-3).
**편당 10분 · 두 편 20분이 사이트 전체를 연다.**
**앞의 위험은 전부 쳤다** — 도구 발화 · 허브 원격 실재(첫 push 성립) · Jekyll 정적 사전점검 ·
전 장치 통과. 경위 = `docs/작업기록.md`.

**두 저장소 상태는 수를 적지 않고 명령으로 읽는다**(사고 31 — 적으면 커밋 하나마다 낡는다.
실제로 108 → 112 → 121 로 세 번 낡았다):
`git rev-parse --abbrev-ref --symbolic-full-name "@{u}"` · `git rev-list --count "@{u}..HEAD"`.
**인천은 upstream 이 있고 허브는 없다** — 「한 번도 push 안 됨」은 **허브에만** 참이다.

아래 넷은 한 동작이다(표본은 **날짜가 시드**라 그날 것을 대조한다).
```
python analysis/review_pick.py reports/report_08_관세청_인천항_수출입신고.md --log
python analysis/review_pick.py reports/report_09_공컨테이너_시계열연장.md --log
git push                                                  # 인천
python analysis/check_links.py --net                       # push대기 → 살아있음
```
**이 세션 안에서 `!` 로 push 하면 훅이 막는다**(`CLAUDECODE`). 운영자 터미널에서 친다.

**#09 검수에서 특히 볼 것 셋** — ① **V3·V4 가 FAIL 인 채로 실렸다**(기준 무수정)
② 적재일 표(2005~2017 이 전부 `2018-06-27`)가 §2.5 **본문**에 있다
③ 판정 불가 10개 연도의 값은 §2.3 표에 싣되 **결론에는 안 썼다.**

**push 직후 확인할 것** — **`python analysis/check_links.py --live`** (허브가
「아직 없다」에서 통과로 바뀌어야 한다 · 인천 push 만 하면 27건이 잠시 `push대기`다) ·
없는 주소 하나를 쳐서 **새 404 지면이 뜨는가**(빌드해야 안다) ·
첫 화면이 「인천항을 공공 1차 데이터로 확인한다」인가 ·
내비게이션에 죽은 링크가 없는가 · 인천 `report_07` 의 지문이 **새 기준선**과 같은가
(`docs/렌더_기준선.md` 2026-09-06 절 — 래퍼가 바뀌는 것이 의도다).

**B-2. Web Archive 외부 타임스탬프 — push 직후 첫 작업.**
실측: 우리 Pages·GitHub **둘 다 보존 이력 0건**이다.
```
curl -sS "https://archive.org/wayback/available?url=https%3A%2F%2Fjisangj03-dev.github.io%2Fincheon-port-analysis%2F"
```
**선커밋 앵커가 전부 우리가 통제하는 저장소 안에 있다** — 해시는 내용의 무결성을
증명하지만 **「그 시점에 존재했다」를 외부에 증명하지 않는다.**
공개 URL 이 있어야 저장이 걸리므로 B-1 이 선행이다.

**B-3. 인쇄 확인 — Ctrl+P 두 번.** 밝은 모드·어두운 모드 각각.
통과 조건은 하나 — **어느 모드로 열었든 종이 미리보기의 글자가 검은가.**
규칙은 고쳤으나 **종이로는 확인 못 했다**(도구가 인쇄 미디어를 못 띄운다).

**B-4. 공공데이터포털 API 활용신청 한 건.** 해수부 「항만시설 및 하역능력정보」 —
부두현황의 「계」 불일치(선석 142 대 138)의 **대조군**이 된다.
**우리 키는 살아 있고** 막힌 것은 이 API 에 대한 신청이다(사고 61).
`https://www.data.go.kr/data/3082243/openapi.do` · 재현 = `probe_13_facility_api.py`.

**B-5. 인천항 실사진을 쓸 것인가.** 지금은 안 쓰는 쪽으로 갔다 —
**어느 항구인지 모르는 사진은 [미확인]을 그림으로 거는 것**이기 때문이다.
**인천항인 것이 확인된 CC 사진이라면 이야기가 다르고**, 라이선스 확인·출처 표기·
**파일 내려받기**가 필요한데 셋째가 기기 조작이라 이쪽이 못 한다.

**B-6. Claude.ai 프로젝트 지식 교체.** 실측·원인·처분은 위 「X-CHECK 2계층」 절이 든다 — 대체본 `본부\챗_운영블록.md` 를 넣고
`본부\프로젝트운영블록.md` 를 뺀다. **`docs/NN_판정결과.md` 와 `docs/STATUS.md` 는 넣지 않는다**(§3-6 · 넣으면 챗이 늘 정답지를 든다).
원문 = `docs/작업기록.md`(2026-09-06 이관).

**B-7. 채널 게시** [2026-08-31 운영자 위임 ②]. 문안 = `본부\채널문안_인천항_20260828.md`, 순서는 그 파일 §5.
한 채널을 돌리고 실소요를 잰 뒤 다음을 연다(`pilot_timer.py`).
**[2026-08-31 갱신]** 게시물 **③(#09 · FAIL 을 그대로 실은 편)**을 새로 썼고 **②보다 앞에 뒀다** —
①「우리 기준으로 통과했다」·②「다른 소스로도 같다」보다 ③「**우리 기준이 우리 표본외에서 깨졌다**」가
이 브랜드가 파는 것에 가장 가깝다. GitHub README 의 「가장 최근 편은…」은 **편이 늘 때마다 낡아서**
성질 서술로 바꿨다(사고 31·83). `lint_publish.py --channel` **PASS**.
**전부 인천 push 이후다** — 그 전에는 게시물의 보고서 URL 이 404 다.

**B-8. 난간 ⑦(공개 유출)을 차단으로 올릴 것인가 — 한 단어다.**
지금은 경고다. 근거는 위 `check_private` 절. **판단 재료 셋:**
① 유출은 push 뒤에 안 고쳐진다(이력을 다시 써야 하고 그건 운영자 판단이다)
② 지금 실측은 인천·허브 **유출 0건**이고 걸리는 것은 로컬 절대경로 **경고 2건**뿐이라
**차단으로 올려도 지금은 아무것도 안 막는다** ③ 막히면 그때 막히는 것은 운영자의 손이다.
올리려면 `analysis/git_pre_push.sh` 의 난간 ⑦ 줄 `--hook` 뒤에 `--strict` 를 붙이고
`python analysis/install_git_hooks.py` 를 다시 돌린다(정본 SHA 가 바뀐다).

**B-9. 측심 출시 — 운영자 기한 2026-09-06, 그 뒤 여러 채널에 홍보.** 순서가 곧 체크리스트다.
**런북 = `..\sounding\tools\RUNBOOK.md`**(어디에 무엇이 있는지 · 명령 전부).
**⑤ 앞에 `higgsfield account status` 한 번**(09-03 에 403 한 번 · 원인 [미확인]). **[09-06 최종점검]** `push.py --check` 두 번 무응답 — 원격은 1초에 401 을 내므로 토큰 만료 의심([미확인] · 갱신 = `repo-access`) · 크레딧 0(09-03 은 21.5).
안 되면 `higgsfield auth login`. 측심에도 `pre-push` 가 깔렸다 — 운영자 터미널에서는 아무것도 안 막는다.
① B-1 ② ~~측심 push~~ · ~~배포~~ **[2026-09-03 끝 · 세션이 운영자 결정으로 실행]** ②′ ~~게시~~ **[끝 · 대시보드 「게시」로 공개됨]** ③ ~~실물 검사~~ **[끝]**
③ 배포 뒤 `python analysis/check_links.py --live` 와 측심 실물 클릭(폼은 `node tools/form.mjs <주소> --expect-id`) ④ 게시 — 문안 `본부\채널문안_측심_20260902.md`(린터 PASS).
```
cd ../sounding && python tools/push.py        # 다음 변경분부터 다시 운영자 몫. 배포는 그 뒤 higgsfield website deploy <id>
```
경위 = `docs/작업기록.md`.

### C. 아직 안 본 축 — 편을 열 때 여기서 고른다

**편을 열기 전에 답할 질문 3개를 커밋한다(§5). 질문 3개가 안 서면 그 편은 열지 않는다.**

- **~~연안 구간~~ — [2026-08-31] 2005~2021 이 닫혔다**(#10 §4-1 · 관측).
  **17개 연도 전부에 값이 있고** 계 **114,580.2 TEU**(외항 합의 1.217%), 마지막 비영 달 2021-12.
  기존 관측(2022~2023 에 값 · 2024 부터 0 · 마지막 비영 2023-04)과 이으면 **비영 구간은 2005~2023**.
  `GInOut_코드규명.md` §3 의 「연안 전 행 0」은 **2025년 창의 관측**이라 이 구간엔 안 맞는다(사고 22).
  **⚠ 선커밋 기준으로는 못 쓴다 — 데이터를 이미 봤다**(§3-7). **왜 0 이 되는지는 이 데이터가 답 안 한다.**
- **터미널별 축.** 인천청 월별 항만운영통계에 터미널 단위 TEU 가 있다.
  **공/적·방향 축은 없어** 공컨 시리즈 연장은 안 된다.
- **NLIC 항만별 물동량.** 외항/내항이 갈리나 **톤·전체 화물**이라 우리 TEU·컨테이너와
  맞대지 않는다(§3-9).
- **2005~2017 소급 적재의 출처.** 이 API 밖에서 확인해야 한다.

## 미해결

- **[2026-08-29 4차 갱신] 부두현황의 자료 기준일 — 없다는 것을 확인했다.**
  원문 지면 두 곳을 실측으로 훑었다: **최종수정·갱신일·기준일·작성일 표시가 0건**이다.
  「안 봤다」가 아니라 **「봤고 없다」**로 남는다. 우리가 아는 것은 수집일뿐이고
  지면도 그렇게만 적는다. **닫으려면 다른 소스거나 기관 문의다.**
- **[2026-08-29 4차 갱신] 두 번째 시설 소스 — 막힌 지점을 특정했다.**
  해수부 「항만시설 및 하역능력정보」 API(`apis.data.go.kr/1192000/FaclManpFclty2`)가
  선석별 하역능력을 낸다 — 우리 값의 대조군이 된다. **쳐 봤더니 「등록되지 않은 서비스키」**다.
  **우리 키 자체는 산다**(대조군 API 에서 인증 통과 확인 · 사고 61).
  공공데이터포털은 **API 마다 활용신청이 따로**이고 그것은 계정 조작이라 **운영자 몫**이다.
  신청: `https://www.data.go.kr/data/3082243/openapi.do` · 재현 = `probe_13_facility_api.py`.
  **다만 이 소스도 기준일은 안 준다**(문서 기준) — 위 항목은 이 경로로 안 닫힌다.
- **[2026-08-29 3차] 공표 부두현황의 부분합과 「계」가 안 맞는다** — 선석 142 대 138.
  **맞추지 않았고 맞출 근거도 없다.** 위 API 가 열리면 대조군이 생긴다.
  기관 문의가 정공이나 **외부로 나가는 행위라 운영자 판단이다.**
- **[2026-08-29] `og.png` 재촬영은 아직 사람이 한다.** 카드 표는 CSV 에서 생성되지만
  이미지는 `build_terminals_page.py` 가 「다시 찍어라」를 출력할 뿐이다. **출력을 안 읽으면
  카드가 조용히 낡는다** — 그리고 **이미지는 린터가 못 본다.** 절차 = 허브 `_og/README.md`.
  촬영 뒤에는 **전후 SHA-256을 대조한다**(사고 57 — 크기 확인은 옛 파일을 통과시킨다).
- **[2026-08-29 4차 신규 · 미확인] 인쇄 결과를 종이로 확인하지 못했다.**
  `@media print`가 **색 토큰을 안 바꿔** 다크 모드 인쇄에서 흰 종이에 회색 글자가 나오고 있었다.
  토큰 21개를 재정의해 고쳤다(사고 68). **그러나 규칙을 읽어서 고친 것이고 결과를 본 것이 아니다** —
  이 도구로 인쇄 미디어를 못 띄운다. **운영자가 한 번 인쇄 미리보기를 열면 닫힌다**
  (브라우저에서 Ctrl+P · 밝은 모드와 어두운 모드 각각). 통과 조건은 하나다 —
  **어느 모드에서 열었든 종이 미리보기의 글자가 검은가.**
  **「고쳤다」와 「고쳐진 것을 봤다」는 다르다**(사고 26).
- **[2026-08-30 신규] 마크다운 표 8개에 이름도 `scope` 도 없다.**
  `data/`·`reports/`·`verify/`·`corrections/` 의 손 편집 표다.
  **로컬 렌더러로 그려 재 봤고 없었다** — 「안 봤다」가 아니라 「봤고 없다」다.
  다만 그것은 우리 렌더러의 결과이고 **kramdown 이 같은 것을 내는지는 [미확인]** 이다.
  **고치지 않았다** — 고치는 수단(`{: }` IAL)이 kramdown 에서 도는지 확인할 수 없고,
  안 되면 그 글자가 지면에 그대로 보인다. **확인 못 한 화면에 얹지 않는다**(사고 8·20).
  **push 뒤 실물에서 한 번 보면 닫힌다.**
- **[2026-08-30 6차 신규] 인천항 실사진 — 운영자 판단 대상.**
  위키미디어 공용 등에 **인천항/인천신항 CC 사진**이 있을 수 있다. 쓰려면
  ①라이선스 확인 ②출처 표기 ③**파일 내려받기**가 필요한데 셋째가 기기 조작이라 못 한다.
  **지금은 안 쓰는 쪽으로 갔다** — 어느 항구인지 모르는 사진은 [미확인]을 그림으로 거는 것이다.
  **인천항인 것이 확인된 사진이라면 이야기가 다르다.** 그 판단은 운영자 몫이다.
- **[2026-08-31 신규 · 미해결] STATUS 상한 처리가 3회차 연속 같은 동작이다 — 「처리했다」로 닫지 않는다.**
  실측: 41,395 → 40,148(이관) → 41,536 → **42,404(초과)** → 41,612 → 41,902 → …
  **지금 값은 여기 안 적는다 — `boot_check` 가 낸다.** 적으면 적는 순간 낡는다(사고 31).
  [2026-09-01] **이 한 라운드에 상한을 세 번 넘겼다** — 장치 하나(`check_jekyll`)를 등재하는 데
  **일곱 블록을 이관하고 중복 하나를 지우고도** 그렇다. **이관은 매번 성공하는데 여유는 계속 준다.**
  **처분이 매번 수동이라 라운드마다 앞머리를 쓴다.** 자동 이관은 축이 공개라 아직 안 만들었다.
  상한(`boot_check.py` `STATUS_LIMIT = 42000`)은 제 일을 하고 있다 — 문제는 **처분이 매번 수동**이고
  **매번 「처리했다」로 닫혀 추세가 안 보였다**는 것이다. 챗 검수가 그 추세를 짚었다.
  **처분 후보:** 끝난 항목(취소선·「[끝남]」)을 `docs/작업기록.md` 로 **자동 이관**하거나,
  장치 절의 **경위**를 상시 상태에서 분리한다. **안 만든 이유는 축이 공개이기 때문이다.**
- **[2026-09-01 갱신] 사고 84 가 **한 세션에 세 번** 났다 — 「멈춤/정상」이 아니라 기울기다.**
  같은 세션 열 번 돌려 **세 번 초과**(>600초). **정상값 14.6~35.2초 · 재시도값 17.1~135.1초.**
  **새로 말하는 것 둘:** ① 재시도가 **135.1초**였던 적이 있다 — 스크립트가 한 지점에 걸리는
  것이 아니라 **기계 전체가 느려졌다가 돌아온다.** ② 그런데 다른 재시도는 **17.1초**였다 —
  **재시도 시간마저 고르지 않다.** 환경 쪽이 유력하나 **무엇이 느리게 하는지는 [미확인]**이다.
  **[2026-09-03] 로그를 붙였다** — `analysis/_boot_log.tsv`(추적 안 함). 스크립트마다 「시작」·「끝」 줄을 따로 적으므로
  **다음 초과 때 마지막 「시작」 줄이 선 자리다.** 이 세션 실측: 26 · 140 · >600 · 37 · >600 · 32 · >600초(경위 = `docs/작업기록.md`).
  **로그가 답을 냈다** — 정지 둘 다 디스크를 가장 많이 건드리는 인수시험 도중 시작해 그 뒤 **모든** 자식이 60초를 넘겼다(기계 전체의 정지).
  정지 중 `OneDrive.exe` 가 CPU 1위. **운영자 손:** 측심 `app/node_modules`·`dist` 를 동기화에서 빼거나 작업 중 멈춘다(상세 = 작업기록).
  [같은 날 처분] 정지 셋이 전부 훅 인수시험 도중 시작해 그 시험이 난간을 안 돌리게 했다(12→0.6초). 효과는 다음 로그가 말한다.
  (사고 밀도로는 신호가 안 난다: 08-29 **11건** · 08-30 **11건** · 08-31 **6건**.)
- **[2026-08-30 신규 · 미확인] 낭독기로 들어 본 적이 없다.**
  표에 이름이 붙었고 `scope` 가 붙었다 — 그러나 **속성의 존재는 낭독의 확인이 아니다.**
  사고 26의 얼굴이므로 그렇게 적어 둔다. 검사기도 같은 문장을 자기 한계로 든다.
- **`07_판정결과.md` §4 ⓐ의 "확정 집계" 표기** — 잠정치 고지와 어긋나나 선커밋 문안 승계라 손대지 않았다.
- **2025년 전체 방향별 승격 보류** — 공식 공개 또는 기관 확인 시에만 해제.
- **연안항 구간** — 해석 미착수. **[2026-08-28 갱신]** 0이 2026만의 일이 아니다 —
  **2024년부터 전 구간 0이고, 2022~2023에는 값이 있었다**(마지막 비영 달 2023-04).
  「집계 공백」 후보는 약해졌으나 **분류·집계 변경 가능성이 남아 확정 아니다.**
  **이 관측은 선커밋 기준으로 못 쓴다**(데이터를 이미 봤다). 경위 = `analysis/probe/probe_10_coastal_zero.py`.
- **티스토리 69편 동결 사유 [미확인].** 동기 부재는 후보에서 제외됐다(운영자 정정).
  남은 후보: 제작 비용 · 소재 고갈 · 유통 부담. **축을 하나로 좁힌 지금 소재 고갈이 가장 큰 잔여 위험**이고,
  §5 편 게이트(질문 3개)가 그것을 겨냥한다. **원인을 모르는 채 건 대비책이다.**
- **v1.1·v1.0 본체와 구 마스터 v6.x 본체는 미확인.** v1.2만 확보했다.
- **[2026-08-28] 브라우저 연결은 세션마다 갈린다.** 같은 날 한 세션에서는 `check_browser.py`가
  「전제통과」인데 `tabs_context_mcp`가 **미연결**을 냈고, 다음 세션에서는 **붙었다.**
  **전제 검사와 연결 확인은 다른 것이고 둘 다 쳐야 갈린다** — 그 스크립트가 경고한 그대로다.
  **붙었다고 가정하고 계획을 세우지 않는다.** 원인은 도구가 안 닿는 자리라 단정하지 않는다.
