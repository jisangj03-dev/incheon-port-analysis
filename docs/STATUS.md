# STATUS

**갱신: 2026-09-11** (40차 · **링크드인 미리보기 — 선언과 실물이 갈려 있었다.** `og:image:width/height` 는 1200×630 인데 `og_image_url` 이 **마켓플레이스 커버(1600×1073)** 를 가리켰다 — `app-meta.json` 한 줄을 옳은 소셜 카드(`sounding_og.jpg` · 정확히 1200×630)로 돌렸다. **크롤러 차단·마크업·속도는 전부 정상이었다**(robots `Allow: /` · og 태그 전부 SSR · 0.58s). **장치로 막았다** — `check_site_facts` 7번(선언 ↔ 실물 화소 · 망을 안 탄다 · 깨진 상태로 발화 확인). **미리보기 실패의 원인이라고 단정하지 않는다** — 링크드인 안은 로그인 없이 못 본다 · 경위 = `docs/작업기록.md` 「40차」)

---

## 착수점 — 이 절만 읽어도 일을 시작할 수 있다

**1) 검사를 한 줄로 친다.**

```
python analysis/boot_check.py
```

`--selftest` 를 가진 스크립트를 **찾아서** 전부 돌리고 상태 검사까지 한 판정으로 낸다.
**목록을 손으로 안 들고 찾는다** — 들면 낡는다(그 낡음이 실제로 있었다: `CLAUDE.md` 가
여덟 줄을 들고 있는 동안 장치는 27개가 됐다). **실패가 있으면 거기부터다.**
**시간이 고르지 않다** — 사고 84(미해결 절이 실측을 든다). 시간 초과는 **한 번 다시 친다**.
**두 번 다 넘으면 「모름」이고 통과가 아니다.** 「느린 순」 세 줄이 같이 나온다.

**2) 지금 축은 「공개」다** [2026-08-31 운영자 위임]. 방향 둘 — ①웹사이트를 빠르게 공개
②공개된 것을 링크드인 등 외부 채널에 게시. **그래서 새 편을 안 연다.**
**①은 2026-09-09 에 끝났다(인천·측심 둘 다 공개). 남은 것은 ② B-7 게시(운영자 손)와 A-3.**
A 는 이쪽이 지금 할 수 있는 것, C 는 아직 안 본 축이다.

**3) 마지막 세션이 어디서 멈췄나 (2026-09-10 37차 · 「mail-01 이 나갔다」 · 경위 = 작업기록 「36차·37차」 절).**
**첫 기고 문의가 나갔다** — 코리아쉬핑가제트. 전문 1,214자 · 정본 해시 `43952af7aee00a40` · 회수 불가 채널이라 주소가 없다(**게시 지면이 없는 채널**이라 대기열도 이제 주소를 안 요구한다).
**표기 문단 한 곳을 고쳤다** — 원안이 「해석은 제가 정했습니다」였는데 **지침 §2.1 은 「H1 부제·한 줄 결론·§1 요약·§2 본문·§3 해석」을 AI 생성으로 명시한다.** §2.2-4 는 판단으로 못 바꾼다. 근거 = 본부 `_mail-01_근거.md` 「표기 문단」 절.
**회신이 오면 §6 부터 본다** — 그 매체의 기고 전속 조건이 공표돼 있지 않고(실측), **이 문의가 그것을 닫는 수단이다.** 수신처·접수 경로 = 본부 `_mail-01_수신처.md`(공개 저장소에 안 적는다).
**B-13 이 닫혔다** — 운영자가 토큰을 갱신했고 측심 push 가 풀렸다. 대기 중이던 `e368fbc` 와 판정 지면 수정 `e7cdf66` 이 함께 올라갔다. **배포가 착지했다** — `status` = `deployed`. **밖에서 확인했다**: 라이브에 새 문장 1건 · 옛 문장 **0건** · 지면의 §3 링크가 **200**. *배포 명령 자체는 「no response received」를 냈으나 `status` 는 `queued` 를 보여 줬다 — **CLI 응답과 작업 등록은 다른 것이다.** 그 자리에서 멈췄으면 다 된 일을 안 된 것으로 적었다.* 크레딧은 배포 전 **0**(RUNBOOK 의 21.5 는 낡았다) — 배포는 0 을 쓴다.
**남은 채널 셋** — li-03 · li-04(다음 주 · 「한 주에 하나씩」) · gn-01(되돌릴 수 없다 · 전문 승인 먼저).

**그 앞 세션(26차)의 채널 상태는 그대로다.** **li-01·li-02 가 링크드인에 나갔다.** li-01 은 운영자가 파이프라인 밖에서(09-09), li-02 는 이 파이프라인으로(09-10 · 카드 = #09 편 · `?src=li`).
**그 과정에서 사고 둘.** ①**문안을 base64 로 옮겨 적다 한 글자를 틀려 「그런」이 「그럵」으로 게시됐다** — 그런데 대조는 「불일치 0」을 냈다. **사본을 사본과 댔기 때문이다**(사고 109). 처분: `--clip`(전사 경로 삭제) · `--verify`(파일과 해시 대조) · `check_hangul`(깨진 음절 의심). ②**「마지막 URL 이 카드가 된다」를 실측이라 적었는데 li-01 이 반증하고 있었다**(사고 110) → **09-10 운영자가 조작 순서를 주자 한 기전으로 닫혔다: 카드는 마지막으로 「입력」된 URL 에 붙는다.** 한 번에 붙여넣는 우리 경로에서는 마지막 URL 이다.
**게시본은 파일과 일치한다**(URL 제외 651자 · 해시 `c430c96f…` 양쪽 · 운영자가 링크드인에서 고친 뒤 확인).

**채널 발행은 파이프라인이다**(25차). 폰에서 읽는 한 줄 = `python analysis/channel_queue.py` ·
대기열 정본 `docs/채널대기열.md` · 문안 `본부\채널문안\` · 규칙 `python analysis/channels.py --list`.
**지금 게시됨 2 · 준비 4(li-03 · li-04 · gn-01 · **mail-01** 기고 문의 수치) · 보류 1(디스콰이엇 — 프로덕트 등록이 먼저다).**
**남은 것은 사람 손 하나씩** — 로그인 · 승인 · 게시 버튼. 측심에 **채널별 유입 집계**가 붙어 있다
(`channel_views` · 버킷 이름과 수만 · `?src=` 갈래는 실측 · **referrer 갈래는 [미확인]**).

**지침이 v7.0 이다**(24차). 규범은 셋 — **데이터 방법**(§3) · **되돌릴 수 없는 것**(§4 가) · **사람 손**(같은 §4).
**그 밖의 절차·순서·도구·분량은 전부 세션 판단이고, 개정도 세션이 한다**(§0-4 · 운영자 선언은 **§1.3 축** 하나).
**22차~25차의 실측 결과표는 작업기록이 든다.**
**권한 구조 — 지금 참인 것 [2026-09-09 · 지침 v6.4 + v7.0].** push·배포·커밋·**지침 개정**은 세션 몫이다. **훅은 검증이다** — `hook_stopline.py` 는 커밋 앞 앵커 검사(실패하면 그 커밋만 막는다), `pre-push` 는 난간 여덟 중 **일곱은 경고 · ⑦ 공개 유출만 차단**(`VIDIMUS_PUSH_OK` 통로 삭제 · 세 저장소 재설치 · 정본 SHA `6BA60E57…`). **deny 는 되돌릴 수 없는 것만** — `.claude/settings.json` `permissions.deny`(힘 push · filter-branch · `repo-access`)와 자동 모드 `hard_deny`. **사용자 설정 `autoMode` 는 세션이 못 고친다**(분류기가 자기 권한 설정 수정을 막는다 · 사고 104) — 바꿀 일이 생기면 교체본과 명령을 내고 **운영자가 자기 터미널에서** 친다. 토큰 갱신도 운영자만(사고 101 · 재발급 권고 그대로). 전모 = `본부\구성_실측_20260909.md`.
**다음 세션 첫 동작 — 한 줄이다.**
`python analysis/channel_queue.py` → 무엇이 대기 중인지. 운영자가 「올려」 하면 거기부터다.
그 뒤 할 것 = **A-3 실물 확인** · 실측치를 `docs/렌더_기준선.md` 에 적기 · 첫 게시 뒤 `channel_inflow.py`.
STATUS 를 고치기 전에 `python analysis/status_archive.py --check` 로 여유를 본다.

**4) push 전에 실물을 보려면 한 줄이다.**
`python analysis/render_preview.py --site . --serve --lan` → `http://127.0.0.1:8800/`. **`--site .` 를 빼면 옛 허브가 뜬다**(09-07 실측).
블로킹 명령이라 운영자 터미널에서 띄운다(세션 안에서는 배경 실행 · 메모리 부족이면 죽는다 · 09-07 실측). 크롬 확장은 창 크기 변경·세 장 이상 배치에서 굳는다 —
체계적 확인은 헤드리스 `analysis/site_qa/`(측심 `tools/node_modules` 의 playwright 를 쓴다). 검수 시트는 아래 「검수 시트」 절이 든다.

**5) 이 프로젝트가 지금 가장 자주 밟는 함정 셋** — 새 작업을 시작하기 전에 한 번 본다.
- **「못 한다」가 「안 봤다」인 것.** 사고 58·63·76 이 전부 그것이었고 76 에서
  **원천의 77%를 안 보고 있었다**는 것이 드러났다. 한계를 적기 전에 **한 번 더 친다.**
- **분모가 0인 통과.** 「N건 발견」 옆에 「M건 검사」가 없으면 0이 통과처럼 보인다(사고 77).
- **조심으로 안 되는 것.** 같은 실수를 두 번 하면 고치지 말고 **경로를 없앤다**(사고 75·81).

---

> 현재 상태 · 다음 할 일 · 미해결만 담는다.
> 규범은 지침(`본부\프로젝트지침.md` — **버전은 그 파일 첫 줄이 든다**)에, 절차는 `.claude/skills/발행/SKILL.md`에,
> 사고는 `docs/사고기록.md`에, 수치는 `docs/FACTS.md`에 있다. **여기에 복제하지 않는다.**
>
> **예외는 아래 「정지선 ↔ 기전」 표 하나다.** 지침 §4는 조항 **문면**을 들고,
> 이 표는 **그 문면을 실제로 집행하는 코드의 명제와 그 한계**를 든다. **둘은 같지 않다**(사고 46).
>
> **여기 남는 것은 지금 참인 상태뿐이다.** 라운드 경위는 `docs/작업기록.md` 로 옮긴다
> (지우지 않는다). `analysis/boot_check.py` 가 이 파일의 크기를 재고 **상한을 넘으면 멈춘다.**
> 이 규율이 왜 생겼는지의 경위도 `docs/작업기록.md` 에 있다.

<!-- 정지선표:시작 (생성됨. 손으로 고치지 마라 — analysis/stopline_table.py) -->

**정지선 ↔ 기전 대조표.** 지침 §4의 조항 문면과 **그것을 실제로 집행하는 코드의 명제**를
나란히 놓는다. **문면과 기전은 같지 않다** — 다르면 다르다고 적는 것이 이 표의 목적이다(사고 46).

> **이 블록은 생성된다. 손으로 고치지 마라.** 정본은 각 가드 파일의 `정지선-집행/명제/한계` 3줄이다.
> `python analysis/stopline_table.py --check` (부트블록 3번) · 갱신은 `--write`.

| 집행 파일 | §4 조항 (문면) | **이 코드가 집행하는 명제** | **닿지 않는 곳** |
|---|---|---|---|
| `git_pre_push.sh` | §4 — push 전 검증 여덟. 일곱은 경고 · ⑦ 공개 유출만 차단 | push 순간에 검수 게이트·생성물 신선도·대장·접근성·셈·Jekyll·공개 유출·사이트 상수 검사기를 돌리고 stderr 로 말한다 · **⑦ 이 실명·개인 이메일·인증키를 찾으면 그 push 를 막는다** | `.git/hooks/`는 **커밋되지 않는다**(새 clone엔 없음) · **나머지 일곱은 경고라 읽지 않으면 지나간다** · ⑦ 도 **이미지 안의 글자와 git 이력은 못 본다** · push 자체를 판정하지 않는다(금지 분기 삭제) |
| `hook_stopline.py` | §4 — 커밋 전 앵커 검사 (push 는 v6.4 로 세션이 친다 · 이 훅은 막지 않는다) | Claude Code 도구 경로로 들어온 **명령 문자열** 중 commit 앞에서 앵커 검사를 돌리고, 실패하면 그 커밋을 막는다 | **문자열만 본다** — `subprocess.run(['git','commit'])` 같은 간접 호출은 못 본다 · **push 는 검사 대상이 아니다**(금지 분기 삭제 · 2026-09-09). 힘 push·토큰 갱신은 `permissions.deny`·`hard_deny` 가 든다 |
| `lint_publish.py` | §4 — 데이터 지위(강등 구간·잠정치·박스 수 기준·`_99`·PASS) / §3 결론 자리 | 발행본·지면·채널 문안의 결론 자리에서 지위 위반·인과 서술·FACTS 미등재를 잡는다 | **발행본은 골격을 못 찾으면 `[구조]` WARN만 낸다**(사고 26) · **지면은 표·그림 안을 안 본다** — 강등 값을 표에만 넣고 산문에서 안 부르면 지나간다 |
| `verify_anchors.py` | §4 — 앵커 3건 수정·정렬·재저장 금지 / 봉인 2건 열람 금지 | 앵커 3건의 SHA-256이 기록값과 같고, 이 도구가 봉인 2건의 내용을 안 읽는다 | 다른 도구·사람이 앵커를 고치는 것은 못 막는다. 커밋 시점에 **사후로** 알려 줄 뿐이다 |

**정지선 중 코드가 집행하지 않는 것은 이 표에 없다** — 데이터 지위·공개 경계·문서 규율 일부가 그렇다.
**없다는 것이 곧 「사람이 지킨다」는 뜻이고, 그것도 사실이므로 적어 둔다.**

<!-- 정지선표:끝 -->

## 현재 상태

- **[2026-09-09] 지침이 v7.0 이다 — 규제 해제판.** 승인 게이트·형식 규정(분량 상한)·도구 지정·사문 조항·판단에 안 쓰이는 이력을 걷었다(37,601 → 31,591 B). **폐지 목록 절(§11)도 같이 내려갔다** — 무엇을 왜 버렸는지는 `docs/작업기록.md` 와 `본부\아카이브\프로젝트지침_v6.4_원문_20260909.md` 가 든다.
  **문면이 바뀐 자리 둘은 실측 정정이다** — ①**§1.2 북극성** 셋째 절의 「사람이 **검수**·발행하는」(검수기록은 9편 중 1건이었다) ②**§6** 의 「`jisangj03-dev.github.io` = 인천항 사이트」(그 저장소는 push 된 적이 없다).
  **실명 금지는 그대로다** — 항 번호만 §2.2-4 → **§2.2-3**(구 1항 폐지분 재번호). 집행 = `check_private.py`.
**프로젝트 방향이 전면 개편됐다.** 무역·유통으로 넓히지 않는다.
근거·조항 전문 = **지침** (§1.3 축 · §5 발행 · §6.1 첫 화면).
이 선언에서 나온 것 넷 — ① 축 축소 ② 매뉴얼 감축 ③ 다수 채널 게시 ④ 인천항 웹사이트.

- **지침 감축 이력** — v6.0(47.8 → 36.0 KB) · v7.0(37.6 → 31.6 KB). 경위 = `docs/작업기록.md`.
  **분량 상한과 그 감시기(`check_guideline_size.py`)는 v7.0 에서 없앴다** — 상한은 형식이라 조항의 질을 못 보고, 지침이 선에 닿았다는 이유로 개시 검사가 세션을 멈춰 세웠다. 대신 §0.1 이 「새 조항이 절차를 담기 시작하면 코드·SKILL 로 내린다」를 든다.
- §0.1(판단 우선) · §6.1(인천항을 내건다) 은 지침과 `CLAUDE.md` 가 든다 — 여기 복제하지 않는다(옛 문안 = 작업기록 2026-09-06 이관).
- **[2026-09-07 운영자 선언 · 지침 v6.3] 브랜드 = 측심(Sounding) 하나.** `Vidimus · 비디무스` 폐기 — 인천 저장소의 제호·기호·OG·파비콘·소개·인용 파일이 측심으로 바뀌었다(발행본 본문 불변). 경위 = 작업기록.

### 저장소 둘 — 역할이 갈렸다

| | 무엇 | 상태 |
|---|---|---|
| `incheon-port-analysis` (이 저장소) | 발행본 원문 · 원시 데이터 · 재현 코드 | **영구 동결 = 존치.** rename·폐기 금지. 인천항 시리즈 **연장편도 여기** |
| `jisangj03-dev.github.io` (허브) | **인천항 사이트.** 첫 화면 · `/reports/` · `/data/` · `/verify/` · `/about/` | **한 번도 push된 적 없다.** `origin/main` 없음 |

- 공개 저장소: `github.com/jisangj03-dev/incheon-port-analysis` (Pages 렌더 정상).
  **발행된 사이트는 2026-09-09 push 기준이다**(`763d2df` 이후 · 미push 수는 `git rev-list --count "@{u}..HEAD"` 로 읽는다).
- 보고서 **#01~#09 발행 완료.** #08·#09 는 검수(통독·대조) 미수행을 표기 행이 적고 있다 — 검수하면 그 행을 갱신한다(`review_pick.py --log`).
- 저작 구조 **B — AI가 생성하고 사람이 운영한다.** 표기 의무(§2.2)가 성립 조건.

### 검사·생성 장치 — 45종(모드 3). `--selftest`가 있는 것은 매 세션 발화 확인
> **이 수는 아래 목록의 이름 개수**이고, `boot_check` 의 「`--selftest` N개」와 **다른 수다**
> (그쪽은 세는 쪽 둘을 포함하고 `--check` 만 가진 장치는 뺀다). **같게 만들려 하지 마라.**

> **이 수와 목록은 `check_counts.py` 가 맞댄다**(pre-push). 손으로 세지 마라 —
> 두 번 어긋났고 두 번째에 장치가 됐다. 경위 = `docs/작업기록.md` · 사고 31·75·80·87.

`lint_publish.py`(+`--channel`) · `safe_edit.py` · `verify_anchors.py` · `hook_stopline.py` ·
`install_git_hooks.py --check` · `stopline_table.py --check` ·
`check_status_fresh.py` · `check_review_log.py` · `check_browser.py` · `review_pick.py` ·
`check_links.py` · `render_preview.py` · `collect_terminal_monthly.py` ·
`build_terminals_page.py --check` · `collect_port_facilities.py` ·
`build_berths_page.py --check` · `check_generated.py` · `facts_worklist.py` ·
`check_facts.py` · `build_datasets_page.py --check` ·
`check_a11y.py` · `htmltable.py` · `build_skyline.py --check` ·
**`collect_09_backfill.py`** · **`chart_09_backfill.py`** · **`check_counts.py`** ·
**`basis_10_monthly_floor.py`** · **`judge_10_monthly.py`** · **`build_series_chart.py`** ·
**`check_jekyll.py`** · **`check_private.py`** · **`check_site_facts.py`** · **`session_snapshot.py`** · **`status_archive.py`** · **`check_refs.py`** ·
**`channels.py`** · **`channel_post.py`** · **`channel_queue.py`** · **`channel_inflow.py`** · **`check_claims.py`** · **`check_session_surface.py`** · **`check_marks.py`** · **`build_featured_card.py`** · **`judge_11_terminal.py`** · **`chart_11_terminal_plan.py`**


**각 장치가 무엇을 보고 무엇을 못 보는지는 `docs/장치.md` 가 든다** — 라운드마다 자라 STATUS 상한을 네 번 밀었다. **목록과 수는 여기, 설명은 그쪽이다.**

### X-CHECK 2계층 — **가동 상태만 여기 든다**(역할과 규율은 `CLAUDE.md` · 지침 §8.1)

- **[2026-08-31 실측 · 확인됨] 챗 사본이 v5.1 이다.** 검수 회차에서 챗이 **v6.0 에 없는 조항**을
  정지선으로 들었고 8건 중 3건이 오판, 그중 둘이 이 낡음에서 나왔다.
  원인은 `본부\프로젝트운영블록.md` 가 자기 안에서 **「충돌하면 v5.1 이 이긴다」고 선언**하는 것.
  대체본 `본부\챗_운영블록.md` 는 **만들어져 있다**(2026-09-02 v6.1 반영).
  **남은 것은 운영자가 프로젝트 지식을 교체하는 일뿐이다** — 도구가 안 닿는 자리다.

## ~~검수 시트~~ — 낡았다 [2026-09-10]

`claude.ai/code/artifact/dc236b19-…`(비공개). **「당신이 정할 것 넷」 중 push·배포 둘이 v6.4 로 세션 몫이 됐고 시트는 다시 안 만들었다** — 스스로 「낡는다」고 적은 그대로다. **지금 운영자가 정할 것은 아래 B 절과 `docs/채널대기열.md` 가 든다.** 경위 = `docs/작업기록.md`.

## 다음 할 일

> **누가 하는가로 먼저 가른다.** 종전에는 1번이 운영자 전용이라 세션을 열자마자
> 막혔다. 막히면 그때부터 무엇을 할지 매번 다시 고르게 되고, 고르는 데 앞머리를 쓴다.
>
> **A는 이쪽이 지금 바로 할 수 있다. B는 운영자 손이 있어야 한다.**
> A 항목에는 **왜 그것이 다음인지**를 같이 적었다 — 다음 세션이 그 판단을 다시 안 하도록.

### A. 이쪽이 바로 할 수 있는 것

> **[2026-08-31 · 공개 위임] A 에 새 분석은 안 채운다.** 축이 공개로 바뀌었고
> **공개를 넘기는 동작이 전부 운영자 손(B)이다.** 여기에 새 편을 채우면
> 재고만 늘고 공개는 그만큼 멀어진다. **#10 판정은 끝났고, 새 편은 B-1 이
> 풀릴 때까지 안 연다**(§1.4 2층 · §10-1 편수 목표 금지).
>
> **[2026-09-02] 그래도 A 가 빈 것은 아니다** — 공개 경로 자체를 튼튼하게 하는 일은 A 이고 재고를 안 늘린다(경위 = 작업기록 2026-09-07 이관).

**끝난 것(이관됨):** A-2 · A-4 · A-5 · A-6 · A-7 (→ 작업기록 2026-09-08)

**A-8. G6 효용 첫 측정 — 다음 채널 라운드가 닫힐 때 친다.** 대상 셋: 포니테일 · 에이전트 스킬즈 · watermarks-remover.
기준 전문 = `docs/도구도입_선커밋_G6효용_20260910.md`(blob `51df8e1a` · **고치지 않는다**).
한 줄은 이것이다 — **「그 도구가 없었으면 우리 이력에 안 남았을 커밋이 한 라운드 안에 하나라도 있는가. 못 대면 처분한다.」**
**「다음 라운드에 보자」를 안 한다** — 그 유예가 이 라운드의 「보류 5」를 두 번 미뤘다.

> **[2026-09-10 34차 · 운영자 지적] 그 문면은 사후에 이야기로 만들기 쉽다.** 선커밋은 고정이라 못 고친다.
> **대신 적용 규칙 둘을 여기 둔다**(§0.1 — 절차는 지침이 아니라 이 자리다):
> ① **댄 커밋의 diff 안에 그 도구의 산출물이 실물로 있어야 한다.** 「그것을 쓰면서 생각했다」는 산출이 아니다.
> ② **「없었으면 무엇이 달랐나」를 반증 가능한 한 줄로 적는다.** 그 줄이 안 써지면 그 커밋은 못 댄다.

**A-3. 마크다운 표 8개의 이름·`scope`.** `data/`·`reports/`·`verify/`·`corrections/`.
로컬 렌더러로는 **봤고 없었다.** kramdown 이 같은 것을 내는지 [미확인]이라 손대지 않았다.
**push 뒤 실물에서 한 번 보면 닫힌다** — 그러므로 B-1 뒤에 온다.

### B. 운영자 손이 있어야 하는 것

**B-3. 인쇄 확인 — Ctrl+P 두 번.** 밝은 모드·어두운 모드 각각.
통과 조건은 하나 — **어느 모드로 열었든 종이 미리보기의 글자가 검은가.**
규칙은 고쳤으나 **종이로는 확인 못 했다**(도구가 인쇄 미디어를 못 띄운다).

**B-4. 공공데이터포털 API 활용신청 한 건.** 해수부 「항만시설 및 하역능력정보」 —
부두현황의 「계」 불일치(선석 142 대 138)의 **대조군**이 된다.
**우리 키는 살아 있고** 막힌 것은 이 API 에 대한 신청이다(사고 61).
`https://www.data.go.kr/data/3082243/openapi.do` · 재현 = `probe_13_facility_api.py`.

**B-5. 인천항 실사진을 쓸 것인가.** 지금은 안 쓰는 쪽으로 갔다 —
**어느 항구인지 모르는 사진은 [미확인]을 그림으로 거는 것**이기 때문이다.
**인천항인 것이 확인된 CC 사진이라면 이야기가 다르고**, 라이선스 확인·출처 표기·
**파일 내려받기**가 필요한데 셋째가 기기 조작이라 이쪽이 못 한다.

**B-6. Claude.ai 「물류」 프로젝트 지식.** [2026-09-09 갱신] 첫 파일 = `본부\구성_실측_20260909.md`(그 §5 가 올릴 목록을 든다). `본부\챗_운영블록.md` 는 챗의 역할 파일로 같이 넣는다.
`본부\프로젝트운영블록.md` 는 아카이브로 옮겼다(09-09 · 넣지 않는다). **`docs/NN_판정결과.md` 와 `docs/STATUS.md` 는 넣지 않는다**(§3-6 · 넣으면 챗이 늘 정답지를 든다).
원문 = `docs/작업기록.md`(2026-09-06 이관).

**끝난 것(이관됨):** B-11 · B-8 (→ 작업기록 2026-09-09) · **B-14 Agent-Reach** (34차에 세션이 판정하고 지웠다 → `docs/에이전트리치_판정_20260910.md`) · **B-12 ruflo 전역 지시문** (2026-09-10 운영자가 세션에 넘겨 지웠다 · 잔여 0) · **B-13 측심 push 토큰** (2026-09-10 운영자가 갱신 · `e368fbc`·`e7cdf66` push 완료)

**B-7. 채널 게시** [2026-08-31 운영자 위임 ② · **2026-09-09 파이프라인화**]. **대기열이 정본이다 — `docs/채널대기열.md`**(읽기 `python analysis/channel_queue.py`). 문안 본문 = `본부\채널문안\<id>.md` · 채널 규칙 = `python analysis/channels.py --list`.
**지금 `준비` 다섯**(링크드인 넷 · GeekNews 하나 · 전부 링크 200 · OG 카드 확인) · **보류 하나**(디스콰이엇 — 프로덕트 등록이 먼저다).
**사람이 할 일은 채널마다 하나씩이다** — 로그인 · 승인 · 게시 버튼. 그 밖은 세션이 한다.
종전 문안 정본 `본부\채널문안_측심_20260902.md` 는 **순서표와 경위**로 남는다(문안 자체는 대기열이 가리키는 파일이 든다).
한 채널을 돌리고 실소요를 잰 뒤 다음을 연다(`pilot_timer.py`).
**[2026-08-31 갱신]** 게시물 **③(#09 · FAIL 을 그대로 실은 편)**을 새로 썼고 **②보다 앞에 뒀다** —
①「우리 기준으로 통과했다」·②「다른 소스로도 같다」보다 ③「**우리 기준이 우리 표본외에서 깨졌다**」가
이 브랜드가 파는 것에 가장 가깝다. GitHub README 의 「가장 최근 편은…」은 **편이 늘 때마다 낡아서**
성질 서술로 바꿨다(사고 31·83). `lint_publish.py --channel` **PASS**.
**전부 인천 push 이후다** — 그 전에는 게시물의 보고서 URL 이 404 다.

**B-10. 「개발 참고」 문단(#01~#06 부록) 처분 — ①/②/③ 한 글자.** 사실 확인·대안 셋·②의 초안 = `docs/작업기록.md` 2026-09-07 「A-6」 절.
권고는 ① 그대로 둔다(참인 당시 기록 · §3-11). ②(수정 이력 한 줄)면 이쪽이 6편에 넣고 커밋한다.

### C. 아직 안 본 축 — 편을 열 때 여기서 고른다

**편을 열기 전에 답할 질문 3개를 커밋한다(§5). 질문 3개가 안 서면 그 편은 열지 않는다.**

- **끝난 것(이관됨):** 연안 구간 (→ 작업기록 2026-09-08) · B-1 · B-2 · B-9 (→ 작업기록 2026-09-09)
- **터미널별 축.** 인천청 월별 항만운영통계에 터미널 단위 TEU 가 있다.
  **공/적·방향 축은 없어** 공컨 시리즈 연장은 안 된다.
- **NLIC 항만별 물동량.** 외항/내항이 갈리나 **톤·전체 화물**이라 우리 TEU·컨테이너와
  맞대지 않는다(§3-9).
- **2005~2017 소급 적재의 출처.** 이 API 밖에서 확인해야 한다.

## 미해결

- **[2026-08-29 4차 갱신] 부두현황의 자료 기준일 — 없다는 것을 확인했다.**
  원문 지면 두 곳을 실측으로 훑었다: **최종수정·갱신일·기준일·작성일 표시가 0건**이다.
  「안 봤다」가 아니라 **「봤고 없다」**로 남는다. 우리가 아는 것은 수집일뿐이고
  지면도 그렇게만 적는다. **닫으려면 다른 소스거나 기관 문의다.**
- **[2026-08-29 4차 갱신] 두 번째 시설 소스 — 막힌 지점을 특정했다.**
  해수부 「항만시설 및 하역능력정보」 API(`apis.data.go.kr/1192000/FaclManpFclty2`)가
  선석별 하역능력을 낸다 — 우리 값의 대조군이 된다. **쳐 봤더니 「등록되지 않은 서비스키」**다.
  **우리 키 자체는 산다**(대조군 API 에서 인증 통과 확인 · 사고 61).
  공공데이터포털은 **API 마다 활용신청이 따로**이고 그것은 계정 조작이라 **운영자 몫**이다.
  신청: `https://www.data.go.kr/data/3082243/openapi.do` · 재현 = `probe_13_facility_api.py`.
  **다만 이 소스도 기준일은 안 준다**(문서 기준) — 위 항목은 이 경로로 안 닫힌다.
- **[2026-08-29 3차] 공표 부두현황의 부분합과 「계」가 안 맞는다** — 선석 142 대 138.
  **맞추지 않았고 맞출 근거도 없다.** 위 API 가 열리면 대조군이 생긴다.
  기관 문의가 정공이나 **외부로 나가는 행위라 운영자 판단이다.**
- **[2026-08-29] `og.png` 재촬영은 아직 사람이 한다.** 카드 표는 CSV 에서 생성되지만
  이미지는 `build_terminals_page.py` 가 「다시 찍어라」를 출력할 뿐이다. **출력을 안 읽으면
  카드가 조용히 낡는다** — 그리고 **이미지는 린터가 못 본다.** 절차 = 허브 `_og/README.md`.
  촬영 뒤에는 **전후 SHA-256을 대조한다**(사고 57 — 크기 확인은 옛 파일을 통과시킨다).
- **[2026-08-29 · 미확인 → 2026-09-07 에뮬레이션으로 확인] 인쇄.** 다크 모드 인쇄에서 흰 종이에 회색 글자가 나오던 것을 토큰 재정의로 고쳤고(사고 68),
  이번에 인쇄 미디어를 **헤드리스로 에뮬레이션**해 밝음·어둠 각각 종이 흰색·글자 검정·바/푸터/차례 숨김·리빌 표시를 쟀다(경위 = 작업기록 2026-09-07).
  **실제 인쇄 대화상자는 아니다** — 운영자가 Ctrl+P 로 한 번 보면 닫힌다(B-3). 「고쳤다」와 「고쳐진 것을 봤다」는 다르다(사고 26).
- **[2026-08-30] 마크다운 표에 이름도 `scope` 도 없다 → [2026-09-07] 인천 발행본 표는 레이아웃이 처리한다.**
  옛 허브(`data/`·`verify/`·`corrections/`)는 측심이 대체해 안 올린다. 인천 보고서의 표는 레이아웃 스크립트가 앞의 「표 N.」 제목을
  이름(`aria-labelledby`)으로 잇고 머리 칸에 `scope=col` 을 준다 — 발행본은 안 건드린다. kramdown 실물에서도 도는지는 push 뒤 확인(A-3).
  **속성이 붙었다는 것이 낭독 확인은 아니다.**
- **[2026-08-30 6차 신규] 인천항 실사진 — 운영자 판단 대상.**
  위키미디어 공용 등에 **인천항/인천신항 CC 사진**이 있을 수 있다. 쓰려면
  ①라이선스 확인 ②출처 표기 ③**파일 내려받기**가 필요한데 셋째가 기기 조작이라 못 한다.
  **지금은 안 쓰는 쪽으로 갔다** — 어느 항구인지 모르는 사진은 [미확인]을 그림으로 거는 것이다.
  **인천항인 것이 확인된 사진이라면 이야기가 다르다.** 그 판단은 운영자 몫이다.
- **[2026-08-31 · 미해결 → 2026-09-08 장치] STATUS 상한 처리가 라운드마다 같은 수동 동작이었다** — 일주일에 13번(`git log --oneline -- docs/STATUS.md | grep 상한`). `status_archive.py` 가 끝난 블록의 이관을 맡는다(위 장치 절). **남은 것은 유입이다** — 착수점 3)·B·미해결의 서술은 판단이라 장치가 못 줄인다. 여유는 `--check` 가 낸다. 경위·추세 = `docs/작업기록.md`(2026-09-07 이관 · 2026-09-08 「계속 일해」 절).
- **끝난 것(이관됨):** 사고 84 — 개시 검사가 한 세션에 세 번 멈췄다 (→ 작업기록 2026-09-10)
- **[2026-08-30 신규 · 미확인] 낭독기로 들어 본 적이 없다.**
  표에 이름이 붙었고 `scope` 가 붙었다 — 그러나 **속성의 존재는 낭독의 확인이 아니다.**
  사고 26의 얼굴이므로 그렇게 적어 둔다. 검사기도 같은 문장을 자기 한계로 든다.
- **`07_판정결과.md` §4 ⓐ의 "확정 집계" 표기** — 잠정치 고지와 어긋나나 선커밋 문안 승계라 손대지 않았다.
- **2025년 전체 방향별 승격 보류** — 공식 공개 또는 기관 확인 시에만 해제.
- **연안항 구간** — 해석 미착수. **[2026-08-28 갱신]** 0이 2026만의 일이 아니다 —
  **2024년부터 전 구간 0이고, 2022~2023에는 값이 있었다**(마지막 비영 달 2023-04).
  「집계 공백」 후보는 약해졌으나 **분류·집계 변경 가능성이 남아 확정 아니다.**
  **이 관측은 선커밋 기준으로 못 쓴다**(데이터를 이미 봤다). 경위 = `analysis/probe/probe_10_coastal_zero.py`.
- **티스토리 69편 동결 사유 [미확인].** 동기 부재는 후보에서 제외됐다(운영자 정정).
  남은 후보: 제작 비용 · 소재 고갈 · 유통 부담. **축을 하나로 좁힌 지금 소재 고갈이 가장 큰 잔여 위험**이고,
  §5 편 게이트(질문 3개)가 그것을 겨냥한다. **원인을 모르는 채 건 대비책이다.**
- **v1.1·v1.0 본체와 구 마스터 v6.x 본체는 미확인.** v1.2만 확보했다.
- **[2026-08-28] 브라우저 연결은 세션마다 갈린다.** 같은 날 한 세션에서는 `check_browser.py`가
  「전제통과」인데 `tabs_context_mcp`가 **미연결**을 냈고, 다음 세션에서는 **붙었다.**
  **전제 검사와 연결 확인은 다른 것이고 둘 다 쳐야 갈린다** — 그 스크립트가 경고한 그대로다.
  **붙었다고 가정하고 계획을 세우지 않는다.** 원인은 도구가 안 닿는 자리라 단정하지 않는다.
