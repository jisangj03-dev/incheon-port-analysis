# STATUS

**갱신: 2026-08-28**

> 현재 상태 · 다음 할 일 · 미해결만 담는다.
> 규범은 지침(`본부\프로젝트지침.md` v6.0)에, 절차는 `.claude/skills/발행/SKILL.md`에,
> 사고는 `docs/사고기록.md`에, 수치는 `docs/FACTS.md`에 있다. **여기에 복제하지 않는다.**
>
> **예외는 아래 「정지선 ↔ 기전」 표 하나다.** 지침 §4는 조항 **문면**을 들고,
> 이 표는 **그 문면을 실제로 집행하는 코드의 명제와 그 한계**를 든다. **둘은 같지 않다**(사고 46).
>
> **[2026-08-28] 이 파일도 v6.0 감축 라운드를 같이 받았다.** 종전 판은 41 KB였고,
> 그중 대부분이 **이미 끝난 일의 경위 서술**이었다. 경위는 `docs/사고기록.md`가 든다.
> 여기 남는 것은 **지금 참인 상태**뿐이다.

<!-- 정지선표:시작 (생성됨. 손으로 고치지 마라 — analysis/stopline_table.py) -->

**정지선 ↔ 기전 대조표.** 지침 §4의 조항 문면과 **그것을 실제로 집행하는 코드의 명제**를
나란히 놓는다. **문면과 기전은 같지 않다** — 다르면 다르다고 적는 것이 이 표의 목적이다(사고 46).

> **이 블록은 생성된다. 손으로 고치지 마라.** 정본은 각 가드 파일의 `정지선-집행/명제/한계` 3줄이다.
> `python analysis/stopline_table.py --check` (부트블록 3번) · 갱신은 `--write`.

| 집행 파일 | §4 조항 (문면) | **이 코드가 집행하는 명제** | **닿지 않는 곳** |
|---|---|---|---|
| `git_pre_push.sh` | §4 — push는 운영자만 | `CLAUDECODE`가 있고 우회 변수가 없으면 **경로와 무관하게** push를 거부한다 | `.git/hooks/`는 **커밋되지 않는다**(새 clone엔 없음) · **`CLAUDECODE`는 지울 수 있다** · 세션 안 `!` 접두도 같이 막혀 `VIDIMUS_PUSH_OK=1`이 필요하다. **내 실수를 막는 난간이지 보안 경계가 아니다** |
| `hook_stopline.py` | §4 — push는 운영자만 / 커밋 전 앵커 검사 | Claude Code 도구 경로로 들어온 **명령 문자열** 중 push를 거부하고, commit 앞에서 앵커 검사를 돌린다 | **문자열만 본다** — `subprocess.run(['git','push'])` 같은 간접 호출은 못 막는다(실측). 2층이 그것을 받는다 |
| `lint_publish.py` | §4 — 데이터 지위(강등 구간·잠정치·박스 수 기준·`_99`·PASS) / §3 결론 자리 | 발행본과 채널 문안의 결론 자리에서 지위 위반·인과 서술·FACTS 미등재를 잡는다 | **결론 자리를 못 찾으면 `[구조]` WARN만 낸다** — 절 번호가 다른 문서는 검사되지 않는다(사고 26) |
| `safe_edit.py` | §4 — 문서 치환은 safe_edit로만. 일치 개수 ≠ 1이면 안 쓴다 | 이 도구를 거친 치환은 일치 1건일 때만 파일을 바꾸고 전후 SHA-256을 남긴다 | **이 도구를 안 거치는 편집은 못 막는다.** Write·Edit·리다이렉션은 그대로 통한다 |
| `verify_anchors.py` | §4 — 앵커 3건 수정·정렬·재저장 금지 / 봉인 2건 열람 금지 | 앵커 3건의 SHA-256이 기록값과 같고, 이 도구가 봉인 2건의 내용을 안 읽는다 | 다른 도구·사람이 앵커를 고치는 것은 못 막는다. 커밋 시점에 **사후로** 알려 줄 뿐이다 |

**정지선 중 코드가 집행하지 않는 것은 이 표에 없다** — 데이터 지위·공개 경계·문서 규율 일부가 그렇다.
**없다는 것이 곧 「사람이 지킨다」는 뜻이고, 그것도 사실이므로 적어 둔다.**

<!-- 정지선표:끝 -->

## 현재 상태

### 축 — 인천항 하나 [2026-08-28 운영자 선언]

**프로젝트 방향이 전면 개편됐다.** 무역·유통으로 넓히지 않는다.
근거·조항 전문 = **지침 v6.0** (§1.3 축 · §5 발행 · §6.1 첫 화면 · §10-9).
이 선언에서 나온 것 넷 — ① 축 축소 ② 매뉴얼 감축 ③ 다수 채널 게시 ④ 인천항 웹사이트.

- **지침 v6.0 확정.** 47,794 B → **35,999 B (−24.7%)**. v5.1 원문은 `본부\아카이브\`.
  **상한도 같이 내렸다** — `check_guideline_size.py` `LIMIT` 48,000 → **38,000**.
  안 내리면 줄인 12 KB가 그대로 다시 채울 여유가 된다.
  현재 값은 `python analysis/check_guideline_size.py`로 읽는다(사고 31).
- **§0.1 신설 — 「지침은 지켜야 할 것만 든다. 어떻게 하는가는 안 든다.」**
  **판단 우선 원칙:** 조항이 상황에 안 맞으면 따르지 말고 그 판단을 보고한다.
  **예외는 §4 정지선과 §2.2 표기 의무뿐이다.**
- **§6.1이 반전됐다.** 「산업이 아니라 축을 내건다」 → **「인천항을 내건다」.**
  그 조항의 전제(「두 번째 산업이 붙는다」)가 선언으로 사라졌다.
- **브랜드 이름은 안 바뀐다.** `Vidimus · 비디무스`는 **방법의 이름**이다. 대상은 태그라인이 진다.

### 저장소 둘 — 역할이 갈렸다

| | 무엇 | 상태 |
|---|---|---|
| `incheon-port-analysis` (이 저장소) | 발행본 원문 · 원시 데이터 · 재현 코드 | **영구 동결 = 존치.** rename·폐기 금지. 인천항 시리즈 **연장편도 여기** |
| `jisangj03-dev.github.io` (허브) | **인천항 사이트.** 첫 화면 · `/reports/` · `/data/` · `/verify/` · `/about/` | **한 번도 push된 적 없다.** `origin/main` 없음 |

- 공개 저장소: `github.com/jisangj03-dev/incheon-port-analysis` (Pages 렌더 정상).
  **발행된 사이트는 커밋 `409fa2d` 기준이다** — 그 뒤는 전부 미push.
- 보고서 **#01~#07 발행 완료. #08은 본문·린터까지 완료, 검수·push 대기.**
- 저작 구조 **B — AI가 생성하고 사람이 운영한다.** 표기 의무(§2.2)가 성립 조건.

### 웹사이트 — 조사 결과를 접목했다 (2026-08-28 2차)

**허브 커밋 `1cbad12`.** 항만·유사 브랜드 조사(`본부\항만사이트조사_20260828.md`)에서 나온 것 넷.
**처음으로 화면을 보고 작업했다** — 위 렌더러 + 크롬.

- **`/data/` 소스 카드** — 소스마다 **창 · 갱신 · 관측 지연 · 형식**. 뉴스타파 DATA 카드 서식.
  **「확인 대기」 절 신설** — 인천청 월별 항만운영통계 · NLIC. 첨부를 안 열었으므로 **소스가 아니다.**
- **`/corrections/` 신설** — FAIL / 이탈 / 오류를 가르고, 잠정 구간과 지위 강등을 적고,
  **린터가 못 잡는 것**을 마지막 절에 적었다. ONS가 정정 정책을 최상위 메뉴로 두는 것을 따랐다.
- **`/verify/` 인용 블록** — OWID 방식. 저자를 둘로 적어 표기 의무가 인용 단계에서 안 사라지게 했다.
- **홈 수치 스트립** — KOSIS의 「지표명('창') ⋯ 값」 형식. **안 본 축 5를 관측한 축 7 옆에 나란히 싣는다.**
- 부수: `.prose` 자체의 max-width가 바로 아래 `.prose table{max-width:none}`을 **무력화하고 있었다**(실측). 고쳤다.

**린터 전 페이지 FAIL 0.** `/data/`의 「10개월」은 린터가 미등재로 잡았고 **우회하지 않고 FACTS에 등재**했다.

### 웹사이트 — 인천항 축으로 개편 완료 (2026-08-28 1차)

허브를 3라인 브랜드 사이트에서 **인천항 단일 사이트**로 바꿨다. 전 페이지 린터 PASS.

- **첫 화면** — 「인천항을 공공 1차 데이터로 확인한다」. 라인 A/B/C 블록을 **인천항 축 지도**로 교체.
- **`/data/` 신설 — 데이터 지도.** 소스 4종(실제로 열어 본 것만) · 관측한 축 7 · **아직 안 본 축 5**.
  **안 본 축을 적지 않으면 목록이 「전부 봤다」로 읽힌다.** 새 편은 여기서만 연다(§5).
- **`/checks/`·`/terms/` 삭제.** 이슈 검증·개념 라인이 v6.0에서 폐기됐다. 내비게이션도 같이 고쳤다.
- `_config.yml`·`about.md`·`reports/index.md`·`verify.md` 문안을 인천항으로 맞췄다.
- 인천 저장소의 `about.md`·`_config.yml`도 같이 맞추고, **낡은 주석 블록을 걷어냈다**
  (추인 A·C 미결을 전제한 서술이 남아 있었다 — 그 미결은 닫혔다).

**[2026-08-28] 로컬 렌더 수단이 생겼다 — `analysis/render_preview.py`.**
Ruby·Jekyll·markdown 라이브러리가 이 기계에 없어(실측) 허브는 한 번도 화면으로 확인된 적이 없었고,
그것이 사이트 작업 전체를 막고 있었다. 의존성 0의 미리보기 렌더러로 그 막힘을 풀었다.
```
python analysis/render_preview.py --selftest
python analysis/render_preview.py --out <경로>
python -m http.server 8765 --bind 127.0.0.1   # file:// 은 브라우저 도구가 못 연다
```
**닿지 않는 곳 — 이것은 Jekyll이 아니다.** 지원 범위는 이 저장소가 쓰는 Liquid·마크다운
부분집합뿐이고, **빌드 성패·플러그인·배포는 못 본다.** 그건 여전히 push해야 안다(사고 20).
**미리보기의 인공물을 실물의 결함으로 읽는 것이 이 도구의 고유 위험**이라 인수시험에 그 경로를 박았다.

**[미확인] 그래서 Jekyll 빌드 성패는 여전히 push 전에 알 수 없다.**

### 채널 — 문안 준비 완료, 링크 카드도 붙였다. 게시는 운영자

**[2026-08-28] 채널·항만사이트 조사를 돌렸다.** 정본 두 개 —
`본부\항만사이트조사_20260828.md`(항만 전문 사이트·유사 브랜드 71건 실측) ·
`본부\홍보채널조사_20260828.md`(링크드인 밖 채널). **여기에 결론을 복제하지 않는다.**
`본부\홍보전략.md`는 **v2로 교체**했다 — v1(2026-07-14)이 「공개 채널 1개 = 링크드인」이라
운영자 선언 ③과 정면으로 어긋나 있었다. 구판은 `본부\아카이브\홍보전략_v1_20260714.md`.

- **허브에 OG·트위터 카드 태그를 넣었다**(허브 커밋 `33b8157`). 종전에는 **한 줄도 없었다** —
  §6이 「채널은 링크만」이므로 **채널에 남는 것은 링크 카드가 전부인데 그것이 비어 있었다.**
  `<head>`만 고쳤다. 보이는 지면은 안 건드렸다(사고 8·20).
- **`og:image` 붙였다**(허브 `a7d05ce`). 헤드리스 크롬으로 1200×630 을 찍었다.
  원본·재생성 절차 = 허브 `_og/`. **2군 채널의 공통 선행 작업이 닫혔다.**
- **인천 발행본은 OG가 있으나** `og:image` 없음 · `og:locale`이 **`en_US`** ·
  `robots.txt`·`sitemap.xml`·`feed.xml` **404**(실측). **발행본은 형식 때문에 안 고친다**(§10-8) —
  `_config.yml`·레이아웃 층의 문제이므로 렌더 기준선 확인 뒤에 본다.

**정본 = `본부\채널문안_인천항_20260828.md`.** 린터 PASS(FAIL 0 · WARN 0).
구판은 `본부\아카이브\채널문안_20260826.md`.

- **§10-3(4순위 이하 개시 금지)이 해제됐다** — 운영자 선언 ③. 남는 제약은 **§7 비용 상한 1건 1시간**.
- **게시물 본문을 이번엔 미리 썼다.** 종전 판은 「파일럿 측정이 파괴된다」로 안 썼는데,
  **그 사유가 구조 B와 어긋난다** — 요약을 쓰는 것은 §2.1상 AI 몫이고, 파일럿이 그것을
  운영자 공정으로 재고 있었다. **존재하지 않는 공정을 재고 있었다.**
  `pilot_timer.py`의 `summary` 구간을 **「AI 문안 검수 + 수치 대조」**로 뜻을 바꿔 쓴다(도구 변경 없음).
  **이 재정의는 운영자 추인 대상이다.** 경위 = `docs/사고기록.md` **54**.
- 준비된 것: 링크드인 헤드라인 · 게시물 ①(#07) · 게시물 ②(#08) · GitHub 프로필 README ·
  채널 순위표(커리어리·디스콰이엇·X 개시 가능 / 뉴스레터·외부 블로그 원문 게재 금지).

### 정리 — 이번 라운드에서 걷어낸 것

- **`본부\` 27개 → 10개.** 브랜드 이름 라운드 7종 · 유사산업 조사 · 3라인 허브 설계 2종 ·
  종결된 링크드인 4주 관찰 2종 · 완료된 선커밋 이행 · 현황 진단 스냅샷 · 임시 파일 2종 → **아카이브**.
  **삭제가 아니라 이동이다** — 되돌릴 수 있고 감사추적이 남는다.
- **허브 `/checks/`·`/terms/`** 삭제(위).
- **`analysis/`는 안 건드렸다.** 참조 0건인 스크립트가 4종 있으나
  **§1.4 1층이 재현 코드 100% 공개를 요구하고, 프로브는 「그 값을 어떻게 찾았나」의 감사추적이다.**
  지우면 되찾을 수 없고 얻는 것은 정돈감뿐이다. **정리 대상은 코드가 아니라 절차 과잉이었다.**

### 검사 장치 — 9종. 전부 발화 확인됨

`lint_publish.py`(+`--channel`) · `safe_edit.py` · `verify_anchors.py` · `hook_stopline.py` ·
`install_git_hooks.py --check` · `stopline_table.py --check` · `check_guideline_size.py` ·
`check_status_fresh.py` · `check_review_log.py` · `check_browser.py` · `review_pick.py`

- **`--selftest`가 있는 것은 매 세션 돌린다**(`CLAUDE.md` 3번). 값은 실행해서 읽는다.
- **`check_guideline_size.py` 인수시험을 고쳤다** — 픽스처가 `40000 B`를 절대값으로 박고 있어
  상한을 내리자 **시험이 먼저 깨졌다.** `LIMIT` 상대값으로 바꿨다. 경위 = `docs/사고기록.md` **53**.
- **push 정지선 2층.** `hook_stopline.py`(도구 경로 · 문자열만 본다) + `.git/hooks/pre-push`
  (git 자신이 부르므로 경로 무관). 관할 2곳(인천 · 허브) 설치 일치 확인.
  **둘 다 실수를 막는 난간이지 보안 경계가 아니다.**
- **검수 게이트** `check_review_log.py` — push 범위의 신규 `reports/*.md`가
  `docs/검수기록.md`에 있는지 본다. 경고가 기본, 차단은 `--strict`(승격은 운영자 판단).
  **닿지 않는 곳:** 수정(M) 보고서는 안 본다 · **기록의 내용이 맞는지는 안 본다** · 소급하지 않는다.

### X-CHECK 2계층

- **역할은 지침 §8.1이 정한다 — ①사실 전달 ②X-CHECK ③위임문 작성. 그뿐이다. 챗은 설계하지 않는다.**
- **챗이 강한 자리는 사실이 아니라 자기모순이다.** 챗은 파일을 못 봐 사실에는 이쪽과 같이 틀렸고
  (사고 47), 강했던 것은 **이쪽이 쓴 것의 내적 모순**을 짚는 일이었다(사고 41·44·45·46·47).
  → **챗에는 파일이 아니라 산출물 본문을 준다.**
- **독립성은 지시로 성립한다.** 넘길 때 「판정결과 문서를 열지 말라」를 **명시**한다.
- **1계층을 안 치면 2계층도 소용없다.** 사고 47이 그 경로였다.
- **[미확인] Claude.ai 프로젝트 지식이 지금 무엇을 들고 있는지 이쪽은 못 본다**(사고 48).
  **v6.0으로 올라갔으므로 운영자가 직접 교체해야 한다.** 낡아 있으면 챗이 폐지된 조항
  (대필 금지 · 문답 세션 · 3계층 분업 · **3산업 순차 개방**)으로 답한다 — **그것이 낡았다는 신호다.**
  운영 블록 정본 = `본부\프로젝트운영블록.md` (v5.1 기준. **v6.0 대조 미반영**).

## 다음 할 일

1. **#08 검수 → 인천 저장소 push (운영자 · 데스크톱).** 본문·린터·차트는 전부 끝났다.
   **검수가 먼저다 — 이 둘은 한 동작이다.**
   ```
   python analysis/review_pick.py reports/report_08_관세청_인천항_수출입신고.md --log
   git log --oneline origin/main..HEAD
   git push
   ```
   뽑힌 수치 1건을 원본과 대조하고 결론 자리(제목·한 줄 결론·§1·§3)를 통독한 뒤 push.
   **인천이 허브보다 먼저다** — 허브 첫 발행이 인천 재동결을 발동시킨다.
   개수는 여기 적지 않는다(§8-3).

2. **허브 첫 push (운영자).** `git -C ..\jisangj03-dev.github.io push -u origin main`
   저장소 이름은 반드시 `jisangj03-dev.github.io` · Public.
   **push 직후 확인할 것 셋:**
   - 첫 화면이 「인천항을 공공 1차 데이터로 확인한다」인가 · `/data/`가 뜨는가
   - 내비게이션에 죽은 링크가 없는가(`/checks/`·`/terms/` 제거됨)
   - 인천 `report_07`의 래퍼 클래스가 `container-lg px-3 my-5 markdown-body` 그대로인가
     (인천 `_config.yml` push가 발행본 렌더를 바꿀 수 있다. 기준선 = `docs/렌더_기준선.md`)

3. **Claude.ai 프로젝트 지식 v6.0 교체 (운영자).** 위 X-CHECK 절.
   `본부\프로젝트운영블록.md`도 v6.0 대조가 안 됐다.

4. **채널 게시 (운영자 · 계정 조작).** 문안 = `본부\채널문안_인천항_20260828.md`.
   순서는 그 파일 §5. 한 채널을 돌리고 실소요를 잰 뒤 다음을 연다.
   ```
   python analysis/pilot_timer.py start linkedin summary
   python analysis/pilot_timer.py stop
   python analysis/pilot_timer.py report --md
   ```

5. **다음 편 주제 — `/data/`의 미관측 축에서 연다.** 열기 전에 **답할 질문 3개**를 커밋한다(§5).
   **질문 3개가 안 나오면 그 편은 열지 않는다.**
   **[2026-08-28 갱신] 후보 2건 다 열었다. `/data/` 「확인 대기」가 비었다.**
   - **인천청 「월별 항만운영통계」** — `probe_08_mof_monthly.py`.
     **공/적·수출입 방향 축은 없었다** → 공컨 시리즈 연장 불가.
     **터미널 운영사 단위(SNCT·HJIT·E1CT·ICT) 월별 TEU가 있었다** →
     미관측 축 「터미널별」의 **축 존재 물음은 닫혔다. 선커밋 질문 3개가 안 서서 안 연다.**
   - **NLIC 항만별 물동량 통계** — `probe_09_nlic_harbor.py`.
     인천항이 **외항(수출입·환적) / 내항(연안)**으로 갈린다. **단 톤·전체 화물이라 우리 TEU·컨테이너와 맞대지 않는다**(§3-9).

   **연안 축 — 물음이 두 번 좁아졌다. 그러나 안 닫혔다.**
   `probe_10_coastal_zero.py` (읽기 전용, 앵커 무변경 자가 증명):
   **2022년 9개월·2023년 4개월에는 값이 있었고 2024년부터 전부 0이다.** 마지막 비영 달 **2023-04**.
   → 「집계가 구조적으로 비어 있다」는 **약해졌다.** 같은 필드가 값을 낸 적이 있다.
   → **그래도 확정 아니다** — 2024년 어름의 분류·집계 변경 가능성이 남는다.
   **⚠ 이 관측은 선커밋 기준으로 못 쓴다 — 데이터를 이미 봤다**(§3-7).
   연안 축으로 편을 열려면 **아직 안 본 구간**에 질문 3개를 먼저 커밋해야 한다.

6. **[2026-08-28 신규] Web Archive 외부 타임스탬프 — push 직후 첫 작업.**
   실측: 우리 Pages·GitHub **둘 다 보존 이력 0건**이다.
   ```
   curl -sS "https://archive.org/wayback/available?url=https%3A%2F%2Fjisangj03-dev.github.io%2Fincheon-port-analysis%2F"
   ```
   → `{"archived_snapshots": {}}`. **선커밋 앵커가 전부 우리가 통제하는 저장소 안에 있다** —
   해시는 내용의 무결성을 증명하지만 **「그 시점에 존재했다」를 외부에 증명하지 않는다.**
   근거·비교 대상(AsPredicted) = `본부\항만사이트조사_20260828.md` §5-1.
   **공개 URL이 있어야 저장이 걸리므로 1·2번 push가 선행이다.**

## 미해결

- **인천 저장소에 전 페이지 푸터가 없다 — §2.2-1 미충족.** `_layouts`·`_includes`가 없고
  발행본 8편은 front matter 없이 primer 테마 렌더에 통째로 걸려 있다.
  **레이아웃을 덮어쓰면 8편의 렌더가 같이 움직이는데, 로컬 빌드 수단이 없어 사전 확인이 불가능하다**
  (사고 8·20: 확인 안 된 화면에 얹지 않는다). **현재 충족 경로는 편별 표기 블록(§2.2-2) +
  `/about` + README 표기 블록**이고, 전 페이지 푸터는 허브가 진다.
  **해석 여지가 있고 운영자 몫이다.** 푸는 방법은 Ruby+Jekyll 로컬 설치(운영자 조작) 하나다.
- **FACTS 대장 미등재.** #01~#06 결론 자리 중 창이 기록되지 않은 값들.
  수량은 `python analysis/lint_publish.py`로 읽는다. **[2026-08-26 실측]** WARN 97건 =
  서로 다른 값 73종, 2회 이상 등장은 17종. → 중복 정리로 줄어드는 몫은 1/4이고 나머지는
  값마다 출처에서 창을 확인해야 한다. **창을 추정해 채우면 사고 22의 재발이므로 일괄 등재하지 않는다.**
- **`07_판정결과.md` §4 ⓐ의 "확정 집계" 표기** — 잠정치 고지와 어긋나나 선커밋 문안 승계라 손대지 않았다.
- **2025년 전체 방향별 승격 보류** — 공식 공개 또는 기관 확인 시에만 해제.
- **연안항 구간** — 해석 미착수. **[2026-08-28 갱신]** 0이 2026만의 일이 아니다 —
  **2024년부터 전 구간 0이고, 2022~2023에는 값이 있었다**(마지막 비영 달 2023-04).
  「집계 공백」 후보는 약해졌으나 **분류·집계 변경 가능성이 남아 확정 아니다.**
  **이 관측은 선커밋 기준으로 못 쓴다**(데이터를 이미 봤다). 경위 = `analysis/probe/probe_10_coastal_zero.py`.
- **티스토리 69편 동결 사유 [미확인].** 동기 부재는 후보에서 제외됐다(운영자 정정).
  남은 후보: 제작 비용 · 소재 고갈 · 유통 부담. **축을 하나로 좁힌 지금 소재 고갈이 가장 큰 잔여 위험**이고,
  §5 편 게이트(질문 3개)가 그것을 겨냥한다. **원인을 모르는 채 건 대비책이다.**
- **v1.1·v1.0 본체와 구 마스터 v6.x 본체는 미확인.** v1.2만 확보했다.
- **[2026-08-28] 브라우저 연결은 세션마다 갈린다.** 같은 날 한 세션에서는 `check_browser.py`가
  「전제통과」인데 `tabs_context_mcp`가 **미연결**을 냈고, 다음 세션에서는 **붙었다.**
  **전제 검사와 연결 확인은 다른 것이고 둘 다 쳐야 갈린다** — 그 스크립트가 경고한 그대로다.
  **붙었다고 가정하고 계획을 세우지 않는다.** 원인은 도구가 안 닿는 자리라 단정하지 않는다.
