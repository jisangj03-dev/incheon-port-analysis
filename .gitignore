# 인증키가 담긴 파일 — 절대 깃허브에 올리지 않는다
config.py

# 파이썬 캐시 폴더
__pycache__/
*.pyc
_probe_dump.txt

# 로컬 미리보기 산출물 — `render_preview.py --out _preview` 가 만든다.
# **원본이 아니라 빌드 결과다.** 반입하면 소스가 둘이 되고, 둘은 반드시 갈라진다(사고 31).
# [2026-08-30] 실제로 `git add -A` 가 이것을 통째로 담을 뻔했다.
_preview/

# 힉스필드 스킬 — 벤더 도구다. 이 저장소는 발행본·원시 데이터·재현 코드만 든다.
# `npx skills add` 가 저장소 안에 깔고 `.claude/skills/` 로 심링크한다(2026-09-02 실측).
# 커밋하면 공개 저장소에 남의 에이전트 지시문 1.3M 이 들어간다. 도구는 도구 자리에 둔다.
.agents/
.claude/skills/higgsfield-*
skills-lock.json

# 개시 검사 소요 로그 — 이 기계의 계열이다. 사고 84 를 일화가 아니라 계열로 남긴다. 반입하지 않는다.
analysis/_boot_log.tsv

# 세션 스냅샷 — 훅이 턴마다 쓴다(analysis/session_snapshot.py). 이 기계의 관측이지 저장소의 상태가 아니다. 반입하지 않는다.
analysis/_session_snapshot.json
analysis/_session_snapshot.md
