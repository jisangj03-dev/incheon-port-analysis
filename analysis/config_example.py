# [사용법 안내용 템플릿 파일]
# 이 저장소를 내려받은 사람은 이 파일을 복사해 'config.py' 로 이름을 바꾼 뒤,
# 아래 따옴표 안에 본인이 공공데이터포털(data.go.kr)에서 발급받은 인증키를 넣으면 된다.
# config.py 는 .gitignore 로 제외되어 깃허브에 올라가지 않는다(키 노출 방지).
SERVICE_KEY = "여기에_본인의_공공데이터포털_인증키를_넣으세요"
